# Stage-1 LAS paper package (Meeting-4)

Show THESE files to Wang for the Meeting-4 Stage-1 discussion. Everything else in
this run (`../logs`, `../*.csv` tables, the debug/report/application figures at the
run root) is appendix / debug / evidence only.

Regenerated by `scripts/plot_las_paper_figures.py` from the existing measured CSVs in
this run (no benchmark was re-run; no number was invented or hand-edited). Source
timings: `../fair_paper.log`, `../fair_l2.log`, `../fair_l3.log`, `../fair_l5.log`
(10 runs x 1000 iters/op, mean +/- sample SD, single thread, -O3). Machine /
compiler / git provenance: `../metadata.txt`.

- `parameter_sets_paper.tex`                  Table 1 -- parameter settings (all 4 settings)
- `per_operation_timing_paper.pdf/.png`       Figure 1 -- per-operation computation timing (L3-like)
- `communication_components_paper.pdf/.png`   Figure 2 -- communication / serialized sizes (L3-like)
- `adaptor_overhead_paper.pdf/.png`           Figure 3 -- adaptor overhead vs ordinary op (all settings)
- `KEY_FINDINGS.md`                           2-3 sentence answer to the research question
- `paper_figure_manifest.csv`                 provenance + classification of every output

Optional appendix figure: `../appendix_package/rejection_sampling_paper.pdf/.png`
(explains the ~37%/attempt acceptance behind the timing variance; NOT a main figure).

`paper-derived / L2-like / L3-like / L5-like` are engineering benchmark settings only;
they are NOT formal NIST / ML-DSA security-equivalence claims.
