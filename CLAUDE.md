# Project context — LAS on Dilithium for blockchain

**This file is the single end-to-end context for the project.** It is written to be the
only thing a new session must read to know what the project is, what is done, what the
rules are, and where every other document lives. Everything below is either a *durable
rule/decision* (hand-written, changes rarely) or *live state* (auto-generated — see the
next section). No other file is required reading before starting work.

## 🔁 How this file stays current (read this once, then trust the file)

Live facts — branch, HEAD, wire sizes, KAT digest, evidence run ids, report word count,
last checkpoint, meeting coverage — live in the **🔄 Live project state** block below and
are regenerated by `scripts/update_claude_context.py`. That script only *reads* files and
git metadata: it never builds, tests, benchmarks, or estimates, and anything it cannot
parse from a real file prints as `(not found)`.

It runs automatically via hooks in `.claude/settings.json`:

| when | what happens |
|---|---|
| `SessionStart` | the block is regenerated **and** injected into the session context, so it is fresh even if `CLAUDE.md` was read first |
| `Stop` (end of every response turn) | the block is regenerated, so the session's own changes land in the file for next time |

Run it by hand any time with `python3 scripts/update_claude_context.py`
(`--check` exits non-zero when the block is stale; `--print` writes to stdout only).

**Never hand-edit inside the AUTO-CONTEXT markers** — those edits are overwritten. Do
hand-edit everything else when a decision, rule, or capability changes; that is the half
the script deliberately does not touch.

**Session-handoff protocol (this is how context stops being a silo):**
1. Durable change (new capability, new rule, changed decision, supervisor ruling) → edit
   the hand-written sections of *this* file in the same session it happens.
2. Work-in-progress detail (what was tried, what broke, exact next step) → append a short
   checkpoint to `PROGRESS.md` (`/checkpoint` skill). Never put narrative here.
3. Live numbers → do nothing; the hook picks them up.

### 🔔 STANDING DUTY — update this file *mid-session*, unprompted (Royce, 2026-08-03)

**Royce must never have to re-explain the same thing in a new session.** When
project-critical context appears in the middle of a conversation, write it into this file
**at that moment** — do not wait to be asked, do not defer it to the end of the session,
and do not assume the next session will re-derive it. A session that ends without
recording what it learned has wasted the learning. His stated stake: *without Claude
understanding the whole project context, the report cannot meet Wang's standard.*

**Triggers — when any of these happens, stop and edit this file before continuing:**
- a **supervisor ruling or meeting outcome** (scope change, terminology ruling, a
  deliverable added/dropped, something explicitly accepted or rejected);
- a **decision Royce makes** that constrains future work ("do it this way", "don't touch
  that", "this is out of scope", a chosen trade-off) — including one that *overrides*
  something already written here;
- a **correction of a belief recorded here** — a number, a name, a claim, or a status
  that turns out to be wrong or superseded;
- a **new capability or artefact** (module, crate, script, runner, evidence stream) or a
  component becoming **deliberately dead / must-not-repair**;
- a **rule with teeth**: a naming/notation convention, a gate that must never be weakened,
  a framing that must not drift, a trap that has already been fallen into once;
- an **owed explanation or open question** the next session would otherwise rediscover
  from scratch.

**How to record it:** put it in the section it belongs to (scope → scope discipline;
naming → naming convention; capability → status; supervisor ruling → the meeting block),
in the smallest edit that carries the *reason*, not just the fact — a rule without its
why gets argued away later. Say in the reply that CLAUDE.md was updated and why. When a
ruling comes from a meeting, also merge it into `las-context-consolidated.md` (the spec)
so the two agree.

**What does NOT belong here:** live numbers (the hook handles them), session narrative
(that is `PROGRESS.md`), and anything the repository already records — code structure,
git history, past fixes.

## ⚠️ FOCUS — the primary comparison (READ FIRST; this has been gotten wrong repeatedly)

**The Stage-1 focus / primary comparison is BASIC SIGNATURE vs LAS ADAPTOR — the cost of
adding the adaptor layer to the base signature.** Everything in the results/evaluation
must lead with this. Precisely (`las-context-consolidated.md` §13.5, §7(e), §14.3):

- **Base-signature path:** Sign / Verify with `c = H(pk, w, M)` (no statement `Y`).
- **LAS adaptor path:** PreSign / PreVerify / Adapt / Extract with `c = H(pk, w+Y, M)`.
- Headline result = the **adaptor overhead**, reported **per operation** at the target
  setting **Simplified Dilithium-III**: PreSign vs Sign `+6.7%`, PreVerify vs Verify
  `+3.1%`, Adapt vs Verify `+8.1%`; Extract has no basic analogue. The two paths share
  algorithm/parameters/primitives, so the difference is purely the adaptor cost.
  (Percentages are indicative of the settled *shape* of the result — single-digit
  per-operation overhead — and move by ~1–2 points between runs on the same machine;
  the authoritative current figures are always the regenerated report macros
  `\ovPreSign` / `\ovPreVerify` / `\ovAdapt` from the evidence run named in the
  live-state block. Run `20260804_101750` measured **+6.6% / +4.0% / +6.9%**. Never
  retype a percentage from this paragraph into the report.)

**NOT the focus — do not drift here:** the four parameter sets (paper / D2 / D3 / D5) are
ONLY a *secondary fairness / parameter-sensitivity* axis (§13.4). Do **not** frame the
results around "across security parameter", "as the scheme scales", or make the parameter
sweep the headline. The across-parameter overhead chart is supporting/appendix material,
**not** a primary body figure.

**Timing rule:** per-operation timing is the PRIMARY timing result (§14.3) — never lead
with cumulative / end-to-end time.

**Presentation rule (Royce):** no table↔chart redundancy (chart in body, exact-number
table in appendix — *except* where Meeting 8 overrides: figures belong embedded in the
text, not relegated to the appendix); no abbreviations of scheme/level names in
tables/figures.

### ⚠️ WORD COUNT — trust `make -C report/latex wordcount`, never a stale `word.count`

**The stored `word.count` was 8,955 while the real body count was 10,361** (found
2026-08-04). `PROGRESS.md` had been planning trims against the stale figure for several
sessions. Always regenerate before reasoning about budget. Facts that matter:
- The count is body only: `%TC:ignore` fences in `report.tex` exclude the frontmatter
  TODO, the **appendix** and the bibliography; the `-sum=1,1,0,0,0` weights exclude
  **captions**. So appendix and captions are FREE, and the rubric agrees.
- **Tabular bodies and prose DO count; TikZ picture content and `generated/*.tex` tables
  do NOT.** Moving a reference table into the appendix is therefore the cheapest real
  saving — done for `tab:tests`, `tab:notation` and `tab:tiers` (now `app:tests`).
- Absolute ceiling is **9,000** (Royce). Current state: **8,990**, PDF builds with zero
  undefined references.

<!-- BEGIN AUTO-CONTEXT — regenerated by scripts/update_claude_context.py. DO NOT EDIT BY HAND: edits here are overwritten on the next session start. -->

## 🔄 Live project state (auto-generated)

*Regenerated 2026-08-04 12:41 by `scripts/update_claude_context.py`, which only reads files and git metadata — it never builds, tests, or benchmarks, and never estimates a number. Anything it could not parse says (not found).*

### Repository right now

- Branch **`report`** · HEAD aa71cbf · 2026-07-31 · utxo bitcoin implementation correction real tx vs paper tx
- Working tree: 96 modified tracked file(s), 0 untracked path(s) · no upstream tracking branch
- Recent commits:
  - `aa71cbf 2026-07-31 utxo bitcoin implementation correction real tx vs paper tx`
  - `4aef1f7 2026-07-29 meeting 7`
  - `53d8d05 2026-07-29 chore: stop tracking Rust build artefacts`
  - `a8b9b10 2026-07-29 partial stark fix hash challenge size`
  - `b58a7f3 2026-07-25 report utxo`

### Target parameter set — anchors parsed from source

- Simplified Dilithium-III / ML-DSA-65-aligned (n=6, ℓ=5, κ=49): `c_tilde` 48 B · public key 4416 B · secret key 704 B · signature = pre-signature 6736 B (`ref/serialize.h` static assert)
- Other sets (signature bytes): D2-aligned 4640 B · D5-aligned 9184 B · paper reproduction 4640 B
- Pinned KAT digest `b4a10ffb…03be` — C and Rust agree ✅
  - full: `b4a10ffb6e645e5076d1ff5993faa72909232fc71e554b93544141d6590503be`

### Latest measured evidence (pointers only — never retype a number)

- Stage-1 benchmark suite: `evidence/latest` → `runs/20260804_101750` (dir mtime 2026-08-04)
- Stage-2 UTXO swap: `evidence/stage2/latest` → `20260730_162109` (dir mtime 2026-07-30)
- On-chain gas (EVM): `evidence/onchain/latest` → `20260730_164836` (dir mtime 2026-07-30)
- Criterion micro-bench: `evidence/criterion/latest` → `20260730_165134` (dir mtime 2026-07-30)
- las-stark: `evidence/stark/latest` → `20260729_175637` (dir mtime 2026-07-29)
- Report word count: **8990** (`report/latex/word.count`, rubric bound 7,000–9,000; `make -C report/latex wordcount`)

### Where the last session stopped

- Last checkpoint in `PROGRESS.md`: Checkpoint — 2026-08-04 — LaZer amortisation measured; the question is now CLOSED
- Next action recorded there:
  - Trim 8,955 -> 8,000 (955 words). Duplication/filler exhausted; remaining source is the
  - ~40 sentences over 40 words plus further high-level-ing of sec 2.6 (848 w) and 2.7.
  - Fill the frontmatter \TODO (student id, prior degrees, acknowledgements) -- Royce only.
  - ## Checkpoint — 2026-08-03 — remaining-work sweep: AMHL cleanup, Adapt gap, ML-DSA hint experiment, EVM/IPFS write-ups
  - Branch: report. No commits made. Royce mid-session: "skip the report for the moment,
- `CONTEXT.md` (long-form handoff): CONTEXT — session handoff (updated 2026-07-29; ninth-session update first)

### Supervisor meetings on record

- Cleaned transcripts present: 1, 2, 3, 4, 5, 6, 7, 8 (`meetingN_cleaned_transcript.md`)
- Merged into `las-context-consolidated.md` (the objectives spec): meetings 1, 2, 3, 4, 5, 7, 8
- ⚠ **NOT in that file**: meeting 6 — read the transcript/summary directly before planning work it touches (meeting 6 is held in `docs/04-evaluation/SUPERVISOR_DELIVERABLES_GAP.md` by design)

### Freshness tripwires

- ⚠ Source newer than Stage-1 evidence: `ref/relation_zk_lazer_batch.c` (2026-08-04 12:16) > `evidence/latest` (2026-08-04 10:19). Numbers in the report may predate the code — re-run the suite before quoting them.
- `CLAUDE.md` hand-written sections last touched 2026-08-04.

<!-- END AUTO-CONTEXT -->

## One-line goal
Implement LAS (Lattice-based Adaptor Signatures, eprint 2020/845) by reusing the
CRYSTALS-Dilithium reference primitives, then demonstrate it in a post-quantum
blockchain **atomic-swap** scenario, with everything benchmarked and documented.

## Why this project exists
- Blockchains sign with ECDSA/Schnorr; Shor's algorithm breaks both. "Post-quantum"
  = built on lattice/hash problems Shor can't solve.
- NIST standardised *basic* PQ signatures (Dilithium, Falcon, SPHINCS+).
- *Exotic* signatures (multisig, ring, group, **adaptor**) add features but in the
  PQ setting are mostly **paper-only** — little working code, none on a blockchain.
  Closing that gap is the thesis.
- Adaptor signatures enable atomic swaps / payment channels (scriptless scripts).

## Key design fact
An exotic scheme = a basic scheme + extra functions. LAS = Dilithium-style
Fiat-Shamir-with-aborts signature + PreSign / PreVerify / Adapt / Ext. We **reuse
Dilithium's poly/NTT/SHAKE/sampling internals** and do not reinvent lattice
arithmetic. LAS itself is built as a small *self-contained* scheme (its own
dimensions and parameters) layered on those primitives.

## The LAS mechanism (variant B — the paper, Algorithm 2)
Earlier notes described a "variant A" (`z̃ = z + y`, statement subtracted at
verify). That was **superseded**: the paper specifies variant B, implemented here.
- Statement/witness `(Y, y)` is **literally another key pair**: `y ← S_1^{n+ℓ}`
  (ternary), `Y = A·y`. Knowing `Y` doesn't reveal `y` (Module-SIS/LWE hard).
- **The core mechanism: the statement is folded into the Fiat–Shamir hash.**
  Sign uses `c = H(pk, w, M)`; **PreSign uses `c = H(pk, w + Y, M)`**.
- `PreSign(sk,Y,M)`: `ẑ = y + c·r`, reject if `‖ẑ‖∞ > γ−κ−1`. Pre-sig `σ̂=(c,ẑ)`.
- `PreVerify(Y,pk,σ̂,M)`: recompute `w' = Aẑ − c·t`, check `c == H(pk, w'+Y, M)`.
- `Adapt((Y,y),σ̂)`: `σ = (c, ẑ + y)`. Now standard `Verify` sees `Az−ct = w+Y`,
  which matches `c` — so the adapted signature is a **fully ordinary** signature.
- `Ext(Y,σ,σ̂)`: `y = z − ẑ`; return it iff `A·y == Y`.
- **On-chain leak (why swaps are atomic):** publishing the adapted `σ` lets anyone
  holding `σ̂` recover `y = z − ẑ` and complete the matching half of the swap.
- **Wire form (Stage B):** what travels is the challenge *digest* `c_tilde`, and
  `c = SampleInBall(c_tilde)` is re-derived locally — the FIPS 204 construction. The
  digest width follows FIPS 204's `λ/4` rule for the aligned set (see naming section).

## THE failure mode to watch (variant B)
The bound budget, not packing. PreSign rejects at the **tighter** `γ−κ−1`; the
ternary witness has `‖y‖∞ ≤ 1`, so the adapted `z = ẑ + y` satisfies
`‖z‖∞ ≤ γ−κ` and clears ordinary Verify. If you loosen PreSign to `γ−κ`, adapted
signatures can exceed the bound and Verify rejects everything. (`γ = κ·d·(n+ℓ)`
governs the MSIS hardness parameter; acceptance is ≈37% per attempt, `≈ e^{−1}` —
expected for the simplified scheme without hint vector, and measured directly via the
`las_attempts` counter, never inferred from timing ratios.)

**Rejection gate (never weaken or rename):** every C and Rust benchmark driver
hard-asserts measured attempts/call against exact theory — **Sign 2.71875**,
**PreSign 2.77483** at the target set, 5σ tolerance. A run that drifts off theory fails
loudly instead of quietly producing a publishable-looking number.

## Known caveat (note in thesis, do NOT need to solve)
"Knowledge gap": here the extracted `y` is **exact**; in the paper's relaxed
setting the witness can carry noise that grows across long payment-channel chains.

## Modulus note
The LAS paper uses `q ≈ 2^24`; this build uses **`Q = 8380417 ≈ 2^23`, which is FIPS
204's modulus** — the one the reused ML-DSA NTT's root-of-unity table is fixed to.
Per the parameter-authority rule above, **that is the correct choice, not a shortfall**:
NIST is the parameter authority, so the report frames `Q` as following FIPS 204 rather
than as a deviation from the paper. `Q > 2γ`, so correctness holds; only the concrete
MSIS/MLWE security margin differs from the paper's (analysis out of scope per
supervisor). Reaching `2^24` exactly would need a new NTT table or schoolbook
multiplication — and is **not** a project goal.

## Status — what exists today

Live sizes, digests, evidence ids and word counts are in the auto block above; this
section says **what is built and what it means**, not the numbers.

**Stage 1 — the scheme and its benchmark (complete).**
- **LAS scheme (variant B)** in C (`ref/las.c`, `ref/basesig.c`, `ref/setup.c`,
  `ref/relation.c`) and Rust (`rust/fips204-las/src/`). The Rust port is the naming
  **authority** and is KAT-locked to the C build byte-for-byte.
- **Correctness:** `test_las` (1000 iters), `test_basesig`, `test_contract` (itemised
  8-point contract), `test_serde` (every single-byte flip rejected), `test_kat`
  (pinned cross-language digest).
- **Serialization + byte-level verifier** — `ref/serialize.{c,h}`: six typed pack/unpack
  pairs, a *validating* decoder, and `base_verify_packed` = the byte interface an
  on-chain verifier consumes. Wire form is `c_tilde ‖ BitPack(z)`; `z` is ~99.3% of the
  signature.
- **Deterministic API + pinned KATs** — `base_keygen_seed` / `base_sign_det` /
  `las_presign_det`, mask seed `SHAKE256(tag‖sk‖[Y]‖M)`. Reproducible across machines.
- **Benchmarks:** `bench_levels` (the primary fair base-vs-LAS comparison, ≥5 runs,
  mean±SD), `bench_las`, `bench_compare` (context only: optimised Dilithium is *not*
  algorithm-matched), `bench_classical` (ECDSA adaptor via vendored `secp256k1-zkp`),
  plus a Rust Criterion harness. Two baselines, per Meeting 2.
- **Classical-baseline headline:** the price of post-quantum here is **communication,
  not computation** (sizes ×29–89); LAS's own adaptor overhead is small, where the
  classical adaptor pays ~4× for its DLEQ proof.

**Stage 2 — the application (complete, Bitcoin/UTXO).**
- `rust/las-swap/` implements the paper §4.1 Fig. 1 atomic swap over a UTXO ledger
  model (the ledger takes the signature algorithm as a parameter, exactly as §4
  assumes), and benchmarks **three configurations**: (1) classical ECDSA adaptor,
  (2) LAS + Groth16, (3) LAS + LaZer. Honest path plus timeout/refund; everything
  derives from one pinned master seed; measured on **time + communication**, including
  off-chain messages.
- Attribution rule baked into the write-up: **2→3 is the controlled comparison** (same
  signature, same relation, only the prover differs — lead with it); 1→2/3 is a
  whole-stack comparison, *not* the cost of the PQ signature alone.
- **Bitcoin transaction structure** (Meeting-8 action, done): `scripts/gen_bitcoin_tx_data.py`
  projects the measured object sizes onto Bitcoin's real wire format (BIP141/144/341),
  self-checking against two published vB figures before it will emit anything →
  `generated/btcmacros.tex` + `tab:btctx`, with the original-vs-modified transaction
  diagram in Ch. 2. Prose: `docs/02-methodology/BITCOIN_TX_STRUCTURE.md`.
- **Legacy C Stage-2 is deliberately dead:** `ref/amhl.{c,h}`, `ref/chain.{c,h}`,
  `ref/test/{test_contract,test_swap,test_pcn,bench_app}.c` still use the pre-seven-type
  API and **do not compile**. Royce ruled them superseded by the Rust Stage-2
  evaluation — **do not repair them**. `STAGE1_ONLY=1` skips them and still regenerates
  everything the report consumes.

**Proof of knowledge π (Fig. 1), Royce-directed scope extension.**
`ref/relation_zk.{c,h}` (`relation_prove` / `relation_proof_verify`, non-ternary
witnesses refused) over the **vendored LaZer** library, with `ref/relation_zk_lazer.c`
as the only TU that includes `lazer.h`. Ternary handled by binary decomposition
`[A|−A|0]·(r₊‖r₋‖e)=t′`. Params `ref/relation_zk_params.h` are committed, generated by
LaZer codegen from `scripts/las_pi_params.py`. Rust twin `relation_zk.rs` FFIs the same
C bridge behind cargo feature `relation-zk` (default off, KAT gate intact). π is
off-chain only. Opt-in targets — needs LaZer built once.

**On-chain verification (EVM) — retained as evidence, not as the deliverable.**
`evm/src/LASVerifier.sol` is a complete native verifier (reusing vendored ZKNox
ETHDILITHIUM SHAKE256/NTT/SampleInBall), validated end-to-end against C: accepts the
golden adapted signature, rejects tampering, bound by a fund-time `keccak256(A′,t,M)`
commitment. It costs **≈56.6 M gas** — far above EIP-7825's per-transaction cap of
16,777,216 — which is precisely *why* Stage 2 moved to Bitcoin/UTXO. A Naysayer
(optimistic) variant is also implemented. Gas figures come only from a captured
`forge --gas-report` log parsed by `scripts/plot_onchain_gas.py`; nothing is hardcoded.

**`rust/las-stark` — post-quantum succinct-proof exploration (honest status: WIP).**
A Winterfell FRI-STARK proving the *arithmetic core* of `base_verify` at the real ring
degree (norm bound + `w' = z_top + A′·z_bot − c·t`). The Fiat–Shamir hashes are **not**
in the AIR — `c` and `w'` remain public inputs — so `z` is bound to `(A′,t,c,w')` but
**not** to `(c̃, M)`. It is a gadget, not a complete proof of on-chain verification;
never describe it as one. No Groth16 wrap: that would defeat post-quantum security.

**⚠️ AMHL is DROPPED (Royce, 2026-08-03).** Multi-hop locks are **out of the project** —
not a bonus, not future work, not a deliverable. Do not build on `ref/amhl.{c,h}`, do not
revive it, and do not re-add it to any status list or work queue. Its C code stays in the
dead-legacy set above.

*Cleanup owed (surveyed 2026-08-03, not yet done).* Distinguish two cases:
**(a) project-artefact claims — must go:** `report/latex/chapters/02-methodology.tex`
(the `\texttt{amhl}` row in the components table), `03-results.tex:369` and
`A-appendix.tex:292` (multi-hop presented as a demonstrated result), plus the AMHL
entries in `docs/STATUS.md`, `docs/LAS.md`, `docs/02-methodology/FUNCTION_MAP.md`,
`THEORY_IMPL_BRIDGE.md`, `C_RUST_IMPLEMENTATION_AND_BENCHMARK_METHODOLOGY.md`,
`STAGE2_UTXO_SWAP_PLAN.md`, `walkthrough/00-diagrams-and-summary.md`,
`docs/03-results/LAS-08-performance-measured.md`,
`docs/A-appendix/REPRODUCE_LAS_RUST.md`, `docs/04-evaluation/PROJECT_HISTORY_EXPLAINED.md`,
`docs/DOCS_BY_CHAPTER.md`, `docs/01-introduction/LAS-01-introduction-and-motivation.md`.
**(b) literature/background mentions — may stay** if they describe *other people's*
work and claim nothing about this project (e.g. `01-introduction.tex:49,87`, where
multi-hop locks are cited as what adaptor signatures enable in general). Check each
occurrence against that test rather than deleting the word wholesale.

**Reproducibility spine:** `README.md` (build/run/reproduce, upstream commit `2374d22`
and toolchain recorded), `docs/02-methodology/FUNCTION_MAP.md` (every Dilithium function
classified call-as-is / modify / new — headline: **zero upstream functions modified**),
two-branch diff view (`dilithium-baseline` vs `main`), and the runner scripts under
`scripts/` that each write a timestamped evidence directory with raw tool output, an
environment/commit record, a `latest` symlink, and automatic macro/figure regeneration.

**ML-DSA adaptor experiment — COMPLETE 2026-08-03 (three binaries, all gated).**
LAS built on FIPS 204 **as specified** (hint vector, Power2Round, high/low-bit split all
enabled). Zero upstream functions modified; the control verifier is the stock
`crypto_sign_verify`. `ref/mldsa_las.{c,h}` is a structural mirror of `ref/sign.c` and
now carries the full scheme incl. a wire format (statement = 23-bit codec since
Power2Round cannot compress `Y`; witness = ML-DSA's own polyeta codec; both decoders
validate).
- `test_mldsa_hint{2,3,5}` — diagnostic: WHICH feature breaks a naive port (a FAILS row
  is a result).
- `test_mldsa_las{2,3,5}` — itemised contract, **13/13 at all three sets**, incl. four
  tamper rejections, malformed-input rejection, wire round-trip and determinism.
- `bench_mldsa_compare{2,3,5}` — head-to-head vs the simplified scheme, **both
  constructions in ONE binary** (no cross-run caveat; name spaces don't collide).
Runner `scripts/run_mldsa_hint_experiment.sh` → `evidence/mldsa_hint/<ts>/`; write-up
`docs/03-results/MLDSA_HINT_EXPERIMENT.md`. **It corrected the project's own claim** —
see the hint bullet in Scope discipline. Not linked into the benchmarks or the KAT; its
numbers must never be mixed with `evidence/latest/`.

**⚠️ Measurement rules this experiment established (do NOT weaken them).** Four faults
were caught by gates, each of which would otherwise have shipped a plausible-looking
wrong number: (1) omitting FIPS 204's 2-byte empty-context prefix `{0,0}` from `mu` made
P4 fail 0/200 in *both* variants and looked like the headline finding; (2) re-signing one
fixed instance made ML-DSA's rejection loop deterministic (*exactly* 4.0000 / 2.0000
attempts/call); (3) the KeyGen benchmark replaced the keypair, so `Verify` was timing the
*rejection* path on a stale signature; (4) measuring Sign and PreSign in separate blocks
let clock drift invert the overhead (−3% to +8%, sometimes negative). Consequently:
**overhead ratios must be PAIRED and INTERLEAVED within each repetition**, sign-class
pairs compared **per attempt**, every timed block followed by a **success-path
assertion**, attempt counters checked to actually track their loop, and an untimed
**warm-up** before the first measurement. `bench_mldsa_compare` exits non-zero if any of
these fails.

**⚠️ TWO FUTURE-WORK ITEMS PROMOTED TO IMPLEMENTED WORK (Royce, 2026-08-04).** Royce was
told explicitly that this **breaks the Meeting-8 feature freeze** ("no further measurements
asked for", polish what exists) and that it competes with the two remaining deliverables
(report polish, the 6–8 min slides). He chose it anyway — that is his call and it stands.
Consequence for the report: **`Shrink the statement, not the signature` and `Reduce the
proof` at `05-conclusion.tex:118-129` are no longer purely open questions** and must be
rewritten once the runs exist; do not leave them phrased as untested directions.

- **Statement compression — `ref/test/test_statement_compress.c`** (+ `test/test_statement_compress{2,3,5}`,
  in `all`). Diagnostic in the ML-DSA-hint shape: a FAILS row is a result. Runs three
  candidates — full statement (control), Power2Round-style truncation swept over
  b=1,2,4,8,13, and seed-instead-of-`Y` — through Gen→PreSign→PreVerify→Adapt→Verify→Ext,
  giving every adaptor call the **compressed** statement. Hard gate: the control must hold
  the contract or nothing is attributable. Decisive rows are **P3 (base Verify accepts the
  adapted signature — the chain)** and **P4 (Ext recovers the witness — atomicity)**.
  It exists to convert `test_mldsa_hint`'s structural finding (d) — "the statement is not
  compressible the way the public key is" — from an assertion into a measurement, the same
  move the hint experiment made. Runner `scripts/run_statement_compress.sh` →
  `evidence/statement_compress/<ts>/`; write-up `docs/03-results/STATEMENT_COMPRESSION_EXPERIMENT.md`.
  - **✅ RUN 2026-08-04, all three sets, 100 iters — the assertion is CONFIRMED, and now
    has a mechanism.** Truncation: **P1 PreVerify and P2 Adapt hold 100/100 while P3 Verify
    and P4 Ext fail 0/100 — at EVERY depth including b=1** (a 4% saving). It is not a
    marginal failure a tolerance could absorb. Reason, and this is the quotable form:
    *truncation is invisible to the adaptor's own functions (both sides hash whatever
    statement they were handed) and fatal at both boundaries — `base_verify` never sees a
    statement and recomputes `Az−ct = w+Y` with the TRUE `Y`, and Ext's acceptance test IS
    the exact relation `A s == Y`.* Seed candidate: 4416 B → 32 B (**138×**), every
    functional property 100/100 because `Y` is reproduced exactly — **and the receiver
    recovered the witness 100/100**, so the largest available compression is a total break,
    demonstrated not argued. Hint-style repair is **net +0 B at every depth by
    construction**. Lossless baseline already in place: 23-bit packing, 4416 B vs 6144 B
    (−28%) — the figure any future proposal must beat. **Do not reopen `Y` compression
    inside this construction; the open question is a different hard relation whose statement
    is smaller by design.**
- **Proof amortisation — `rust/las-swap/src/bin/bench_amortise.rs`** + `BatchedRelationCircuit`
  in `groth16_circuit.rs`. One Groth16 proof over k=1,2,4,8 instances, reporting totals and
  per-swap cost. **The relation was factored into `emit_instance`, shared by the single and
  batched circuits, so a batch cannot prove something weaker than k single proofs** — and a
  per-batch tamper check on the LAST instance's public input enforces that in the
  measurement. Uses the same master seed and `subseed` tags as `bench_swap`, so the
  statements are the swap's own. Runner `scripts/run_amortise_bench.sh` →
  `evidence/amortise/<ts>/`; write-up `docs/03-results/PROOF_AMORTISATION_EXPERIMENT.md`.
  **Groth16 only** — LaZer's proof grows with the relation and batching it would need
  `relation_zk_params.h` regenerated; not measured, no claim made.
  - **✅ RUN 2026-08-04 (k=1,2,4,8 × 5 reps) — a NEGATIVE result, and report it as one.**
    The proof is **128 B at every k**, so per-swap proof bytes fall exactly `1/k`
    (128→64→32→16). **But 128 B was never the bottleneck.** In `evidence/stage2/latest`
    configuration 2 spends **645,621 µs in Prove(π) = 97.3% of the swap and 47.1× the next
    phase** (both figures recomputed from that log, not asserted), against a 128 B proof
    beside a 4416 B statement and 6736 B signature. So **batching amortises the cost that
    was already negligible and leaves the dominant one untouched.** Per-swap proving is
    **flat to slightly worse** — this run 494→653 ms (+32% at k=8); across the three runs
    captured that day it was variously flat and rising, **never better**, so do not claim a
    clean monotone law. Verification per swap is flat (~12 ms). Setup grows with the batch
    (667→5687 ms) and is per circuit. **The correct framing for the report:** this is a
    statement about *Groth16*, not about batching — the same `1/k` would land on a cost that
    matters for a large-proof/cheap-generation system, which is LaZer's profile.
  - **✅ LaZer MEASURED TOO 2026-08-04 (Royce: "uji LaZer sekarang") — batching fails for
    BOTH provers, for OPPOSITE reasons. This closes the amortisation question.** New
    capability: **batched π** — `ref/relation_zk_batch.{c,h}` (block-diagonal statement: k
    copies of `[I|A'|−I|−A'|0]` on the diagonal, off-block entries zero, so the batch is the
    conjunction of k copies of the *deployed* statement) + bridge
    `ref/relation_zk_lazer_batch.{c,h}` (the **second and only other** TU that may include
    `lazer.h`) + committed generated sets `ref/relation_zk_params_k{2,4,8}.h` ⇐
    `scripts/gen_lazer_batch_params.sh` (needs SageMath, which **is** on this machine at
    `~/micromamba/envs/lazer-sage/bin/sage` — earlier notes implying it was unavailable were
    wrong). Benchmark `ref/test/bench_lazer_amortise.c`, runner
    `scripts/run_lazer_amortise.sh` → `evidence/lazer_amortise/<ts>/`.
    **k=1 dispatches to the COMMITTED `las_pi_params`**, so the baseline row is the shipped
    prover, not a lookalike. **New gate names `PI_BATCH_*` — they do NOT rename or alias
    `PI_ROWS`/`PI_COLS`/`PI_DEG`/`PI_PROOF_MAX_BYTES`, which stay the k=1 module's.**
    **Result:** proof/swap 30723→17645 B (**0.57×, −43%**) — a real saving on config 3's
    dominant *communication* object, and the first real saving in this direction. **But
    per-swap prove+verify is 3.33× WORSE** (prove 159→445 ms, verify 75→335 ms): LaZer's
    work grows **superlinearly** in the batch. Since the role-A proof is already **98.6% of
    config 3's end-to-end time**, the binding constraint is compute, and batching buys bytes
    by spending exactly that. **Verdict: a genuine trade-off, and the wrong way round here.**
    ⚠️ **NOT wired into the swap** — config 3 still uses the k=1 module; deployed prover, KAT
    and every benchmark of record untouched. Batched param sets are **not independently
    reviewed** and no security claim is made about batching.
- **NOT attempted, and why** (both were in the same request; neither is a shortfall of
  effort): *analyse the ML-DSA variant's security* is barred by the standing supervisor
  ruling that security proofs/analysis are out of scope — it is a reduction, not code;
  *solve one-transaction verification* needs a deployed SHAKE256 precompile, a Merkle-opened
  dispute or a succinct proof of verification, i.e. a new artefact well beyond the freeze
  and beyond the time left. Both stay in Chapter 5 future work.
- **Both RUN 2026-08-04 on Royce's explicit instruction** → `evidence/statement_compress/latest`
  and `evidence/amortise/latest`; both write-ups now carry the measured numbers. All gates
  passed: the compression control held at every set, and the amortisation binary's
  success-path assertions and per-batch tamper checks all passed (exit 0).
  ⚠️ `evidence/amortise/` holds **three** runs from that day — the first two were superseded
  by a correction to the binary's own closing interpretation (it wrongly said the swap was
  proof-dominant in *both* time and communication; it is dominant in time only) plus a `%%`
  format bug. **`latest` is the one to quote.**

**EVM/Naysayer and IPFS — write-ups DONE 2026-08-03**, both as Meeting 8 framed them.
`docs/02-methodology/EVM_TX_STRUCTURE.md` is the EVM counterpart of the Bitcoin
transaction breakdown (same shape, field by field), answering "where do the added
components go" for Ethereum: the adaptor adds **no on-chain field at all** in the honest
path, the whole classical-vs-PQ on-chain difference is signature size, and Ethereum
needs *no consensus change* (a LAS signature is application payload inside `data`, never
the transaction's own secp256k1 signature) — the mirror image of Bitcoin, which needs a
consensus change but then meters by size rather than execution. Naysayer verdict recorded
as a **negative result**: the honest path fits (1.2 M gas) but the `naysayDigest` fraud
proof is 1.7× over the EIP-7825 cap, and a fraud proof that cannot be mined is not one.
`docs/04-evaluation/IPFS_OFFCHAIN_STORAGE.md` records IPFS as a **fallback, not adopted**
— headline: the swap needs none of it (π is a direct party-to-party message), and for the
optimistic verifier a data-availability failure becomes a *soundness* failure. No new
measurement was taken for either; both quote `evidence/onchain/latest`.

**What remains** (verified 2026-08-03): report polish (word count, and the frontmatter
TODO — student id, prior degrees, acknowledgements: Royce only) and the **6–8 minute
presentation slides**. Also owed in the report, deferred by Royce on 2026-08-03 to focus
on implementation first: fold the ML-DSA hint result into Ch. 4 (it **corrects** the
"reference optimisations fight the adaptor identity" claim at `04-evaluation.tex:169`)
and rewrite the Ch. 5 future-work list (drop the `q≈2^24` migration bullet — it is
DROPPED, not future work). **Added 2026-08-04:** run the two new experiments
(`scripts/run_statement_compress.sh`, `scripts/run_amortise_bench.sh`) and rewrite the two
Chapter 5 bullets they answer — see the promoted-future-work block above. Done since
Meeting 8: the ≈270× `Adapt` explanation, the ML-DSA hint experiment, the EVM and IPFS
write-ups, the AMHL cleanup, and the statement-compression + proof-amortisation
experiments (built, not yet run). None of what remains is optional — see the work-priority
rule below. Already satisfied from Meeting 8: the transaction breakdown + diagrams, the
"signature hash" vs "transaction" terminology split, Chapter 5's title, and
functions-not-protocols wording.

### ⚠️ WORK-PRIORITY RULE — nothing here is "optional" (Royce, 2026-08-03)

**Every remaining item is mandatory; they differ only in ORDER.** Do not label anything
"optional", "if time permits", "stretch", or "bonus" in this file, in the docs, or in the
report's future-work section — that framing was wrong and is retired. Rank the queue by:

1. **What Wang instructed or prioritised** — his stated priority always goes first;
2. **Low-hanging fruit** — cheapest to finish, so the queue keeps shrinking;
3. **Novelty value to the report** — what earns marks by being genuinely new;
4. **Feasibility** — what can actually be completed in the time left.

Derived ordering as of 2026-08-03 (re-derive it whenever Wang speaks again): report
polish + the 6–8 minute slides (Wang's explicit ask) → the `Adapt` ≈270× explanation
(cheap, and he asked for it) → the **NIST ML-DSA hint experiment** (Wang named it for the
week after next, and it carries the most novelty — it converts an asserted claim into a
demonstrated one) → EVM/Naysayer (Wang: only once Bitcoin is fully finished) →
IPFS-fallback write-up (documentation-only). When in doubt about order, ask Royce rather
than silently reordering.

## ⚠️ MEETING-8 (2026-07-31) — LATEST WORD: freeze features, break down the transaction

Authority: `las-context-consolidated.md` **§17** (merged 2026-08-03) · transcript
`meeting8_cleaned_transcript.md`. Governing instruction: *"You don't need to contain all
the stuff — you just need to make sure that what you have done looks good, looks perfect,
looks great."*

- **Results are accepted; stop measuring.** Groth16 = slower to generate, smaller proof;
  LaZer = fast generation, much larger proof; PQ proof sizes ≈30× classical (≈300× on
  on-chain components). Wang: *"it's what we expected."* **No further measurements asked
  for.** Configuration 1 legitimately carries no π (the classical protocol specifies
  none — only DLEQ).
- **THE remaining requirement: break down the Bitcoin transaction** — what a standard
  transaction contains (from Bitcoin's own definition, *not* the LAS paper, which "is a
  very oversimplification of the architecture"), exactly which components the adaptor
  layer adds (pre-signature, statement `Y`, witness; π stays off-chain), and **two
  diagrams — standard vs modified transaction** with changed fields highlighted. That
  breakdown is what justifies the reported communication-size increase. Also confirm how
  the witness is really carried on-chain. Wang: *"that's the most important thing
  remaining."* (Implemented — see Stage 2 above.)
- **Terminology rulings, report-wide, every occurrence:**
  1. **"Transaction" must not mean "the signed message"** — in a Bitcoin context it names
     a predefined format; use a different term for the signed message.
  2. **PreSign / PreVerify / Adapt / Ext are `functions`, not `protocols`** — "protocol"
     implies high-level design like a consensus protocol.
- **Sequencing: finish Bitcoin/UTXO before any further EVM/Naysayer work.** The same
  "where do the added components go" question applies to the EVM anyway; the EVM becomes
  a discussion of a more advanced solution *after* a fully complete Bitcoin solution.
- **Report:** word count need **not** be proportional to rubric weighting — write more
  about results, less about background. **Figures embedded between paragraphs, not
  collected at a chapter end or in the appendix** (this overrides the older
  figures-to-appendix habit); group the four LAS function figures side by side; avoid
  single-figure pages. **Chapter 5 is titled "Conclusion, critical reflection and future
  work".**
- **Scope frozen:** no second signature scheme, no zkVM/RISC-V, no functional signatures,
  no live-network deployment — polish what exists.
- **Named for the week after next: the hint experiment.** Wang and Royce framed it in the
  meeting as "use the hint to avoid transmitting full public parameters and shrink
  statement `Y`", the open question being whether `Adapt`/`Ext` survive it. **Royce's
  clarification (2026-08-03) supersedes that framing** — the real objective is to build
  LAS on **NIST ML-DSA as specified** and thereby *demonstrate* that it must be modified
  for LAS to work. See the parameter-authority rules in Scope discipline. Wang's own
  words: *"just try it, just try it."* Also on the list: IPFS-style off-chain storage,
  documented as a fallback with its bridge/maintenance caveats.
- **Owed explanation:** why LAS `Adapt` is ≈270× ECDSA's (≈1.5 µs). Current answer — "the
  pre-signature is huge" — did not satisfy; check ECDSA's adaptor algorithm.
- **Deliverable:** draft slides for a **6–8 minute** presentation, week after next.

## ⚠️ MEETING-7 PIVOT (2026-07-24) — Stage 2 retargets from the EVM to Bitcoin/UTXO

Authority: `las-context-consolidated.md` §16 · transcript `meeting7_cleaned_transcript.md`.
**Read §16 before planning any application work.**

- **Target chain.** Stage 2 moved from a smart-contract chain to **Bitcoin / a UTXO
  chain**. Reason (Wang): native on-chain LAS verification is infeasible against the gas
  limit, and adaptor signatures are used in practice for swaps on UTXO chains. Bitcoin
  has no gas limit — only fees — and the heavy work stays off-chain.
- **The EVM work is NOT retracted.** The measured native verifier and the Naysayer
  variant are retained as *the evidence for why* the UTXO venue was chosen.
- **Deliverable = three configurations** (classical+Groth16, LAS+Groth16, LAS+LaZer),
  benchmarked. ✅ done.
- **Metrics: gas → time + communication cost**, counting off-chain protocol messages,
  plus the usability finding (heavy pre-transaction computation may need a dedicated PC
  rather than a phone).
- **Permitted simplifications:** no real sockets; π stays off-chain; refund/timeout are
  edge cases (honest path first); packing in the swap path optional. Exploration/demo,
  not a product.
- **Report rulings applied:** evaluation is its own chapter; critical reflection is its
  own section in Chapter 5 (achieved / fell short / would do differently); the rejection
  figure is a **cumulative acceptance curve**, not P(exactly k attempts).

## Scope discipline (supervisor, Meeting 2 onward; spec = `las-context-consolidated.md`)
- Target the **Simplified Dilithium-III** set (LAS code is mode-independent; also built
  under modes 2/5 for portability).
- Do NOT implement or analyse security proofs. Implement + benchmark + demo only.
- **Two-stage spine:** Stage 1 = standalone LAS + benchmarks ✅; Stage 2 = blockchain
  application ✅ (now Bitcoin/UTXO — see the Meeting-7 block).
- **Two benchmark baselines required:** (i) LAS vs the base signature/Dilithium ✅;
  (ii) LAS vs a **classical adaptor signature** ✅.
- **⚠️ Parameter authority is NIST FIPS 204 (ML-DSA), NOT the LAS paper** (Royce,
  2026-08-03 — this **overrides** every earlier "migrate to the paper's parameters"
  note, including `las-context-consolidated.md` §4/§5). The build's `q = 8380417 ≈ 2²³`
  is **ML-DSA's modulus and is therefore correct**, not a compromise against the paper.
  **The `q≈2²⁴` migration is DROPPED, not deferred** — NIST does not ask for it, so it
  is not a goal and must not reappear in the work queue or in future-work lists.
  Where the paper and FIPS 204 differ on a *parameter*, follow FIPS 204 and say so.
  (This does **not** touch the notation rule below: report *mathematics* still uses the
  LAS paper's symbols.)
- **⚠️ The "hint optimisation" means building LAS on real NIST ML-DSA** (Royce,
  2026-08-03) — **not** "shrinking statement `Y`", which is how Meeting 8 phrased it.
  The experiment: construct LAS on top of ML-DSA **as NIST specifies it** — hint vector
  `h`, Power2Round, high/low-bit decomposition all **enabled** — instead of on the LAS
  paper's simplified Dilithium. **Its purpose is evidential:** the project currently
  *asserts* that NIST's ML-DSA construction must be modified for LAS to work correctly
  (the clean identity `Az − ct = w + Y` only holds once those features are disabled).
  Running the experiment turns that assertion into a **demonstration** — showing exactly
  what breaks in `Verify`/`Adapt`/`Ext` when the hint machinery is present, and what
  therefore had to change. That is the strongest available defence of the simplified
  scheme: **the highest-novelty item in the queue, and mandatory**, not a size
  micro-optimisation and not optional.
  - **✅ DONE 2026-08-03 — and it CORRECTED the assertion. Do not restate the old
    claim.** `ref/mldsa_las.{c,h}` + `ref/test/test_mldsa_hint.c` build the adaptor on
    ML-DSA with hint/Power2Round/high-low-bit split **all enabled**; evidence
    `evidence/mldsa_hint/latest`, write-up `docs/03-results/MLDSA_HINT_EXPERIMENT.md`.
    Result, reproduced at ML-DSA-44/65/87, 200 iters each: **the signer's side must be
    modified, the verifier's side must NOT.** The naive port (statement added to the
    hash, nothing else changed) fails at PreVerify 0/200; but once the *whole*
    commitment path — committed high bits, low-bits rejection test **and** `MakeHint` —
    moves onto `w+Y` and PreSign tightens to `GAMMA1−BETA−ETA`, all seven adaptor
    properties hold 200/200, **including that the UNMODIFIED FIPS 204 `crypto_sign_verify`
    accepts the adapted signature**. So the correct claim is now: *PreSign and PreVerify
    are necessarily new algorithms; `Verify` is not.* A matched baseline (same loop, no
    statement) reproduces FIPS 204's own repetition rates (4.255/5.105/3.865 vs
    ≈4.25/5.1/3.85), which is the fidelity gate that makes the variant rows
    attributable. Adaptor cost ≈0 in rejection sampling; statement `Y` is **larger than
    the signature** at every set (4416 B vs 3309 B at ML-DSA-65) because Power2Round
    cannot compress `Y`. **Caveat that must travel with the claim:** functional
    demonstration only — the security of committing to `HighBits(w+Y)` is NOT analysed
    (security analysis stays out of scope).
  - **Head-to-head vs the simplified scheme (2026-08-03, same-binary comparison).**
    Adaptor overhead is single-digit percent on BOTH constructions at every set (paired
    per-attempt: PreSign +1.3/+2.2/+2.4% simplified vs +3.8/+2.8/+3.5% ML-DSA), so
    building on real ML-DSA costs no more adaptor overhead. Per-signature cost is nearly
    equal **for opposite reasons**: ML-DSA needs ~2× the restarts (5.22 vs 2.71
    attempts/Sign at level 3) but each attempt is ~half the price (mask bit-unpacked from
    `γ1=2^19` over `L=5` polys vs rejection-sampled over `n+ℓ=11`). **Sizes are the real
    difference and the real limit:** ML-DSA halves the signature (3309 vs 6736) and the
    public key (1952 vs 4416) at no compute cost, **but `Y` is byte-identical (4416 in
    both)** because it is `K` full-width polynomials in either scheme — so the swap
    payload only improves to 0.69×, not 0.49×. **Any future size work must target `Y`,
    not the signature.**
- **Focus: LAS only** — no alternative-PQ-scheme comparison in scope, per Royce.
- **Hard out-of-scope (supervisor):** Ethereum-consensus multisigs, blind/group
  signatures, heavy ZKP/MPC — one related-work paragraph max. **EXCEPTION
  (Royce-directed): the Fig. 1 proof of knowledge π IS implemented** via vendored LaZer;
  do not re-flag it as out-of-scope.
- **ML-DSA alignment — required framing, do not drift:** the target set *aligns the
  reusable ML-DSA primitives and challenge-digest strength with ML-DSA-65, while
  retaining LAS-specific distributions, bounds, exact relations, and unoptimised
  serialization*. Do **not** claim LAS *is* ML-DSA-65 or inherits its security category;
  do **not** claim eprint 2020/845 specifies a FIPS-style 256-bit `c_tilde` (it defines
  `H : {0,1}* → C` only). `fair_paper` is a separate historical reproduction set,
  appendix-only; main benchmarks are the D2/D3/D5-aligned sets with D3 as headline.

## ⚠️ CANONICAL NAMING CONVENTION — Rust ⇄ C ⇄ paper (seven-type layout; DO NOT DEVIATE)

The Rust port (`rust/fips204-las/src/`) is **DONE, KAT-PROVEN, and is the AUTHORITY**.
The C mirror (`ref/`) must reproduce these names **exactly** so the two languages stay
faithful to each other and to the paper↔code notation. Never invent a variant; if a name
here looks wrong, fix it against the paper (`docs/paper/LAS_2020_845_NOTATION.md`), not
from memory.

**Construction parameters** (paper Section 3 / Table 1):

| paper | Rust (setup.rs) | C (setup.h) | value @ target set |
|---|---|---|---|
| n (module rank) | `N` | `LAS_N` (kept; `-DLAS_N=` sweepable) | 6 |
| ℓ | `ELL` | `ELL` (`-DELL=`) | 5 |
| n+ℓ | `N_PLUS_ELL` | `N_PLUS_ELL` | 11 |
| d (ring degree) | `D` | `LAS_D` (`#define LAS_D N`, params.h N=256) | 256 |
| κ | `KAPPA` | `KAPPA` (`-DKAPPA=`) | 49 |
| γ = κ·d·(n+ℓ) | `GAMMA` | `GAMMA` | — |
| seed length | `LAS_SEEDBYTES` | `LAS_SEEDBYTES` | 32 |

`ref/setup.h`'s *defaults* are the historical paper set (4,4,60); the target set is
selected by `-DLAS_N=6 -DELL=5 -DKAPPA=49` from the Makefile. Never read the current
parameter set off those defaults.

C-only divergence: **only** `LAS_N` and `LAS_D` keep the `LAS_` prefix (params.h
already owns bare `N`=256 and `D`=13 for the reused Dilithium primitives). C
ring-degree loops in the **LAS files** read `LAS_D`; the reused Dilithium
primitive files keep bare `N`.

**Challenge-digest width `LAS_CTILDEBYTES`** is keyed on **(LAS_N, ELL, KAPPA) together**
— dimensions alone cannot separate the D2-aligned set from the paper set, which share
(4,4) — with `#error`/`panic!` on any unrecognised set. FIPS 204 §7.3 Alg. 29 takes a
seed in `B^{λ/4}`:

| (n, ℓ, κ) | aligns with | `c_tilde` |
|---|---|---|
| (4, 4, 39) | ML-DSA-44 | 32 |
| (6, 5, 49) | ML-DSA-65 | **48** ← project target |
| (8, 7, 60) | ML-DSA-87 | 64 |
| (4, 4, 60) | none — historical paper reproduction | 32 |

It is **not** keyed on `DILITHIUM_MODE` (the Makefile picks the mode only to satisfy
params.h). `c_tilde` buffers must be sized `LAS_CTILDEBYTES`, never `LAS_SEEDBYTES` —
at 48 that mismatch is a 16-byte overread. PRG-seed sites stay at `LAS_SEEDBYTES`.

**The seven semantic types** (the six object types live in las_types.rs / las_types.h;
`public_params` stays in setup.rs / setup.h; each owned by one layer):

| paper object | Rust type {fields} | C type {fields} | owner |
|---|---|---|---|
| pp = A = [I\|A'] | `PublicParams { a_prime, seed }` | `public_params { a_prime, seed }` | setup |
| pk = t | `PublicKey { t }` | `public_key { t }` | basesig |
| sk = r | `SecretKey { r }` | `secret_key { r }` | basesig |
| σ = (c, z) | `Signature { c_tilde, z }` | `signature { c_tilde, z }` | basesig |
| σ̂ = (c, ẑ) | `PreSignature { c_tilde, z_hat }` | `pre_signature { c_tilde, z_hat }` | las |
| Y = t' | `Statement([R;N])`, `as_t_prime()` | `statement { t_prime }` | relation |
| y / s (witness) | `Witness([R;N_PLUS_ELL])`, `as_relation_vector()`/`from_relation_vector()` | `witness { value }` | relation |

`witness.value` is NEUTRAL storage: Gen's honest ternary r′ AND Ext's extracted
s (relation R′_A, may exceed norm 1). A statement is pk-shaped but is NOT a
public_key; a pre_signature is sig-shaped but NOT a signature — never cast/alias.

**Rejection bounds** (each owned by its scheme, NOT in setup):

| Rust | C | value |
|---|---|---|
| `basesig::BOUND_SIGN` | `BOUND_SIGN` (basesig.h) | γ−κ+1 |
| `las::BOUND_PRESIGN` | `BOUND_PRESIGN` (las.h) | γ−κ |
| — (dropped in Rust) | `BOUND_PRESIGN_K(K)` (las.h; AMHL-only hook) | γ−κ−K+1 |

**Public function names** (paper/upstream ⇄ Rust ⇄ C):

- setup: `setup_public_params` (both).
- relation (Gen): `gen`/`gen_seed` ⇄ `relation_gen`/`relation_gen_seed`.
- relation_zk (π, paper §4.1/Fig. 1): `relation_zk::prove`/`relation_zk::proof_verify`
  (Rust relation_zk.rs) ⇄ `relation_prove`/`relation_proof_verify` (C relation_zk.c).
  Bridge (C-only TU, FFI'd by Rust build.rs): `relation_zk_lin_prove`/
  `relation_zk_lin_verify` (relation_zk_lazer.c). Gate names — NEVER rename:
  `PI_ROWS`/`PI_COLS`/`PI_DEG`/`PI_PROOF_MAX_BYTES` (both languages), params symbol
  `las_pi_params` (generated header relation_zk_params.h ⇐ scripts/las_pi_params.py),
  cargo feature `relation-zk`.
- basesig (Algorithm 1, Σ): `keygen`⇄`base_keygen`, `keygen_seed`⇄`base_keygen_seed`,
  `sign_internal`⇄`base_sign_internal`, `sign`⇄`base_sign`, `sign_det`⇄`base_sign_det`,
  `verify_internal`⇄`base_verify_internal`, `verify`⇄`base_verify`; packed
  `keygen_packed`/`sign_packed`/`verify_packed` ⇄ `base_keygen_packed`/
  `base_sign_packed`/`base_verify_packed`.
- las (Algorithm 2): `presign_internal`⇄`las_presign_internal`, `presign`⇄`las_presign`,
  `presign_det`⇄`las_presign_det`, `preverify_internal`⇄`las_preverify_internal`,
  `preverify`⇄`las_preverify`, `adapt`⇄`las_adapt`, `ext`⇄`las_ext`; packed
  `las_presign_packed`/`las_preverify_packed`/`las_adapt_packed`/`las_ext_packed`.
  (Algorithm 1 lives only in basesig — never in las.c.)
- **Gate names — NEVER rename:** `LAS_ATTEMPTS`/`las_attempts`,
  `BASE_ATTEMPTS`/`base_attempts`, `las_expected_attempts`, `LAS_SEEDBYTES`,
  `LAS_CTILDEBYTES`, serialize bit-widths `LAS_{PK,SK,Z}_COEFF_BITS`
  (`LAS_C_COEFF_BITS` is DELETED — the challenge is no longer bit-packed),
  `LAS_Z_OFFSET`, `LAS_Z_MAX`.

**Serialize sizes** (semantic `*_BYTES`, six typed pack/unpack pairs over 3 encoders):
`PUBLIC_KEY_BYTES`, `SECRET_KEY_BYTES`, `SIGNATURE_BYTES`, `STATEMENT_BYTES`=pk,
`WITNESS_BYTES`=sk, `PRE_SIGNATURE_BYTES`=sig. Pairs: `pack_/unpack_` ×
{`public_key`,`statement`,`secret_key`,`witness`,`signature`,`pre_signature`}. The
current byte values for every parameter set are `_Static_assert`-anchored in
`ref/serialize.h` and mirrored by `expected_wire_sizes()` in Rust — the live block above
prints them; **never retype a size from memory or from an older document.**

**Locked local conventions** (bit-for-bit): vector split `x_0`/`x_1`; NTT-operand
suffixes `_hat`/`_mont`; products `c_r`/`c_t`; mask counter `mask_nonce`; recomputed
challenge digest `c_tilde_check` (verify byte-compares digests); commitments
`w_prime`/`w_plus_t_prime`; object vars `sigma`/`sigma_hat`/`statement`/`witness`;
challenge sampler named after its upstream twin `sample_in_ball` (the `las_` prefix is
reserved for the four Algorithm-2 public ops, not private helpers).

## LAS paper source-of-truth rule

Before editing LAS labels, benchmark plots, report text, **report equations or
symbol/notation tables**, API documentation, README explanations, or comments
explaining the protocol, read **FIRST** (this is the crucial-information authority for
notation — do not decide symbols from memory):

`docs/paper/LAS_2020_845_NOTATION.md`

That file is the repo's curated working guide derived from `2020-845.pdf`. If it
conflicts with `2020-845.pdf`, the PDF wins. If a detail is not present in the
guide, check `2020-845.pdf` before editing. Do not invent paper notation, do not
rename variables casually, and mark uncertain details as TODO rather than guessing.

**Report mathematical notation is governed by §3/§6/§7 of that guide, and the PAPER
(not the code) is the notation authority — never adopt a code symbol for report maths.**
Settled forms that must stay consistent across `report/latex/` (never introduce a variant,
never change one occurrence in isolation — fix every occurrence together):
- ring degree: the paper's **`d`** (this build runs at `d = 256`, the reused Dilithium
  NTT's ring degree). Use `d` in the report; do **not** substitute the code's `N`.
- rejection/masking bound: **`γ = κ·d·(n+ℓ)`** (paper form — `d`, not `N`; `(n+ℓ)` in
  formulas). `M = n+ℓ` may appear only as a table-column shorthand, not in equations.
- challenge weight **`κ` is per parameter set** (60 / 39 / 49 / 60 for paper/D2/D3/D5),
  *not* a fixed constant — never hard-code `κ=60` as global.

If a report symbol looks inconsistent, reconcile it to the paper across *every* occurrence
(body equations + `tab:params` + `tab:notation` + captions + figure labels), not just the
one in front of you. **Never justify report notation by what the C code names a variable.**

**Claim precision (strict-review standard):** self-contained references (never a bare
"Fig. 1" — say which paper's figure); no overclaiming and no misattribution to cited
work; label ceilings as upper bounds; verify that each citation supports the specific
claim it is attached to.

## Guardrails (standing — do NOT do any of these without an explicit instruction)
- **Do not create, delete, or switch git branches** unless explicitly requested.
- **Do not run benchmarks or tests** (no `make`, no executing benches/tests, no
  build-and-run) unless explicitly instructed. Edit code/docs and explain; let
  Royce run the build/bench himself, or wait to be told to.
- **Do not invent or estimate benchmark numbers.** Only ever report figures that
  came from a real, reproduced run; if a number isn't measured, say so — never
  fill a table with plausible-looking values.
- **Do not hand-edit evidence logs** (`evidence/**` and any saved measurement output),
  and do not hand-edit generated report artefacts (`report/latex/generated/*.tex`,
  `figures/*.pdf`). They are captured/derived artefacts: regenerate them by running the
  tool, never by typing numbers.
- **Do not put meeting or transcript narrative into code comments** — keep code comments
  technical.
- **Do not repair the dead legacy C Stage-2/AMHL files** (see Status) — they are
  superseded by design.

## Working agreement — token-saving mode

Use token-saving mode by default.

### Default session startup

Read this `CLAUDE.md` (its live block tells you the current state). Read the **latest
section** of `PROGRESS.md` if the task continues previous repository work. Do **not**
read every project document at the start of every session.

Read these only when needed:

* `las-context-consolidated.md` — supervisor objectives, meeting decisions, scope
  confirmation (the *spec*; meetings 6 and 8 live elsewhere — see the live block).
* `docs/STATUS.md` — the deliverable × built/tested/documented matrix, with the exact
  command that reproduces each claim (the *progress tracker*).
* `docs/LAS.md` — design, benchmark interpretation, report-source text, implementation
  claims.
* `docs/02-methodology/THEORY_IMPL_BRIDGE.md` — paper-equation → function mapping.
* `docs/paper/LAS_2020_845_NOTATION.md` — before touching any notation (see rule above).
* `README.md` — build/reproducibility instructions.
* `docs/02-methodology/FUNCTION_MAP.md` — reused/modified/new classification.
* `CONTEXT.md` — long-form narrative handoff from recent sessions (background only;
  where it disagrees with the live block above, the live block wins).
* `PROGRESS.md` (recent checkpoints) and `PROGRESS copy.md` (the earlier archive).

If the user names specific files, inspect only those first. Read more only if directly
included, referenced, or necessary for accuracy.

### Repository scanning

Do not scan the whole repository unless the task clearly requires it. Prefer targeted
inspection: (1) user-named files, (2) directly included headers/sources, (3) relevant
evidence logs, (4) relevant documentation sections. If a wider scan is needed, explain
why before doing it.

### Documentation sync

Documentation must stay consistent with code, but do not update documentation for every
read-only audit. When implementation changes affect design, API behaviour, benchmark
interpretation, report claims, or theory mapping, update the relevant documentation
section (and this file's hand-written half). When the task is only explanation,
diagnosis, Git help, or read-only review, do not edit documentation unless asked.

### Subagent policy

Do not spawn subagents unless the user explicitly asks. Allowed only for large
independent audits: benchmark evidence vs report claims, blockchain/gas/application
audit, theory-vs-implementation audit, final report/rubric mapping. Never for simple Git
questions, explaining terminal output, small code edits, checking one or two files, or
short documentation patches.

### Checkpoint policy

Checkpoints are short and append-only: current branch, current objective, files
inspected/changed, key decisions, evidence/logs used, unresolved risks, next exact
action. No long reasoning, no full transcripts, no full diffs, no repeated background.

### Context management

Continuing the same work → save a short checkpoint, then `/compact`. Switching topic →
`/clear`. Do not keep an 8+ hour session alive without a clear reason.

### Who runs what

Royce runs all build/benchmark commands; Claude reads the resulting logs/JSON/CSV. Claude
explains each edit before making it, and gives a read-only diagnosis first unless asked
for immediate editing.

## Output discipline

Be direct, evidence-based, and scoped.

For code audits: (1) verdict, (2) evidence by file/function, (3) missing or misleading
parts, (4) exact suggested fix, (5) whether editing is needed. If the inspected files do
not prove something, say so.

For benchmark/report claims: distinguish measured evidence from interpretation; cite the
exact evidence log or source file; never invent missing numbers; flag stale or
contradictory documentation.

For edits: smallest possible change, show the diff, no tests/benchmarks unless requested.

## Reference
- **Objectives (spec):** `las-context-consolidated.md` · **progress tracker:** `docs/STATUS.md`.
- **Meetings:** `meetingN_cleaned_transcript.md` (+ `meeting8_summary.md`); Meeting-6
  directives in `docs/04-evaluation/SUPERVISOR_DELIVERABLES_GAP.md`.
- LAS paper: eprint 2020/845 (Esgin, Ersoy, Erkin) · poqeth: eprint 2025/091 ·
  `NIST.FIPS.204.pdf` (+ `docs/paper/NIST_FIPS_204.md`).
- Design + math + results: `docs/LAS.md` · theory↔code: `docs/02-methodology/THEORY_IMPL_BRIDGE.md`.
- Reproducibility: `README.md` · function classification: `docs/02-methodology/FUNCTION_MAP.md`.
- Plain-English explainer: `docs/01-introduction/LAS_WALKTHROUGH.md` · build order:
  `docs/04-evaluation/PROJECT_HISTORY_EXPLAINED.md` · gas story:
  `docs/03-results/GAS_LIMIT_INVESTIGATION.md`.
- Stage-2 swap: `rust/las-swap/README.md` + `docs/02-methodology/STAGE2_UTXO_SWAP_PLAN.md`.
- **Assessment criteria:** `MSc_Report_and_Video_Rubric.md` · writing guidance:
  `docs/references/Lecture5_ResearchWriting_2026_Lin.md`, `muthesis_formatting_rules.md`.
