@scripts/run_benchmark_suite.sh belum termasuk benchmark dengan @ref/test/bench_classical.c ! Ada beberapa finding menarik yang belum kebaca dari gambar sekarang, karena gambar sekarang terlalu fokus L3 atau terlalu summary. Finding 1 — adaptor overhead kecil dan stabil across levels. Finding 2 — PreSign selalu menjadi cost dominan dalam LAS lifecycle. Finding 3 — expensive computation bukan witness-add, tapi c*r vector + hash + A-product. Finding 4 — c*r one polynomial hampir konstan, tapi all-polys naik karena M = n + ell. Finding 5 — communication cost bukan cuma signature; statement Y juga berat. Finding 6 — off-chain payload ≈ 2.6× one signature. Finding 7 — rejection sampling stabil across parameter sets. Finding 8 — AMHL grows linearly with K. Finding 9 — AMHL norm bound is extremely loose in measured runs. Finding 10 — paper vs L2 punya size sama, tapi timing beda. “Finding 10 paper vs L2 timing beda” jangan langsung diklaim finding.
   Itu harus di-cross-check dari fresh run, karena paper dan L2 sama dimensi tapi beda kappa/gamma dan bisa juga noise. cross-check semua finding dengan actual data, aku bisa salah!

benchmark data yang kurang:

1. c*t all LAS_N public-key polys
   Untuk explain Verify / PreVerify cost.

2. Ext breakdown:
   s = z - z_hat
   A*s
   t' == A*s check
   Untuk explain Extract cost.

3. KeyGen / Gen breakdown:
   sample r
   A*r
   Karena statement Y generation = KeyGen.

4. Protocol component catalogue rows:
   pp=(A,H)
   A seed / expanded A
   y_mask
   w=A*y_mask
   π not implemented/measured
   tx metadata excluded

5. Norm-margin diagnostics:
   max ||z||∞ vs gamma-kappa
   max ||z_hat||∞ vs gamma-kappa-1
   AMHL max ||s_j||∞ vs K / bound

6. Optional serialization timings:
   pack/unpack signature, pre-signature, pk/Y, sk/witness.



Semua tambahan diagnostic akan mengubah stdout/CSV, jadi benchmark harus regenerate oleh user.



rsync -av \
  --exclude='.git' \
  --exclude='.claude' \
  --exclude='PROGRESS.md' \
  --exclude='las-context-consolidated.md' \
  --exclude='*transcript*' \
  --exclude='*.tmp' \
  --exclude='__pycache__' \
  --exclude='evidence/latest' \
  --exclude='defense' \
  --exclude='docs' \
  --exclude='CLAUDE.md' \
  /home/melly/dilithium-msc-project/ \
  /home/melly/dilithium-las-clean/


cd /home/melly
rm -rf /home/melly/dilithium-las-clean
mkdir -p /home/melly/dilithium-las-clean

rsync -av \
  --exclude='.git/' \
  --exclude='.claude/' \
  --exclude='.github/' \
  --exclude='CLAUDE.md' \
  --exclude='PROGRESS.md' \
  --exclude='MEMORY.md' \
  --exclude='las-context-consolidated.md' \
  --exclude='*context*' \
  --exclude='*Context*' \
  --exclude='*checkpoint*' \
  --exclude='*Checkpoint*' \
  --exclude='*transcript*' \
  --exclude='*Transcript*' \
  --exclude='meeting*_cleaned_transcript.md' \
  --exclude='Wang_transcript*' \
  --exclude='Voice_*' \
  --exclude='Cek repo*' \
  --exclude='MSc_Report_and_Video_Rubric*' \
  --exclude='Lecture5-*' \
  --exclude='Session 4*' \
  --exclude='research_writing_guide.md' \
  --exclude='docs/' \
  --exclude='defense/' \
  --exclude='report/' \
  --exclude='evidence/latest' \
  --exclude='evidence/latest/' \
  --exclude='evidence/runs/' \
  --exclude='evidence/*.log' \
  --exclude='figures/' \
  --exclude='tables/' \
  --exclude='scratch/' \
  --exclude='tmp/' \
  --exclude='__pycache__/' \
  --exclude='*.pyc' \
  --exclude='*.tmp' \
  --exclude='*.bak' \
  --exclude='*.aux' \
  --exclude='*.log' \
  --exclude='*.out' \
  --exclude='*.toc' \
  --exclude='*.lot' \
  --exclude='*.lof' \
  --exclude='*.fls' \
  --exclude='*.fdb_latexmk' \
  --exclude='*.synctex.gz' \
  --exclude='third_party/' \
  /home/melly/dilithium-msc-project/ \
  /home/melly/dilithium-las-clean/