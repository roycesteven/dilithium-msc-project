                                            Performance Analysis of Quantum-Secure Digital
                                                  Signature Algorithms in Blockchain
                                                                                                     Tushar Jain
                                                                                            Institute of Computer Science
                                                                                                  University of Tartu
                                                                                                     Tartu, Estonia
                                                                                                   tushar.jain@ut.ee


                                            Abstract—The long-term security of public blockchains strictly    from drop-in replacements for existing signatures in Bitcoin
                                         depends on the hardness assumptions of the underlying digi-          [4] to constructions tailored specificaly for blockchain designs
arXiv:2601.17785v1 [cs.CR] 25 Jan 2026




                                         tal signature schemes. In the current scenario, most deployed        [8].
                                         cryptocurrencies and blockchain platforms rely on elliptic-curve
                                         cryptography, which is vulnerable to quantum attacks due to             Majority of the literature on PQ signatures focuses on
                                         Shor’s algorithm. Therefore, it is important to understand how       cryptographic properties and micro-benchmarks, for example,
                                         post-quantum (PQ) digital signatures behave when integrated          measuring key generation, signing, and verification costs in
                                         into real blockchain systems. This report presents a blockchain      standalone libraries [5], [6]. These results are essential but
                                         prototype that supports multiple quantum-secure signature al-        they do not directly answer how different schemes affect the
                                         gorithms, focusing on CRYSTALS-Dilithium, Falcon and Hawk
                                         as lattice-based schemes. This report also describes the design      behaviour of an actual blockchain implementation. In a real
                                         of the prototype and discusses the performance metrics, which        system, the cost of signature depends on transaction, block
                                         include key generation, signing, verification times, key sizes and   size, and validation latency, and schemes with similar micro-
                                         signature sizes. This report covers the problem, background, and     benchmark performance may have different results when de-
                                         experimental methodology, also providing a detailed comparison       ployed at scale [7], [9].
                                         of quantum-secure signatures in a blockchain context and ex-
                                         tending the analysis to schemes such as HAETAE.                         In this report, we study quantum-secure signatures within
                                            Index Terms—blockchain, post-quantum cryptography, digital        a blockchain environment. We implement a local, single-
                                         signatures, CRYSTALS-Dilithium, Falcon, Hawk                         node blockchain prototype in which the underlying digital
                                                                                                              signature algorithm can be swapped between three lattice-
                                                               I. I NTRODUCTION                               based schemes: Dilithium, Falcon, and Hawk [5], [6], [10].
                                            Blockchains are a distributed list of transactions maintained     The prototype implements a single wallet and transaction
                                         by a network of untrusted nodes [1]–[3]. Between these nodes,        model, ensuring that modifications to the scheme do not
                                         the integrity and authenticity of transactions are maintained        impact the application logic. This provides a fair comparison
                                         through digital signatures. Every transaction is signed by the       of the algorithms from a blockchain perspective, measuring
                                         owner of the private key, and nodes validate signatures before       how they influence transaction signing cost, block verification
                                         accepting new blocks. In deployed systems such as Bitcoin            time, and storage overhead via key and signature sizes.
                                         and Ethereum, these signatures are typically produced with              The contributions of this preliminary report are as follows:
                                         elliptic-curve cryptography (ECC), for example, ECDSA or                • We illustrate a blockchain-like prototype that enables
                                         Schnorr signatures over secp256k1 [3], [4]. The security of                transparent switching between quantum-secure algo-
                                         these schemes is dependent on problems such as the discrete                rithms by separating application logic from the under-
                                         logarithm, which are believed to be safe against classical                 lying digital signature technique.
                                         adversary. But these problems are efficiently solvable on a             • We outline a measuring approach for gathering micro-
                                         large quantum computer using Shor’s algorithm. Consequently,               benchmark data (key generation, signature, verification)
                                         large-scale quantum computers would break the current au-                  and combining it with blockchain-level metrics like val-
                                         thentication mechanisms of blockchains, leading to forgery of              idation latency and block creation.
                                         recorded transactions.                                                  • We place our work in the context of current research on
                                            The goal of post-quantum cryptography (PQC) is to develop               digital signatures and post-quantum blockchains, and we
                                         cryptographic algorithms that are safe from both classical                 identify how our prototype can be extended to additional
                                         and quantum attackers. The U.S. National Institute of Stan-                schemes such as HAETAE [11].
                                         dards and Technology (NIST) chose CRYSTALS-Dilithium                    The full source code of the prototype blockchain and
                                         (now ML-DSA) and Falcon as the main lattice-based digital            benchmarking scripts are available online.1 The outline of
                                         signature schemes after considerable work in recent years            this report is as follows, Section II reviews blockchain fun-
                                         to standardize PQC [5], [6]. At the same time, there is              damentals. Section III introduces the post-quantum signature
                                         growing interest in understanding how these schemes can be
                                         deployed in blockchain protocols [7]–[9]. The ideas range              1 https://github.com/Stanotron/Post-Quantum-Blockchain
algorithms used in our prototype. Section IV describes the           history and exploiting it once quantum machines become
system design and measurement methodology, and Section V             available.
presents preliminary performance results. Finally, we conclude          To address this threat, the cryptographic community has
with a discussion and future work.                                   developed post-quantum or quantum-resistant schemes that are
                                                                     believed to be secure against both classical and quantum adver-
                      II. BACKGROUND                                 saries. The NIST Post-Quantum Cryptography standardization
   This section briefly introduces blockchain fundamentals and       project has selected several algorithms for standardization,
the role of digital signatures, outlines the quantum threat to       including lattice-based key encapsulation mechanisms and
current schemes, and presents an overview of the post-quantum        digital signatures [9]. For signatures, NIST chose CRYSTALS-
signatures considered in our report. We focus on concepts            Dilithium (standardised as ML-DSA) and Falcon as its primary
needed to understand the design of our blockchain prototype          lattice-based candidates [5], [6]. These techniques are based
and the performance trade-offs between different algorithms.         on mathematical problems such as Module-LWE and NTRU,
                                                                     which currently have no known efficient quantum attacks and
A. Blockchain and Digital Signatures                                 have undergone intense cryptanalytic inspection. Blockchains
   A blockchain can be viewed as a replicated, append-only           are a natural target application for PQ signatures, and a number
list maintained by a set of nodes that do not fully trust each       of papers have been written studying how to migrate existing
other [1], [2]. The list is organised into blocks, each containing   systems or design PQ-native protocols [4], [7]–[9].
a batch of transactions. Blocks are chained together using              In this report, we focus on four lattice-based digital sig-
cryptographic hashes, so that modifying a past block would           nature schemes: CRYSTALS-Dilithium (ML-DSA), Falcon,
require recomputing all subsequent hashes, making tampering          Hawk, and HAETAE. All four are designed to be secure
easily detectable. A protocol (such as proof-of-work or proof-       against quantum adversaries while offering acceptable perfor-
of-stake) determines which blocks are accepted into the chain        mance on conventional hardware, but they exhibit different
[1], [3].                                                            trade-offs in terms of key sizes, signature sizes, and compu-
   Digital signatures play a key role in blockchain security.        tational cost.
Each user controls one or more public-private key pairs,                CRYSTALS-Dilithium is a lattice-based signature scheme
the public key (or its hash) acts as an address, and the             built on the Module-LWE and Module-LWR problems [5].
private key is used to sign transactions that transfer value         Falcon is another lattice-based signature scheme, based on the
or currency [3], [4]. Nodes verify signatures before accepting       NTRU problem and using a more complex structure inspired
transactions into their mempool or including them in a block.        by GPV signatures and fast Fourier sampling [6]. Hawk is a
Similarly, some designs sign block headers such that a block         more recent lattice-based signature scheme designed to provide
can only be proposed by a validator or authorized miner. If          short signatures and competitive performance [10]. It targets
an adversary can forge signatures, they can impersonate users,       similar application domains as Dilithium and Falcon, but with
create fraudulent transactions, or rewrite parts of the chain.       different design choices aimed at improving efficiency and
   In classical blockchains such as Bitcoin and Ethereum, sig-       signature size. HAETAE is another lattice-based proposal that
natures are typically instantiated with elliptic-curve schemes       aims to provide shorter Fiat–Shamir signatures with strong
(e.g., ECDSA or Schnorr over secp256k1) [3], [4]. These              security guarantees [11].
schemes are efficient and have small key and signature sizes,           In summary, ML-DSA, Falcon, Hawk, and HAETAE rep-
which is attractive for systems where bandwidth and storage          resent different points in the design space of lattice-based
are constrained. However, their security relies on problems          signatures. The main objective of the experiment outlined in
such as the elliptic-curve discrete logarithm problem, which         the following sections is to understand how these trade-offs
are vulnerable to quantum attacks. This creates a long-term          appear in a blockchain environment.
risk for any blockchain whose security guarantees must remain
valid for decades, particularly given that transaction data is       C. Number Theoretic Transform in Lattice-Based Signatures
publicly stored and may be harvested today and attacked once            Lattice-based schemes such as CRYSTALS-Dilithium, Fal-
quantum computers are available [1], [9].                            con, Hawk, and HAETAE all work with polynomials over
                                                                     finite rings, for example Zq [x]/(xn + 1), where q is a prime
B. Quantum Threat and Post-Quantum Signatures                        modulus and n is the polynomial degree [5], [6], [10],
   Quantum computing poses a threat to widely deployed               [11]. In these schemes, the dominant operation is multiplying
public-key cryptography. Shor’s algorithm shows that integer         polynomials of length n, basically combining the coefficient
factorisation and discrete logarithms can be solved in poly-         vectors in a way so that each output coefficient depends on
nomial time on a large-scale quantum computer, breaking              many input coefficients. A naive multiplication needs O(n2 )
RSA and elliptic-curve schemes that are used in most current         modular multiplications and additions, which quickly becomes
blockchain deployments [8], [9]. Even though practical, large-       a problem for large n.
scale quantum computers do not yet exist, the long-term nature          The Number Theoretic Transform (NTT) is the Discrete
of blockchain data means that adversaries can perform “store         Fourier Transform defined over the ring Zq instead of the
now, decrypt or forge later” attacks, storing today’s transaction    complex numbers. By evaluating a polynomial at special points
called roots of unity modulo q, multiplying the resulting                                   TABLE I
values pointwise, and then applying the inverse transform, one           ML-DSA PARAMETER SETS USED IN OUR EXPERIMENTS [5].
can complete these operations in quasi-linear time O(n log n)         Parameter                 ML-DSA-44     ML-DSA-65     ML-DSA-87
instead of O(n2 ) [12].                                               (k, ℓ)                         (4, 3)        (6, 4)        (8, 5)
                                                                      PK size [bytes]                 1312          1952          2592
      III. P OST-Q UANTUM S IGNATURE A LGORITHMS                      SK size [bytes]                 2560          4032          4896
                                                                      Signature size [bytes]          2420          3309          4627
  In this section we describe the four lattice-based signature
schemes considered in this work: CRYSTALS-Dilithium (stan-
dardised as ML-DSA), Falcon, Hawk, and HAETAE.
                                                                      5) Output the signature σ = (c, z), together with the
A. CRYSTALS-Dilithium / ML-DSA                                           additional hint/compression information required by the
   CRYSTALS-Dilithium is a lattice-based digital signature               concrete ML-DSA encoding [5].
scheme whose security is based on the hardness of finding            Signature verification (high level): To verify σ = (c, z) on
short vectors in module lattices, expressed using the Module-       M using the authentic public key (A, t):
LWE and Module-SIS problems [5]. The scheme was later                 1) Recompute
standardised by NIST under the name ML-DSA, and is
considered as a long-term replacement for current signatures                                   w′ = Az − ct    (mod q).
such as RSA and ECDSA in a post-quantum setting.
   Dilithium operates over the polynomial ring                        2) Accept iff the recomputed challenge matches:
                   Rq = Zq [X]/(X n + 1),                                                          c = H(M ∥ w′ ),
where n = 256 and q = 8380417 is a prime chosen so that fast
                                                                          and the scheme-specific bound checks and hint-assisted
NTT-based polynomial multiplication is possible [5]. Compu-
                                                                          reconstruction succeed [5].
tations are expressed as linear algebra over Rq (matrices and
vectors with polynomial entries), with polynomial products             In our blockchain prototype we use the three ML-DSA
accelerated using the NTT.                                          parameter sets exposed by the liboqs [13] library, cor-
     a) High-level algorithms.: For presentation and later          responding to ML-DSA-44, ML-DSA-65, and ML-DSA-87.
analysis, we summarise the scheme as a compact Key-                 All three share the same ring dimension n = 256 and
Gen/Sign/Verify view. (The real specification additionally          modulus q = 8380417, but differ in matrix dimensions (k, ℓ)
includes compression, “high/low bits” decomposition, hint           and bounds such as η, γ1 , γ2 , β, and ω, which together
values, and rejection sampling; these details do not change         determine security level and efficiency [5]. Higher security
the overall algebraic structure shown below [5].)                   levels use larger matrices, which directly increases the number
   Key generation (high level):                                     of polynomial multiplications and the size of public keys and
   1) Sample a public matrix A ∈R Rqk×ℓ (from a short seed).        signatures.
   2) Sample short secrets s1 ∈R Sηℓ and s2 ∈R Sηk .                   Table I summarises the main parameters we rely on in the
   3) Compute                                                       rest of the report.

                   t = As1 + s2 ∈ Rqk     (mod q).                  B. Falcon
  4) Output public and secret keys:                                    Falcon is a lattice-based digital signature scheme built on
                  pk = (A, t),       sk = (s1 , s2 ) ,              the NTRU problem and the GPV hash-and-sign framework
                                                                    [6]. Conceptually, the public key defines a linear relation over
      where in the instantiated scheme pk and sk also store         a structured lattice, while the secret key provides a trapdoor
      auxiliary hashes and compressed encodings [5].                that enables efficient sampling of a short preimage for hashed
Recovering s1 from (A, t) corresponds to a Module-LWE               message targets. Verification checks both correctness of the
instance [5].                                                       relation and that the signature is short.
   Signature generation (high level): To sign a message M ∈            Falcon works over the NTRU-style ring
{0, 1}∗ :
   1) Sample a random masking vector y ∈R Sγℓ1 .                                          Rq = Zq [X]/(X n + 1),
   2) Compute a commitment w = Ay (in the real scheme,
                                                                    where n is a power of two and q is a small prime modulus
      the verifier-relevant part is derived from the high bits of
                                                                    [6]. The NIST parameter sets use n ∈ {512, 1024} and
      w) [5].
                                                                    modulus q = 12289 [6], enabling fast polynomial arithmetic
   3) Compute the challenge c = H(M ∥ w), where c has
                                                                    using FFT/NTT-style techniques. In practice, Falcon relies
      bounded weight/structure as specified [5].
                                                                    on a specialised discrete Gaussian sampler (via Fast Fourier
   4) Compute the response
                                                                    Orthogonalization) to produce short signatures with the right
                            z = y + cs1 .                           distribution [6].
     a) High-level algorithms.: To align with our                                           TABLE II
implementation-level discussion later, we summarise Falcon                S ELECTED FALCON PARAMETER VALUES ( FROM [6]).
using a compact KeyGen/Sign/Verify view. (The real scheme              Parameter                     Falcon-512      Falcon-1024
includes encoding/compression and restart conditions; the              NIST security level                    I                V
structure below captures the core relations [6].)                      Ring degree n                        512             1024
                                                                       Modulus q                          12289            12289
   Key generation (high level):                                        Std. deviation s (approx.)        165.74           168.39
   1) Sample short polynomials f, g, F, G ∈ Z[X]/(X n + 1)             Max. signature norm bound β      5833.93          8382.44
      such that the NTRU equation holds:                               Salt length k (bits)                 320              320

                          f G − gF = q.
  2) Construct a short basis (trapdoor) for the corresponding    C. HAWK
     NTRU lattice:                                                 Hawk is a lattice-based digital signature scheme whose
                                       
                                g −f                             security is based on problems from the lattice isomorphism
                        B=                .
                                G −F                             problem (LIP) framework [8], [10]. It follows a hash-and-sign
  3) Compute the public key polynomial:                          approach: a message is hashed into a structured target, and
                                                                 the signer uses a trapdoor to produce a short lattice vector
                        h = gf −1 mod q.
                                                                 that satisfies a public relation. The current Hawk specification
  4) Output:                                                     (version 1.1) defines several parameter sets, of which HAWK-
                       pk = h,       sk = B,                     512 and HAWK-1024 are intended to provide NIST level I
     where pk may also include an encoding of h and sk           and level V security, respectively [10].
     includes auxiliary data for sampling [6].                     At a high level, Hawk operates over a polynomial ring of
  Signature generation (high level): To sign a message m:        the form
  1) Hash the message to a target in the public relation:                             Rq = Zq [X]/(X n + 1),

                              H(m).                              where n is a power of two and q is a prime modulus
                                                                 chosen to support efficient polynomial arithmetic [10]. The
  2) Using the trapdoor basis B, sample a lattice vector v ∈
                                                                 parameter sets HAWK-512 and HAWK-1024 use degrees
     Λ(B) that is close to the target representation.
                                                                 n ∈ {512, 1024}; this supports fast structured polynomial
  3) Compute the short preimage (the signature vector):
                                                                 arithmetic (e.g., NTT-style multiplication) in implementations
                       s = c − v = (s1 , s2 ),                   [10].
     where c is the target derived from H(m) in the public-            a) High-level algorithms.: To align with our later engi-
     key relation.                                               neering and benchmarking discussion, we summarise Hawk
  4) Output the signature σ = s, together with the scheme’s      using a compact KeyGen/Sign/Verify view. (The full specifi-
     compressed encoding (Falcon signatures are variable-        cation includes additional details such as symmetry breaking,
     length due to compression and possible restarts) [6].       rejection conditions, and exact encoding/compression; these
  Signature verification (high level): Given public key h,       do not change the overall flow below [10].)
message m, and signature s = (s1 , s2 ):                            Key generation (high level):
  1) Check that s is short (norm bound).                            1) Sample f, g ∈ Rn with i.i.d. coefficients:
  2) Check the public relation (NTRU-lattice equation):                                   f [i], g[i] ← Bin(η).
                     sA = H(m)       (mod q),
                                                                   2) Find (F, G) ∈ Rn2 such that the NTRU relation holds:
      for the public map A derived from h (commonly written
      as A = (1, h)T in compact matrix form).                                           f G − gF = 1       in Rn .
   3) Accept iff both checks hold [6].
                                                                   3) Define the secret trapdoor basis:
   The official Falcon parameter sets, Falcon-512 and Falcon-                                          
1024, target NIST security levels I and V, respectively. Both                                    f F
                                                                                           B=             .
share the same modulus but differ in ring dimension and                                          g G
internal parameters that control Gaussian sampling and the         4) Define the public key matrix (public quadratic form):
maximum allowed signature norm. Table II summarises the
main public parameters taken from the Falcon security analysis                                 Q = B ⋆ B.
[6].
In our prototype and measurements, Falcon’s main practical         5) Hash the public key:
advantage is its compact public keys and signatures, which                                   hpub = H(Q).
reduces transaction and block size. The main trade-off is im-
plementation complexity and noticeably higher key generation       6) Output:
cost compared to ML-DSA in our test environment [6].                                 pk = Q,         sk = (B, hpub ).
                           TABLE III                               Its security is based on module variants of the Learning With
                H AWK PARAMETER SETS ( FROM [10]).                 Errors (MLWE) and Short Integer Solution (MSIS) problems
         Parameter                 HAWK-512     HAWK-1024          in the Quantum Random Oracle Model, and the parameter sets
         Degree n                       512          1024          are chosen using a Core-SVP style security analysis [11].
         PK size [bytes]               1024          2440             HAETAE works over the polynomial ring
         SK size [bytes]                184           360
         Signature size [bytes]         555          1221                              R = Zq [X]/(X n + 1),
                                                                   with degree n = 256 and modulus q = 64513 for all parameter
  Signature generation (high level): To sign a message m           sets [11]. These choices are NTT-friendly, so polynomial
with sk = (B, hpub ):                                              multiplications can be implemented efficiently using NTT and
  1) Compute M ← H(m ∥ hpub ).                                     inverse NTT. The public key contains a structured public
  2) Sample salt ← {0, 1}λ .                                       matrix A, while the secret key contains a short vector s; the
  3) Compute h ← H(M ∥ salt) ∈ Rn2 .                               module dimensions (k, ℓ) increase with the targeted security
  4) Sample a short vector x from a Gaussian-like distribu-        level.
      tion; reject and retry if ∥x∥ is too large.                       a) High-level algorithms.: To match the level of detail
  5) Compute:                                                      used for ML-DSA and Falcon, we summarise HAETAE using
                                                                   a compact KeyGen/Sign/Verify view. (The full specification
                   w ← B −1 x,          s ← 21 (h − w).            includes additional steps such as compression/entropy coding,
  6) Output the signature:                                         auxiliary values, and multiple rejection checks; the structure
                                                                   below captures the main flow [11].)
                             sig = (salt, s1 ),                       Key generation (high level):
                                                                                            k×(ℓ−1)
     where s1 is a compressed encoding derived from s [10].           1) Sample Agen ← Rq           and small secrets sgen , egen .
 Signature verification (high level): Given pk = Q, message           2) Compute
m, and sig = (salt, s1 ):                                                               b ← Agen sgen + egen ∈ Rqk .
 1) Recompute hpub ← H(Q).                                           3) Build the public matrix A from (Agen , b).
 2) Compute M ← H(m ∥ hpub ).                                        4) Form the secret vector
 3) Compute h ← H(M ∥ salt) ∈ Rn2 .
 4) Reconstruct s = (s0 , s1 ) from (Q, h, s1 ) and compute:                               s ← (1, s⊤      ⊤ ⊤
                                                                                                    gen , egen ) .

                                  w ← h − 2s.                        5) Restart if the secret fails the scheme’s bound checks
                                                                        (used to keep later signing efficient) [11].
  5) Accept if w is valid (including symmetry-breaking con-          6) Output:
     sistency) and satisfies the norm bound:                                             sk = s,       pk = A.
                              ∥w∥Q ≤ bound.                          Signature generation (high level): To sign a message M :
   Like the other lattice-based schemes considered in this           1) Sample a masking vector using the hyperball sampler:
work, Hawk relies heavily on structured polynomial arithmetic                               y ← U (Hyperball).
and fast hashing. The reference implementation is written in
constant-time C, uses only integer arithmetic, and supports          2) Compute the challenge
both a generic and an AVX2-optimised version [10]. Table III
                                                                                 c ← H(HighBits(Ay, α), aux(y), M ),
summarises the main public parameter sets relevant for our
experiments. In addition to HAWK-512 and HAWK-1024,                     where c is sparse with fixed weight (controlled by τ )
the specification also defines a lower-security “challenge”             [11].
set HAWK-256 intended mainly for cryptanalysis rather than           3) Choose b ← {0, 1} and compute the response
deployment [10]. In this work we focus on HAWK-512 and
HAWK-1024.                                                                                 z ← y + (−1)b c · s.
In our blockchain prototype, Hawk is used as an additional PQ        4) Compute a helper/hint value h (for high-bits consistency
signature option alongside ML-DSA and Falcon, allowing us               during verification):
to compare the effects of different lattice-based design choices
on signature sizes and per-transaction verification costs.                     h ← HighBits(Az, α) − HighBits(A1 z1 , α).

D. HAETAE                                                            5) Reject and retry if ∥z∥2 violates the bound (and apply
                                                                        one additional light rejection step) [11].
   HAETAE is a lattice-based digital signature scheme that
                                                                     6) Output (packed/encoded in the concrete scheme):
follows the Fiat–Shamir-with-aborts paradigm, like Dilithium,
but is designed to reduce signature and verification key sizes                       σ = (Encode(HighBits(z1 , α)),
for space-constrained applications such as IoT devices [11].                               LowBits(z1 , α), h, c).
                           TABLE IV                                 (standardised as ML-DSA), and the Falcon-512 and
                HAETAE PARAMETER SETS ( FROM [11]).                 Falcon-1024 parameter sets for Falcon, in line with the
 Parameter                HAETAE-120    HAETAE-180    HAETAE-260    NIST post-quantum cryptography process [5], [6], [9]. For
 (k, ℓ)                        (2, 4)        (3, 6)        (4, 7)   Hawk, we used the authors’ reference implementation since
 PK size [bytes]                 992          1472          2080    hawk is not a part of liboqs library [10], [14].
 SK size [bytes]                1408          2112          2752
 Signature size [bytes]         1474          2349          2948       Timing measurements are taken using a high-resolution
                                                                    wall-clock timer returning microsecond timestamps. To reduce
                                                                    noise, we execute each micro-benchmark (e.g., key generation
 Signature verification (high level): Given pk = A, message         or block validation) a fixed number of times and report the
M , and signature σ:                                                average over all iterations. In particular, block validation time
 1) Decode/reconstruct z̃ from σ (using h and the decom-            is obtained by repeatedly calling validateBlock on a fully
      position).                                                    populated block, rather than extrapolating from microbench-
 2) Recompute auxiliary input h′ ← aux(z̃).                         mark results. All results shown in Section V are based on 100
 3) Recompute the challenge                                         key generations, 1000 transaction signatures within a block,
                                                                    and 100 repeated block validations for each algorithm.
               c′ ← H(HighBits(Az̃ − qcj, α), h′ , M ).
   4) Accept iff c′ = c and ∥z̃∥2 satisfies the verification        B. Blockchain Prototype
       bound [11].
                                                                      The prototype follows an account-based model similar to
   HAETAE comes with three main parameter sets, denoted
                                                                    Ethereum [3], but omits consensus and networking to focus
HAETAE-120, HAETAE-180, and HAETAE-260, which
                                                                    on the cryptographic overhead. The core data structures are:
roughly target NIST security levels 2, 3, and 5. All three
share the same ring degree n = 256 and modulus q = 64513,             • Transaction: A transaction contains a sender public key,
but differ in (k, ℓ), the challenge weight τ , and the rejection        a recipient public key, a transfer amount, a monotoni-
bounds. Table IV summarises the main parameters we rely on              cally increasing nonce, and a digital signature over the
in the rest of the report.                                              transaction body (all fields except the signature itself).
In this work, we treat HAETAE as an additional post-quantum           • Block: A block consists of an index, the hash of the
signature candidate for comparison. We use the authors’ ref-            previous block, a timestamp, a vector of transactions,
erence implementation to obtain standalone microbenchmark               and the hash of the block contents. The block hash is
results for key generation, signing, and verification, but we do        computed over the serialized header and all transactions
not integrate HAETAE into our Windows-based blockchain                  (including their signatures).
prototype. The reference code relies on GCC-specific features         • Blockchain: The blockchain maintains a vector of blocks,
such as variable-length arrays and uses OpenSSL in its random           starting with a hard-coded block. The prototype provides
number generator, which does not compile cleanly under our              methods to create a new block from a batch of transac-
MSVC toolchain without modifying the code or adding extra               tions and to validate a block.
dependencies. As a result, HAETAE is evaluated only through           • Wallet: A wallet encapsulates a public–private key pair
standalone microbenchmarks rather than full blockchain in-              for a given signature scheme and exposes a method to
tegration, but these results still allow us to compare its              create and sign transactions to a recipient public key.
performance and size characteristics with the other schemes            For hashing, we use SHA3-256 hash function used only to
in Section IV.                                                      define block identifiers and block linkages. This function is
         IV. S YSTEM D ESIGN AND M ETHODOLOGY                       intended to be cryptographically secure and is sufficient for
                                                                    our purpose of measuring serialization overhead and verifying
   This section describes the software architecture of our pro-
                                                                    that tampering with block contents is detected by recomputing
totype blockchain, how post-quantum signature schemes are
                                                                    the block hash.
integrated, and how we obtain the performance measurements
                                                                       The Blockchain::validateBlock method checks:
reported in Section V. Our goal is to isolate the impact of the
                                                                    (i) that the block index and prev_hash correctly link to
digital signature algorithm on transaction and block processing
                                                                    the latest block in the chain; (ii) that the block hash matches
while keeping the rest of the system as simple and uniform as
                                                                    a recomputation over the serialized contents; and (iii) that
possible.
                                                                    every transaction signature in the block verifies under the
A. Experimental Environment                                         corresponding sender public key. If any check fails, the block
  All experiments were run on an x86_64 desktop machine             is rejected.
running a 64-bit Windows operating system. The prototype               The full source code of the prototype blockchain and the
was compiled in Release mode using the Microsoft Visual C++         benchmarking scripts used in this work are available in an
toolchain, and linked against the liboqs [13] library for post-     open-source repository.2
quantum signatures. We used the ML_DSA_44, ML_DSA_65,
and ML_DSA_87 parameter sets for CRYSTALS-Dilithium                   2 https://github.com/Stanotron/Post-Quantum-Blockchain
C. Cryptographic Integration                                                                  TABLE V
                                                                        S IZE METRICS FOR A 1000- TRANSACTION BLOCK ( BYTES ).
  To abstract over concrete signature schemes, we define a
Crypto interface with the following core operations:                    Scheme           PK    Signature   Avg tx size   Block size
                                                                        ML-DSA-44       1312       2420          5092     5092084
  • generateKeypair(): produce a public/secret key                      ML-DSA-65       1952       3309          7261     7261084
    pair;                                                               ML-DSA-87       2592       4627          9859     9859084
  • sign(msg, sk): sign a message using a secret key;
                                                                        Falcon-512       897        658          2497     2497118
                                                                        Falcon-1024     1793       1275          4905     4904685
  • verify(msg, sig, pk): verify a signature against                    Hawk-512        1024        555          2651     2651084
    a message and public key;                                           Hawk-1024       2440       1221          6149     6149084
  • metadata methods such as name(), family(),
    variant(), and functions returning expected public
    key, secret key, and maximum signature sizes.                   5) Block validation benchmark: We first validate the
                                                                       block once to ensure it is accepted, then deliberately flip
  Concrete implementations of this interface wrap different
                                                                       a bit in the first transaction’s signature to confirm that
backends:
                                                                       validation fails as expected. Finally, we run 100 repeated
  • OqsMldsaCrypto: wraps the liboqs APIs for ML-                      validations of the original block, measuring the total
    DSA (Dilithium) and supports variants 44, 65, and 87               time and deriving both the average time to validate the
    [13].                                                              whole block and the average per-transaction verification
  • OqsFalconCrypto: wraps the liboqs APIs for Falcon,
                                                                       time.
    supporting Falcon-512 and Falcon-1024 [13].
                                                                    From this workflow we obtain the following metrics for
  • HawkCrypto: wraps the reference implementation of
                                                                  each scheme and parameter set:
    Hawk and presents it through the same interface [14].
                                                                    • average key generation time per wallet;
   An AlgoConfig structure encodes the selected algorithm
                                                                    • average transaction signing time in a 1000-transaction
family (ML_DSA, FALCON, or HAWK) and variant (e.g., "44",
                                                                      block;
"512", or "1024"). A factory function createCrypto
                                                                    • serialized public key size, signature size, average trans-
takes this configuration and instantiates the appropriate con-
                                                                      action size, and block size;
crete Crypto implementation. The rest of the system wallets,
                                                                    • average block validation time and average per-transaction
transactions, and blocks only depend on the abstract Crypto
                                                                      verification time.
interface, so swapping the signature scheme does not require
changes to the application logic.                                    These metrics allow us to compare schemes both at the
                                                                  micro level (key generation, signing, verifying) and at the
D. Benchmark Workflow and Metrics                                 blockchain level (impact on block size and validation latency),
                                                                  following the system-oriented perspective advocated in prior
   The main benchmarking program performs the following
                                                                  work on post-quantum blockchains [7]–[9].
steps for each selected algorithm:
  1) Key generation benchmark: Using the configured                                   V. E XPERIMENTAL R ESULTS
     Crypto instance, we call generateKeypair a total
     of 100 times. Each call is timed individually, and we           This section summarizes the measurements obtained from
     compute the average key generation time per wallet in        the benchmark described above. Each result corresponds to a
     microseconds.                                                block containing 1000 transactions from Alice to Bob, using
  2) Wallet setup: We construct a Blockchain object               the same transaction structure and serialization format across
     bound to the same Crypto instance and create two             all algorithms. We report size metrics for keys, signatures,
     wallets, “Alice” and “Bob”. Each wallet generates a new      and blocks, followed by timing metrics for key generation,
     key pair, and we record the size of their public keys.       transaction signing, and transaction verification.
  3) Transaction signing benchmark: We create 1000 trans-
                                                                  A. Size Metrics
     actions from Alice to Bob, with a fixed transfer amount
     and a monotonically increasing nonce. Each transaction          Table V reports the public key size, signature size, average
     is signed by Alice’s wallet, which internally serializes     serialized transaction size, and total serialized block size for a
     the transaction body and invokes Crypto::sign. We            block with 1000 transactions for each scheme and parameter
     measure the average signing time per transaction in this     set. All sizes are given in bytes.
     block context.                                                  Within the ML-DSA family, increasing the security level
  4) Block creation and size measurement: The 1000                from 44 to 87 leads to a monotonic increase in public key size,
     signed transactions are bundled into a single block on       signature size, and transaction size. The average transaction
     top of the genesis block. We compute the block hash,         size grows from approximately 5.1 kB to 9.9 kB, and the
     serialize the full block (including the hash), and record    total block size (with 1000 transactions) grows from about
     the total serialized block size and the average serialized   5.1 MB to 9.9 MB. This behaviour is consistent with the pa-
     size per transaction.                                        rameterisation of Dilithium, where higher security corresponds
                                                                                                       TABLE VI
                                                                             T IMING METRICS ( MICROSECONDS , AVERAGE OVER ALL ITERATIONS ).

                                                                                        Scheme          Keygen     Sign / tx    Verify / tx
                                                                                        ML-DSA-44          78.9       281.3           97.5
                                                                                        ML-DSA-65         138.4       457.8          144.5
                                                                                        ML-DSA-87         200.3       570.9          234.1
                                                                                        Falcon-512       5721.4       206.1           51.6
                                                                                        Falcon-1024     16825.6       401.9          103.5
                                                                                        Hawk-512         1477.8        31.4           47.1
                                                                                        Hawk-1024        9549.2        70.0           98.5




Fig. 1. Average serialized transaction size for each signature scheme and
parameter set (1000-transaction block).



to larger underlying lattice dimensions and hence larger keys
and signatures [5].
   Falcon exhibits substantially smaller signatures than ML-
DSA at comparable security levels: Falcon-512 signatures
are only 658 bytes, compared to 2420 bytes for ML-DSA-
44, and Falcon-1024 signatures are 1275 bytes, compared
to 4627 bytes for ML-DSA-87. As a result, Falcon-based
                                                                            Fig. 2. Average key gen, signing and per-transaction verification times for
transactions and blocks are significantly more compact; for                 each scheme (logarithmic or linear scale as preferred).
example, the Falcon-512 block with 1000 transactions is
roughly 2.5 MB, versus over 5 MB for ML-DSA-44. This
aligns with the design goals of Falcon, which targets compact
                                                                            verification time increases from about 0.10 ms to 0.23 ms.
public keys and signatures using NTRU lattices and carefully
                                                                            Key generation cost remains relatively low (well below 1 ms
optimised sampling [6].
                                                                            for all three variants), which is consistent with their suitability
   Hawk offers even shorter signatures than Falcon at both
                                                                            for frequent wallet creation in blockchain systems [5].
parameter sets, with 555 bytes for Hawk-512 and 1221 bytes
for Hawk-1024. Public keys for Hawk are somewhat larger                        Falcon presents a somewhat different profile. Key generation
than Falcon’s at the same security level, but still smaller than            is significantly more expensive—around 5.7 ms for Falcon-
those of ML-DSA. Consequently, Hawk-based transactions                      512 and 16.8 ms for Falcon-1024—reflecting the complexity
and blocks are also compact: Hawk-512 yields an average                     of its lattice sampling procedures [6]. However, signing and
transaction size of about 2.65 kB and a block size of about                 verification are comparatively fast: Falcon-512 signs a trans-
2.65 MB. These results highlight Hawk as an attractive can-                 action in about 0.21 ms on average and verifies in about
didate from a bandwidth and storage perspective, though it is               0.052 ms, making it faster than all ML-DSA variants in
not yet standardised and still under active evaluation [8], [10].           terms of verification, which is the dominant operation for full
   Figure 1 can be used to visualise the average transaction                validating nodes.
size per scheme as a bar chart, making the trade-offs between                  Hawk shows particularly strong performance for signing:
different parameter sets immediately apparent.                              Hawk-512 signs in roughly 0.03 ms and Hawk-1024 in about
                                                                            0.07 ms per transaction, significantly faster than both ML-
B. Timing Metrics                                                           DSA and Falcon at similar security levels. Verification times
   Table VI presents timing results for each scheme: average                are also competitive: Hawk-512 verifies in about 0.047 ms
key generation time per wallet, average transaction signing                 per transaction, slightly faster than Falcon-512, and Hawk-
time in the block context, and average per-transaction verifi-              1024 verifies in about 0.10 ms, comparable to Falcon-1024.
cation time derived from repeated block validation. All values              Key generation for Hawk lies between ML-DSA and Falcon,
are in microseconds (µs).                                                   with Hawk-512 at roughly 1.5 ms and Hawk-1024 at around
   Within the ML-DSA family, higher security levels again                   9.6 ms.
translate into higher computational cost. Average transaction                  Figure 2 can be populated with a bar chart comparing sign-
signing time increases from approximately 0.28 ms (ML-                      ing and verification times across schemes, clearly illustrating
DSA-44) to 0.57 ms (ML-DSA-87), while per-transaction                       the relative positions of ML-DSA, Falcon, and Hawk.
                           TABLE VII                                        nearly 10 MB for 1000 transactions, whereas Falcon-512
            STANDALONE HAETAE MICROBENCHMARKS                               and Hawk-512 stay around 2.5–2.7 MB. For blockchains
  Scheme         PK [B]    Sig [B]  Keygen [µs] Sign [µs] Verify [µs]       where block size limits are tight or where network band-
  HAETAE-120         992     1474        390.4    1654.7        71.5        width is constrained, schemes with smaller signatures
  HAETAE-180        1472     2349        626.3    2083.5       132.6        and transactions may be advantageous [4], [8].
  HAETAE-260        2080     2948        539.6    2925.0       158.4
                                                                        These preliminary results suggest that, at least on our test
                                                                     platform and for the simple prototype considered here, Falcon
C. Standalone HAETAE Microbenchmarks                                 and Hawk offer attractive trade-offs between performance and
                                                                     data size compared to ML-DSA, especially for verification-
   In addition to the integrated blockchain benchmarks, we also
                                                                     heavy workloads. However, ML-DSA benefits from its simpler
measured the standalone performance of HAETAE using the
                                                                     design and standardisation status, while Hawk remains a non-
authors’ reference implementation haetae2-benchmark,
                                                                     standard scheme whose long-term security and robustness are
haetae3-benchmark, and haetae5-benchmark on
                                                                     still being studied [5], [8], [10].
the same machine and with the same high-resolution timing
                                                                        A more comprehensive evaluation would consider additional
setup as described in Section III. These measurements report
                                                                     metrics such as memory usage, energy consumption, and the
the time for a single key generation, signature, and verifi-
                                                                     impact of cryptographic cost on end-to-end block propagation
cation operation, averaged over many iterations, but do not
                                                                     delays in a multi-node network. In future work, we plan to
include any blockchain-specific overhead such as transaction
                                                                     extend our prototype to incorporate HAETAE [11] and other
serialization or block validation. We therefore treat them as
                                                                     emerging schemes, and to perform experiments in a distributed
pure cryptographic microbenchmarks that complement the
                                                                     setting to better understand the trade-offs involved in migrating
integrated results for ML-DSA, Falcon, and Hawk.
                                                                     real-world blockchains to post-quantum digital signatures.
   Table VII summarises the HAETAE microbenchmark re-
sults together with the public key and signature sizes taken                                VI. C ONCLUSION
from the parameter sets in Table IV. All timings are given in           The results in Section V provide a view of how dif-
microseconds (µs).                                                   ferent post-quantum signature schemes behave in a simple
   Overall, the HAETAE variants achieve key and signature blockchain prototype. In the tests, ML-DSA, Falcon, and
sizes that are noticeably smaller than those of ML-DSA at Hawk all achieve verification times that are compatible with
similar security levels, but somewhat larger than the most block validation at the scale of thousands of transactions per
compact Falcon and Hawk parameter sets. In terms of runtime, block, but they do so with different trade-offs in terms of
key generation stays in the sub-millisecond range, comparable key sizes, signature sizes, and implementation complexity.
to the ML-DSA variants in Table VI, while signing ranges ML-DSA offers the simplest design and is standardised by
from roughly 1.65 ms to 2.93 ms and verification from about NIST, but has comparatively large public keys, signatures, and
0.07 ms to 0.16 ms. These verification costs place HAETAE block sizes. Falcon and Hawk, achieve significantly smaller
between the faster Falcon/Hawk variants and the slower ML- signatures and faster verification, which leads to more compact
DSA-87 configuration on our platform. Because HAETAE is blocks and lower validation latency, but they have more
not yet integrated into our blockchain prototype, these numbers complex constructions.
should be interpreted purely as cryptographic costs; a full             From a blockchain point of view, two aspects appear par-
blockchain-level evaluation with HAETAE is left to future ticularly important. First, verification cost has a direct impact
implementation.                                                      on validation throughput. For a block with 1000 transactions,
                                                                    Falcon-512 and Hawk-512 validate in tens of milliseconds,
D. Blockchain-Level Observations
                                                                    whereas ML-DSA-87 reaches a few hundred milliseconds,
   From a blockchain perspective, two aspects are particularly      suggesting that Falcon- and Hawk-based designs could support
relevant:                                                           higher transaction rates per validating node under similar hard-
   1) Validation throughput: For a block containing 1000            ware assumptions. Second, signature and key sizes translate
      transactions, the measured average block validation           almost linearly into transaction and block sizes: ML-DSA-87
      times range from about 47 ms (Hawk-512) and 52 ms             produces blocks close to 10 MB in our setup, while Falcon-512
      (Falcon-512) up to approximately 234 ms for ML-DSA-           and Hawk-512 stay around 2.5–2.7 MB for the same workload.
      87. These values are measured by validating a 1000-              On the other hand, HAETAE achieves key and signature
      transaction block 100 times and dividing the total time       sizes smaller than those of ML-DSA at comparable security
      by the number of transactions. In a high-throughput           levels, with verification costs that lie between the Falcon/Hawk
      blockchain, lower verification times translate into higher    variants. Because HAETAE is not yet integrated into the
      sustainable transaction rates per validating node [7], [9].   blockchain prototype and is only evaluated via microbench-
   2) Bandwidth and storage: As shown in Table V, ML-               marks, these numbers should be interpreted as indicative of
      DSA at higher security levels produces noticeably larger      its cryptographic cost rather than as full system-level metrics.
      blocks than Falcon or Hawk for the same number of             Overall, the experiments highlight that there is no single
      transactions. For example, ML-DSA-87 yields a block of        scheme that dominates on all areas, instead, practitioners will
need to balance implementation complexity, size overhead, and                     [7] L. Gan and B. Yokubov, “A performance comparison of post-quantum
verification latency when selecting a post-quantum signature                          algorithms in blockchain,” The Journal of The British Blockchain
                                                                                      Association, vol. 6, pp. 1–10, Sep. 2022.
for blockchain use.                                                               [8] M. Buser, R. Dowsley, M. F. Esgin, C. Gritti, S. K. Kermanshahi,
                                                                                      V. Kuchta, J. T. LeGrow, J. K. Liu, R. C.-W. Phan, A. Sakzad,
                     VII. F UTURE W ORK                                               R. Steinfeld, and J. Yu, “A survey on exotic signatures for post-
   This prototype has several limitations that suggest directions                     quantum blockchain: Challenges & research directions,” Cryptology
                                                                                      ePrint Archive, Paper 2022/1151, 2022. [Online]. Available: https:
for future work. First, all experiments are conducted on a                            //eprint.iacr.org/2022/1151
single-node prototype without networking or consensus, so                         [9] Z. Yang, H. Alfauri, B. Farkiani, R. Jain, R. D. Pietro, and A. Erbad,
they do not capture effects such as block propagation delays,                         “A survey and comparison of post-quantum and quantum blockchains,”
                                                                                      IEEE Communications Surveys & Tutorials, vol. 26, no. 2, pp. 967–
peer-to-peer bandwidth constraints, or interactions with block                        1002, 2024.
production protocols. Extending the prototype to a multi-                        [10] Huang, T. Pornin, E. W. Postlethwaite, T. Prest, L. N. Pulles, and W. van
node environment, with realistic network latencies and block                          Woerden, “HAWK version 1.1,” available at https://hawk-sign.info.
                                                                                 [11] J. H. Cheon, H. Choe, J. Devevey, T. Güneysu, D. Hong, M. Krausz,
propagation, would allow us to study how cryptographic costs                          G. Land, M. Möller, D. Stehlé, and M. Yi, “HAETAE: Shorter
influence end-to-end throughput.                                                      lattice-based fiat-shamir signatures,” Cryptology ePrint Archive, Paper
   Second, we currently evaluate only a fixed transaction                             2023/624, 2023. [Online]. Available: https://eprint.iacr.org/2023/624
                                                                                 [12] A. Satriawan, R. Mareta, and H. Lee, “A complete beginner guide to the
template (simple transfers between two accounts) and a single                         number theoretic transform (NTT),” Cryptology ePrint Archive, Paper
block size of 1000 transactions. Future work could explore                            2024/585, 2024. [Online]. Available: https://eprint.iacr.org/2024/585
more diverse workloads, including smart-contract style trans-                    [13] Open Quantum Safe Project, “liboqs: C library for quantum-safe cryp-
                                                                                      tography,” https://github.com/open-quantum-safe/liboqs, 2025.
actions, varying block sizes, and different mixes of transaction                 [14] T. Pornin and L. N. Pulles, “Hawk implementation: C library for the
types.                                                                                hawk lattice-based signature scheme,” https://github.com/hawk-sign/dev,
   Third, our HAETAE evaluation is limited to standalone                              2025, reference and AVX2-optimised C implementations.
microbenchmarks, the reference implementation depends on
GCC-specific features and OpenSSL-based randomness that
do not compile cleanly under our MSVC-based Windows
environment. Porting HAETAE to our toolchain or moving
the blockchain prototype to a Linux-based environment would
make it possible to measure HAETAE at the blockchain level
using the same methodology as for ML-DSA, Falcon, and
Hawk, and to confirm whether its advantages in size and
verification time translate into better block-level performance.
   Beyond raw speed and size, future development could also
include memory usage and energy consumption on different
hardware platforms, which will consider nodes running on em-
bedded or energy constrained devices. Together, these changes
would provide a more complete picture of the costs and trade-
offs involved in migrating current blockchains to quantum-
secure digital signatures.
                     ACKNOWLEDGMENT
  The author would like to thank Associate Prof. Sedat
Akleylek of the Institute of Computer Science, University of
Tartu, for supervision and valuable feedback on this work.
                              R EFERENCES
 [1] M. Nofer, P. Gomber, O. Hinz, and D. Schiereck, “Blockchain,” Business
     & Information Systems Engineering, vol. 59, no. 3, pp. 183–187, 2017.
     [Online]. Available: https://doi.org/10.1007/s12599-017-0467-3
 [2] M. D. Pierro, “What is the blockchain?” Computing in Science &
     Engineering, vol. 19, no. 5, pp. 92–95, 2017.
 [3] V. Buterin et al., “Ethereum white paper,” GitHub repository, vol. 1, no.
     22–23, pp. 5–7, 2013.
 [4] M. León-Chávez, L. Perin, and F. Rodríguez-Henríquez, Post-Quantum
     Digital Signatures for Bitcoin, Jul. 2022, pp. 251–270.
 [5] L. Ducas, E. Kiltz, T. Lepoint, V. Lyubashevsky, P. Schwabe, G. Seiler,
     and D. Stehlé, “CRYSTALS-Dilithium: A lattice-based digital signature
     scheme,” IACR Transactions on Cryptographic Hardware and Embedded
     Systems, vol. 2018, no. 1, pp. 238–268, Feb. 2018. [Online]. Available:
     https://tches.iacr.org/index.php/TCHES/article/view/839
 [6] P.-A. Fouque, P. Gajland, H. de Groote, J. Janneck, and E. Kiltz, “A
     closer look at falcon,” Cryptology ePrint Archive, Paper 2024/1769,
     2024. [Online]. Available: https://eprint.iacr.org/2024/1769
