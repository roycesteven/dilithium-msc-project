\chapter{Results and discussion}
\label{ch:results}

This chapter reports what was measured and why the results should be believed:
correctness first, then computation cost, then communication cost with its component
breakdown, and finally the application results.

\section{Correctness and robustness}
\label{sec:res-correctness}

No performance claim means anything until the implementation is shown \emph{correct},
and an adaptor signature is not correct merely because it signs and verifies --- it must
satisfy a specific contract. \Cref{tab:contract} lists every clause and its outcome; all
pass, with zero compiler warnings under the project's strict flags. Clauses~1--4 are the
adaptor contract itself: a pre-signature is verifiable yet unspendable (the tripwire),
becomes an ordinary signature once the witness is added, and completing it reveals that
witness --- the mechanism that makes an atomic swap atomic. Clauses~5--6 are robustness,
and clause~7 the reproducibility that lets an independent verifier be cross-checked
against fixed vectors.

\begin{table}[t]
  \centering
  \caption[The adaptor-signature correctness contract]{The adaptor-signature correctness contract, each clause checked and passed
  by \texttt{test\_contract} (corroborated at scale by the 1000-iteration functional
  suite on modes 2/3/5, the 256-instance serialization suite, and the pinned
  known-answer test).}
  \label{tab:contract}
  \small
  \begin{tabular}{@{}l L{0.74\linewidth} c@{}}
    \toprule
    \# & Property & Result \\
    \midrule
    1 & PreSign produces a pre-signature that PreVerify accepts & pass \\
    2 & the pre-signature \emph{fails} basic Verify (statement-binding tripwire) & pass \\
    3 & Adapt yields a signature that basic Verify accepts & pass \\
    4 & Extract recovers the witness \emph{exactly} ($y'=y$) & pass \\
    5a & a tampered message fails Verify & pass \\
    5b & every one of the \tamperFlips{} single-byte flips of the packed signature is rejected & pass \\
    6 & malformed packed bytes are rejected ($pk\!\ge\!Q$, ternary code-3, $z$ out of band) & pass \\
    7 & the deterministic API is byte-identical on re-run & pass \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Computation cost}
\label{sec:res-compute}

The primary computation result is the cost of the \emph{adaptor layer} --- what LAS adds
to the base signature it is built on. The two paths share algorithm, parameters and
primitives, so any difference is purely the price of adaptor functionality
(\cref{sec:benchmethod}). \Cref{tab:overhead-l3} reports it at \emph{both} measurement
boundaries and \cref{fig:timing} visualises the core tier. At the core boundary, at the
target setting, every adaptor operation stays within single digits of its counterpart
and Extract --- which has no base analogue --- is the cheapest of all; every operation
completes in well under two milliseconds, signing and pre-signing dominating because of
rejection sampling.

\begin{table}[t]
  \centering
  \caption[Adaptor overhead at the target setting, both timing tiers]{The primary
  computation result: per-operation adaptor overhead at the target setting
  \emph{Simplified Dilithium-III} ($n{=}6$, $\ell{=}5$, $\kappa{=}49$,
  $\gamma{=}137\,984$, $d{=}256$), at \emph{both} measurement boundaries of
  \cref{sec:benchmethod}. Each LAS adaptor operation is paired with the basic
  operation it mirrors; ``Overhead'' is the extra cost as a percentage of that
  basic operation, within the same tier. All timings are measured on the C
  implementation, the implementation of record (\cref{sec:benchmethod}); the
  Rust port's independent measurements are in \cref{tab:rust}. Timings are $\mu$s per operation (mean
  $\pm$ sample standard deviation over \benchReps{} repetitions $\times$
  \benchItersSign{}/\benchItersVerify{} iterations; machine of record in
  \cref{sec:benchmethod}). KeyGen, Sign, and Verify are reused unchanged by LAS;
  Extract has no basic analogue. The packed-tier overheads are larger because
  each adaptor operation additionally decodes the public-key-sized statement
  $Y$ (\stmtBytesTarget~B) --- serialization, not adaptor arithmetic. Absolute
  core-tier timings at every parameter set are in \cref{tab:timing} (appendix).}
  \label{tab:overhead-l3}
  \small
  \input{generated/tab_overhead_target}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_timing}
  \caption[Adaptor overhead per operation: basic signature vs LAS]{The primary computation
  result, shown visually: each LAS adaptor operation (orange) paired beside the basic
  operation it mirrors (blue), at the target setting \emph{Simplified Dilithium-III} (its
  parameters are annotated above the plot), measured on the C implementation
  (the implementation of record, \cref{sec:benchmethod}). The percentage above each orange bar is the
  adaptor overhead over its blue partner; exact means and standard deviations, and the
  packed tier, are in \cref{tab:overhead-l3}. KeyGen, Sign, and Verify are reused unchanged by LAS, so KeyGen is
  shown once (shared) and Sign/Verify are the blue partners of PreSign/PreVerify/Adapt;
  Extract has no basic analogue and is shown LAS-only. The absolute $\mu$s scale also shows
  the shape: every operation is sub-two-milliseconds and the rejection-sampling operations
  (Sign, PreSign) dominate, while the verify-class operations and Extract are far cheaper.
  Error bars are the sample standard deviation over \benchReps{} repetitions of
  \benchItersSign{} (sign-class) / \benchItersVerify{} (verify-class) iterations on the
  machine of record (\cref{sec:benchmethod}); the same comparison across all four parameter
  sets is the scaling check in \cref{fig:overhead} (appendix).}
  \label{fig:timing}
\end{figure}

At the core boundary, then, the adaptor layer is almost free in computation, for
structural reasons: PreSign ($+\ovPreSign\%$ over Sign) runs the same reject loop,
adding only $Y$ to the commitment and a tighter bound; PreVerify ($+\ovPreVerify\%$
over Verify) recomputes the same commitment shape with the extra $+Y$; Adapt
($+\ovAdapt\%$ over Verify) runs a PreVerify then adds the witness vector, costing
about one verification plus a few polynomial additions; and Extract merely subtracts
$z-\hat z$ and checks $A\,y=Y$. None changes the asymptotic work. This is the headline
of the standalone-signature evaluation: \emph{at the core tier}, adaptor functionality
costs only single-digit percentages of extra computation, and the two operations unique
to an adaptor signature cost no more than a verification each. The premium stays in
single digits at all four parameter sets, so it is not an artefact of one choice
(\cref{fig:overhead}, appendix).

That argument concerns the core tier alone. The packed-tier block of
\cref{tab:overhead-l3} shows what changes when the boundary moves to packed bytes, as a
wire or on-chain verifier requires: the premium grows to $+\packedOvPreSign\%$,
$+\packedOvPreVerify\%$ and $+\packedOvAdapt\%$ --- an order of magnitude above the
core-tier figures, and no longer single-digit. This gap is serialization, not adaptor
arithmetic: each adaptor operation must additionally decode the public-key-sized
statement $Y$ (\stmtBytesTarget~B), and Adapt re-packs the completed signature. The
object that dominates the \emph{communication} cost (\cref{sec:res-comm}) therefore
reappears as the dominant \emph{computation} cost once the byte boundary is included,
so a deployment verifying from bytes should budget for the packed row. Presenting both
tiers with each boundary stated is deliberate: reported lattice-signature timings are
notoriously sensitive to where the boundary is drawn \cite{riou2026apples}.

\begin{table}[tbp]
  \centering
  \caption[Rejection-sampling statistics: model vs measurement]{Rejection-sampling
  statistics at the target setting \emph{Simplified Dilithium-III}: the closed-form
  geometric model of \cref{eq:rejacc} against every measured sample. ``Mean'' is
  attempts per call and ``Accept.''\ is the per-attempt acceptance rate --- the two
  quantities every source records, and the ones the model predicts. The per-call
  \emph{dispersion} (the geometric standard deviation $\sigma=E\sqrt{1-1/E}$, the
  medians and 95th percentiles, and the sampled maxima) is annotated on the companion
  acceptance curve of \cref{fig:rejcdf} rather than repeated here. The C driver is the
  implementation of record; the Rust protocol driver and the Criterion harness are
  the independent samples of \cref{sec:rustport}. Every measured row passes the
  five-standard-error validity gate of \cref{sec:benchmethod}.}
  \label{tab:rejstats}
  \small
  \input{generated/tab_rejstats}
\end{table}

\begin{figure}[tbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_rejection_cdf}
  \caption[Cumulative probability of acceptance at the target setting]{The
  probability that a sign-class call has been accepted \emph{within} $k$
  rejection-sampling attempts, at the target setting \emph{Simplified
  Dilithium-III}. The curves are the closed-form geometric model of
  \cref{eq:rejacc} for the basic Sign bound $\gamma-\kappa$ (blue) and the
  tighter LAS PreSign bound $\gamma-\kappa-1$ (orange): acceptance is
  $36.8\%$ and $36.0\%$ respectively on the first attempt, passes $90\%$ by
  five attempts, and then flattens --- the two curves are nearly coincident,
  which is exactly why the PreSign-versus-Sign timing gap is so small. The
  annotated statistics are \emph{measured} on the C implementation (the
  2000-call distribution sample tabulated exactly in \cref{tab:rejstats}). The
  flattening is the practical point: allowing many more attempts buys almost
  nothing, because the overwhelming majority of calls are accepted within the
  first few.}
  \label{fig:rejcdf}
\end{figure}

Signing and pre-signing are dearer than verification because of rejection sampling,
which is intrinsic to the Fiat--Shamir-with-aborts family rather than a defect here.
\Cref{eq:rejacc} predicts 2.719 attempts per Sign and 2.775 per PreSign, the latter
restarting about $2\%$ more often because its bound is tighter by exactly one; measured
directly, by counting attempts rather than estimating from a timing ratio, the run of
record observed \rejAttBase{} and \rejAttLas{} respectively (\cref{tab:rejstats}), and
\cref{fig:rejcdf} shows how quickly acceptance accumulates. At the drivers' 2500 timed
calls the gate band ($\pm\!\sim\!8\%$) is wider than that $2\%$ separation, so the two
counts are statistically indistinguishable at this sample size --- which is why the
core-tier PreSign overhead ($+\ovPreSign\%$) should be read as of the same order as the
attempt-count prediction, not as a precise constant. Only the $\approx\!10^5$-call
Criterion harness, band $\pm\!\sim\!1\%$, resolves it.

\section{Cross-implementation check: C implementation versus Rust port}
\label{sec:res-rust}

The scheme exists twice (\cref{sec:rustport}), and the two implementations agree at
every level at which they can be compared. At the byte level agreement is exact: the
same packed sizes at the target setting (public key \pkBytesTarget{}~B, signature
\sigBytesTarget{}~B, checked programmatically on every Rust run) and the same pinned
known-answer digest (\texttt{\katDigestHead\ldots\katDigestTail}). Since that digest
covers key generation, signing, the four adaptor operations and the serialization, a
divergence anywhere in either implementation's sampling, algebra or encoding would flip
it; its equality is a strong, cheap argument that the two codebases compute the same
scheme.

\begin{table}[t]
  \centering
  \caption[Per-operation timing: C implementation vs Rust port]{Per-operation timing of the
  two implementations at the target setting, \emph{Simplified Dilithium-III}
  ($\mu$s per operation). The C and Rust columns come from the two protocol drivers,
  which deliberately mirror each other (same repetition scheme of \benchReps{}
  repetitions $\times$ \benchItersSign{}/\benchItersVerify{} iterations, fixed
  public-parameter seed, fixed message, same success-path gating), so they are directly
  comparable. The final column is the independent Criterion.rs statistical harness
  (\critSamples{} samples per operation, bootstrap 95\% confidence interval of the
  mean); its hot-loop protocol yields lower absolute times, and it is included to show
  that the \emph{relative} conclusions do not depend on the hand-rolled driver.}
  \label{tab:rust}
  \small
  \input{generated/tab_rust}
\end{table}

Timing agreement is necessarily looser, the builds going through different compilers,
but close: \cref{tab:rust} shows every operation agreeing within about \rustCMaxDev\%
(largest gap \rustCMaxDevOp). More importantly the central conclusion reproduces in
both languages. At the core tier the Rust port measures $+\rustOvPreSign\%$,
$+\rustOvPreVerify\%$ and $\rustOvAdapt\%$ for PreSign, PreVerify and Adapt against
their base operations, all single-digit in magnitude alongside the C values of
$+\ovPreSign\%$, $+\ovPreVerify\%$ and $+\ovAdapt\%$. The Rust Adapt figure is
slightly \emph{negative}: at this scale the genuine overhead is smaller than the cost
gap between the base and LAS verify paths, separate helpers compiling to marginally
different code, so the measurement floor rather than a real speed-up fixes its sign.
The packed tier reproduces too --- $+\rustPackedOvPreSign\%$,
$+\rustPackedOvPreVerify\%$ and $+\rustPackedOvAdapt\%$, all positive and in the C
figures' order --- confirming that once statement decoding is included the adaptor
premium is a cross-language signal, not a compiler artefact. Both ports' counted
rejection rates pass the same gate, and the Criterion harness gives lower absolute
times but the same ordering and shape (\cref{fig:criterion}). The headline results are
therefore properties of the algorithm, not of one codebase, compiler or harness.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_criterion_presign}
  \caption[Criterion.rs sampling distribution for PreSign]{The statistical harness's
  own report for PreSign at the target setting, reproduced unmodified from
  Criterion.rs (its SVG output converted to PDF): \critSamples{} timed samples
  (dots), the kernel-density estimate of the sampling distribution (shaded), the mean
  (vertical blue line), and Criterion's automatic outlier classification (mild /
  severe, orange / red). This is the evidence behind the Criterion column of
  \cref{tab:rust}: a well-formed, single-mode distribution whose spread comes from
  rejection sampling and scheduling noise, with only a handful of classified
  outliers.}
  \label{fig:criterion}
\end{figure}

\section{Communication cost and component breakdown}
\label{sec:res-comm}

The computation results show the adaptor layer is cheap at the core boundary; the size
results show where the real cost lies. \Cref{tab:complete-l3} decomposes every LAS
object into bytes at the target setting, to explain \emph{which} component drives the
size rather than merely state that signatures are large. Sizes are fixed by the
encoding, so they are reported once, as exact bytes with no dispersion.

\begin{table}[t]
  \centering
  \caption[Serialized size of every LAS object]{Serialized size of every LAS object at
  the \emph{Simplified Dilithium-III} (target) setting, in packed wire bytes (not
  on-chain gas), with its paper notation. ``In the basic signature?'' marks which
  objects LAS shares with the base scheme and which it adds.
  Three facts read off directly: the signature, pre-signature, and adapted signature are
  byte-identical (adaptation changes the \emph{value} of the response, not its size); the
  response $z$ dominates every signature; and the only extra \emph{public} object LAS
  adds over a basic signature is the statement $Y$, the size of a public key. The sizes
  hold for the C and the Rust implementation alike: the wire encoding is byte-identical
  in both by construction, asserted programmatically on every Rust run
  (\cref{sec:res-rust}). Sizes at
  every parameter set: \cref{tab:components} (appendix).}
  \label{tab:complete-l3}
  \small
  \input{generated/tab_complete_target}
\end{table}

Two points follow. First, \textbf{the response $z$ is essentially the entire
signature} --- \zPctTwo\% at Simplified Dilithium-II, rising to \zPctFive\% at -V ---
since the challenge travels as the fixed 32-byte hash $\tilde c$
(\cref{sec:serialize}); any optimisation of LAS signatures is therefore an optimisation
of $z$. Second, \textbf{an adaptor swap additionally transmits a public-key-sized
statement $Y$ and a signature-sized pre-signature}, the witness being small; these are
the only objects an adaptor protocol sends that a basic signature does not.

\noindent\textbf{The sharp conclusion: the cost of LAS is not adaptor computation but
communication --- specifically the response vector $z$ and the statement $Y$.} At the
core tier the adaptor operations add at most $+\ovMaxAll\%$ of compute, while the
objects moving on the wire are one to a few kilobytes, dominated by $z$. That matters
more in a blockchain setting than ``the signature is \sigBytesTarget{} bytes'': bytes
reaching a ledger are stored and replicated by every node, while the statement and
pre-signature, exchanged off-chain, still cost both parties bandwidth. The response is
stored at full width --- 18--19 bits per coefficient, neither range-reduced nor
entropy-coded --- the clearest opportunity for reducing that footprint
(\cref{sec:future}).

\subsection{The price of post-quantum: classical adaptor baseline}
\label{sec:res-classical}

The second baseline is a classical secp256k1 ECDSA adaptor signature
\cite{secp256k1zkp}: the natural functional counterpart of LAS, with the same operation
set and driving the same scriptless swaps, but resting on classical discrete-log rather
than lattice hardness. It therefore measures the practical cost of moving from a
classical adaptor signature to a post-quantum one. LAS is instantiated at
\emph{Simplified Dilithium-II} and only there: secp256k1 offers roughly 128-bit
classical security and the nearest post-quantum target is NIST level~2, so those
dimensions are the most like-for-like engineering match, while the -III or -V settings
would pit the classical scheme against a deliberately stronger configuration. Following
the tier rule, LAS appears at both its boundaries while the classical scheme sits at the
\emph{hybrid native-API boundary} (\cref{tab:tiers}), so the comparison is read
conservatively --- against LAS's core tier it slightly flatters LAS, against its packed
tier the classical scheme --- but the conclusions survive either way, resting on
order-of-magnitude gaps rather than percent-level differences.

\begin{table}[t]
  \centering
  \caption[Classical vs.\ post-quantum adaptor signature]{Classical vs.\ post-quantum adaptor signature: same functionality, different
  security foundation. Classical secp256k1 ($\approx$128-bit \emph{classical} security)
  versus LAS at the \emph{Simplified Dilithium-II} parameters ($n=4$, $\ell=4$, $M=8$,
  $\kappa=39$, $\gamma=79\,872$, $d=256$, $q=8\,380\,417$). The Dilithium-II dimensions are
  an engineering match to the $\approx$128-bit (NIST level~2) target, \emph{not} a formal
  security-equivalence claim (\cref{tab:params}); the stronger LAS settings are reserved for
  the parameter-sensitivity study (\cref{sec:res-compute}) and would overstate the
  post-quantum cost here. Timings $\mu$s/op (classical: mean $\pm$ SD over \clReps{} runs
  $\times$ \clIters{} iterations; LAS: \benchReps{} repetitions $\times$
  \benchItersSign{}/\benchItersVerify{} iterations; same machine); sizes in bytes.
  Measurement boundaries (\cref{sec:benchmethod}): the classical column is the library's
  \emph{hybrid} native-API boundary --- PreSign/PreVerify/Adapt/Extract pack or parse the
  162-byte pre-signature \emph{inside} the timed calls (packed-like), while
  KeyGen/Sign/Verify exchange in-memory structs with their wire codecs outside
  (core-like); LAS is
  shown at both its core and packed tiers, measured on the C implementation. KeyGen
  also produces the statement/witness, which take the public-key/secret-key size. The
  classical pre-signature is a syntactically distinct object (an ECDSA signature plus a
  discrete-logarithm-equality proof), whereas the LAS pre-signature shares the signature
  format.}
  \label{tab:classical}
  \small
  \input{generated/tab_classical}
\end{table}

Two findings stand out. First, \textbf{the post-quantum price is paid almost entirely
in size}: LAS's signature and public key are roughly $\clRatioSig\times$ and
$\clRatioPk\times$ larger than the classical adaptor's, whereas in computation every LAS
operation stays well under a millisecond --- even the largest per-operation factor
(\clRatioTimeMaxOp, about $\clRatioTimeMax\times$, against a classical operation that is
nearly free) costs under a tenth of a millisecond absolutely. Second, \textbf{the two
schemes pay their adaptor overhead in opposite ways}: the classical adaptor must attach
a discrete-logarithm-equality proof \cite{fournier2019adaptor}, making its PreSign and
PreVerify several times its own base sign and verify, whereas LAS folds the statement
into a hash it already computes and so stays within a few percent of its base at the
core tier. The lattice adaptor layer is thus relatively cheaper to add, and the
post-quantum cost surfaces in communication --- consistent with the component analysis
above.

\section{Application demonstration}
\label{sec:res-app}

The implementation drives an end-to-end post-quantum atomic swap following Fig.~1 of
\cite{esgin2020post} \emph{verbatim}, including its proof of knowledge $\pi$, which
\cref{app:swapdemo} sets out step by step together with the proof's construction over
the LaZer library \cite{lazer} and its test suite. Two results matter here. First, the
protocol runs and every step is asserted, including the counterfactuals that a
pre-adaptation signature fails basic verification and that $\pi$ is rejected against any
other statement; a modelled ledger additionally carries a timeout/refund path and a
multi-hop payment. Second, $\pi$ is what makes the second party's final step
\emph{guaranteed} rather than merely expected: by the Module-SIS uniqueness argument of
\cite{esgin2020post} the extracted witness equals the proven ternary one, so the adapted
signature is certain to clear the verification bound --- without $\pi$ a malicious
counterparty could claim and leave an unadaptable pre-signature behind. The proof
measures approximately 31 kilobytes at a knowledge error below $2^{-127}$, and is
exchanged off-chain only, exactly as \cite{esgin2020post} note, so it adds nothing to
on-chain cost.

As a supporting check on deployability, native on-chain LAS verification was
\emph{implemented} and measured on a real Solidity stack at the target parameter set: a
complete, numerically-correct byte-level verifier assembled from vendored ZKNox
ETHDILITHIUM primitives and validated end-to-end against the C implementation, which it
matches on a valid signature and on tampered bytes. Wired into the swap, one full
verified settlement measured approximately 56.5 million gas (\cref{tab:onchain};
\cref{fig:onchain}) --- roughly 746$\times$ the equivalent classical ECDSA-adaptor
claim, which verifies in a native precompile whereas LAS runs entirely in EVM bytecode.
This supersedes an earlier op-count \emph{estimate} of approximately 16.7 million gas,
the measured figure being larger because a complete verifier must also decode the
signature, run a real Solidity SHAKE256 and canonically pack its inputs. At that price
it exceeds the EIP-7825 per-transaction cap of $16\,777\,216$ gas; raw demand sits
below the 60-million default block limit, but the per-transaction cap binds. An
\emph{optimistic} (Naysayer) alternative was therefore also implemented and measured,
with a deliberately mixed result: honest settlement becomes cheap (approximately $1.1$
million gas per claim), but the worst-case fraud proof --- a Solidity SHAKE256 hash
dispute at approximately $29.4$ million gas --- still exceeds the cap on execution gas
alone (\cref{app:naysayer}). Both on-chain results are supporting evidence: they
inform, but do not displace, the first-stage signature results above.

\begin{table}[t]
  \centering
  \caption[On-chain settlement gas: classical vs.\ post-quantum]{On-chain settlement gas
  for the atomic swap's claim step, measured on a local EVM (Foundry
  \texttt{forge\ --gas-report}). Gas is deterministic for the fixed bytecode, EVM
  revision, inputs, and contract state, and is independent of the host CPU. The ratios
  compare complete settlement calls, including calldata, memory, state transition, event
  emission, transfer, and verification. Both the classical path and the full LAS path perform
  \emph{complete} signature verification --- the difference is that ECDSA verifies in the
  native \texttt{ecrecover} precompile whereas LAS runs its entire verifier in EVM
  bytecode. \texttt{claimLAS} is a floor that performs \emph{no} lattice verification
  (calldata for the \sigBytesTarget-byte signature plus one \texttt{keccak256}), a strict
  lower bound. \texttt{claimLASVerified} runs the complete native \texttt{base\_verify}
  (\cref{sec:construction}) --- decode, the norm bound, $w'=A\vecz-c\,t$, and the SHAKE256
  challenge check --- assembled from the vendored ZKNox ETHDILITHIUM primitives and
  validated end-to-end against the C reference. At $\approx$56.5\,M gas it exceeds the
  EIP-7825 per-transaction cap ($16\,777\,216 = 2^{24}$) by $\approx$3.4$\times$, so it
  cannot execute as a single mainnet transaction; its raw demand is below the 60-million
  default block gas limit, but the per-transaction cap is the binding ceiling.}
  \label{tab:onchain}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{@{}L{0.33\linewidth}L{0.30\linewidth}rr@{}}
    \toprule
    Claim path & On-chain verification & Gas & $\times$ classical \\
    \midrule
    Classical ECDSA adaptor (\texttt{claimClassical})
      & full, \texttt{ecrecover} precompile   & 75\,751     & 1$\times$ \\
    LAS floor (\texttt{claimLAS})
      & none (calldata + \texttt{keccak256})   & 289\,930    & 3.8$\times$ \\
    LAS full verify (\texttt{claimLASVerified})
      & full \texttt{base\_verify}, in Solidity & 56\,538\,682 & 746$\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_onchain}
  \caption[On-chain settlement gas for the three claim paths]{On-chain settlement gas for
  the three atomic-swap claim paths (\cref{tab:onchain}), measured on a local EVM
  (\texttt{forge --gas-report}; deterministic EVM gas), on a logarithmic axis. The
  classical \texttt{claimClassical} (75\,751 gas) verifies in the native \texttt{ecrecover}
  precompile; \texttt{claimLAS} (289\,930) is a settlement floor doing \emph{no} lattice
  verification; and \texttt{claimLASVerified} (56\,538\,682, $\approx$746$\times$ the
  classical claim) runs the complete native LAS \texttt{base\_verify} in EVM bytecode. The
  dashed line is the EIP-7825 per-transaction gas cap ($16\,777\,216 = 2^{24}$); the full
  verifier's bar crosses it, which is exactly why it cannot execute as a single mainnet
  transaction and why a deployable path needs a precompile, a succinct proof, or an
  optimistic (Naysayer) scheme.}
  \label{fig:onchain}
\end{figure}

\section{The swap on a UTXO chain: three configurations compared}
\label{sec:res-stage2}

The demonstration above establishes that the protocol runs. This section measures what
it \emph{costs} and isolates which component is responsible, over the UTXO ledger and
under the design of \cref{sec:stage2method}: three configurations (\cref{tab:configs}),
of which only the $2\to3$ step is controlled. Timings are the mean $\pm$ sample standard
deviation over \stageTwoRuns{} complete swaps at the \emph{packed} tier, so they are
not comparable with the core-tier figures of \cref{sec:res-compute}. Both LAS
configurations passed the rejection gate, measuring \cfgTwoRejMeasured{} attempts per
\textsc{PreSign} against the closed-form \cfgTwoRejExpected{}.

\begin{table}[t]
  \centering
  \caption[Per-phase execution time of the swap]{Per-phase execution time of one
  complete Fig.~1 swap, mean $\pm$ sample SD over \stageTwoRuns{} swaps
  ($\mu$s; run \stageTwoRunId{}, \stageTwoCpu{}). Dashes mark phases a
  configuration does not have. End-to-end is context, not the headline.}
  \label{tab:stage2-time}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Phase & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    KeyGen $\times2$ + Gen & \cfgOneKeyMatMean{} & \cfgTwoKeyMatMean{} & \cfgThreeKeyMatMean{} \\
    Prove ($\pi$)          & ---                 & \cfgTwoProveMean{}  & \cfgThreeProveMean{} \\
    PreSign ($u_1$)        & \cfgOnePreSignAMean{} & \cfgTwoPreSignAMean{} & \cfgThreePreSignAMean{} \\
    ProofVerify ($\pi$)    & ---                 & \cfgTwoProofVerifyMean{} & \cfgThreeProofVerifyMean{} \\
    PreVerify (abort gate) & \cfgOnePreVerifyMean{} & \cfgTwoPreVerifyMean{} & \cfgThreePreVerifyMean{} \\
    PreSign ($u_2$)        & \cfgOnePreSignBMean{} & \cfgTwoPreSignBMean{} & \cfgThreePreSignBMean{} \\
    Adapt ($u_1$)          & \cfgOneAdaptAMean{} & \cfgTwoAdaptAMean{} & \cfgThreeAdaptAMean{} \\
    Settle chain 2         & \cfgOneSettleBMean{} & \cfgTwoSettleBMean{} & \cfgThreeSettleBMean{} \\
    Extract ($u_2$)        & \cfgOneExtMean{} & \cfgTwoExtMean{} & \cfgThreeExtMean{} \\
    Adapt ($u_2$)          & \cfgOneAdaptBMean{} & \cfgTwoAdaptBMean{} & \cfgThreeAdaptBMean{} \\
    Settle chain 1         & \cfgOneSettleAMean{} & \cfgTwoSettleAMean{} & \cfgThreeSettleAMean{} \\
    \midrule
    end-to-end             & \cfgOneTotalMean{} & \cfgTwoTotalMean{} & \cfgThreeTotalMean{} \\
    \quad of which $\pi$   & ---                & \cfgTwoProofPct{}\% & \cfgThreeProofPct{}\% \\
    \quad excluding $\pi$  & \cfgOneNonProofUs{} & \cfgTwoNonProofUs{} & \cfgThreeNonProofUs{} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[t]
  \centering
  \caption[Communication cost of the swap]{Communication cost of one complete swap,
  in bytes, observed from the transmitted buffers over \stageTwoRuns{} swaps. A
  range indicates a message whose size varied between runs. Off-chain messages are
  counted, not only what reaches a ledger.}
  \label{tab:stage2-comm}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Message & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    statement $Y$        & \cfgOneMsgY{} & \cfgTwoMsgY{} & \cfgThreeMsgY{} \\
    proof $\pi$          & ---           & \cfgTwoMsgPi{} & \cfgThreeMsgPi{} \\
    $\hat{\sigma}_1$     & \cfgOneMsgPreSigA{} & \cfgTwoMsgPreSigA{} & \cfgThreeMsgPreSigA{} \\
    $\mathit{tx}_1$      & \cfgOneMsgTxA{} & \cfgTwoMsgTxA{} & \cfgThreeMsgTxA{} \\
    $\hat{\sigma}_2$     & \cfgOneMsgPreSigB{} & \cfgTwoMsgPreSigB{} & \cfgThreeMsgPreSigB{} \\
    $\mathit{tx}_2$      & \cfgOneMsgTxB{} & \cfgTwoMsgTxB{} & \cfgThreeMsgTxB{} \\
    \midrule
    off-chain subtotal   & \cfgOneBytesOffChain{} & \cfgTwoBytesOffChain{} & \cfgThreeBytesOffChain{} \\
    on-chain subtotal    & \cfgOneBytesOnChain{} & \cfgTwoBytesOnChain{} & \cfgThreeBytesOnChain{} \\
    \midrule
    total per swap       & \cfgOneBytesTotal{} & \cfgTwoBytesTotal{} & \cfgThreeBytesTotal{} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{The proof, not the signature, is the cost of a post-quantum swap.}
The role-A proof consumes \cfgTwoProofPct{}\% of end-to-end time in configuration~2 and
\cfgThreeProofPct{}\% in configuration~3. Strip it out and the entire post-quantum
signature workload --- two key generations, a statement, two pre-signatures, a
pre-verification, two adaptations, an extraction and two settlements --- costs
\cfgTwoNonProofUs{}\,$\mu$s, about \stageTwoCryptoRatio{}$\times$ the classical swap's
\cfgOneNonProofUs{}\,$\mu$s. A factor of five on a millisecond-scale workload is
unremarkable; a proof of hundreds of milliseconds decides whether the protocol feels
instantaneous. Even at the packed tier the signature workload is not the bottleneck ---
the post-quantum penalty is not where intuition puts it.

\paragraph{The controlled comparison inverts the expected trade-off.} Replacing Groth16
with LaZer --- same signature, same $\mathbf{A}$, same inputs --- makes the swap
\emph{faster} by \cmpTimePctAbs{}\% while making it \emph{larger} by
\cmpBytesPctAbs{}\%: the post-quantum system is roughly twice as quick to produce a
proof yet emits one about \stageTwoProofSizeRatio{}$\times$ bigger (\cmpProofFrom{}\,B
against \cmpProofTo{}\,B). Two structural facts drive this. Groth16 is \emph{succinct}
--- three group elements whatever the circuit --- but producing that proof means a
multi-scalar multiplication over the whole constraint system, whereas LaZer is not
succinct, its proof growing with the statement, but proving a lattice relation with
lattice machinery avoids encoding arithmetic over a 23-bit modulus into a 254-bit
pairing-friendly field. Verification runs the other way: Groth16 verifies in
\cfgTwoProofVerifyMean{}\,$\mu$s against LaZer's \cfgThreeProofVerifyMean{},
succinctness paying off exactly where it is designed to.

\paragraph{Configuration~2's trade is worse than it looks.} Pairing security is broken
by Shor, so its proof is precisely the component a post-quantum adversary attacks: a
post-quantum signature does not protect a swap whose fairness argument rests on a
classical proof. Groth16 also requires a \emph{trusted, per-circuit} setup ---
\cfgTwoSetupTime{} here --- whose secret must be destroyed for soundness, whereas
LaZer's parameters are public and transparent. Configuration~3 is thus not merely the
post-quantum option: it is faster end-to-end, needs no trusted party and removes a
quantum vulnerability, paying only in bytes.

\paragraph{Communication is again the real price, with a usability consequence.} A
classical swap moves \cfgOneBytesTotal{}\,B in total; the fully post-quantum swap moves
\cfgThreeBytesTotal{}\,B, about \stageTwoBytesRatioThree{}$\times$ more, and puts
\stageTwoOnChainRatio{}$\times$ more on the ledgers. That on-chain multiple is what
matters economically, since a UTXO chain prices transactions by size --- and it is also
why the venue was changed, the same objects costing prohibitive gas on the EVM
(\cref{tab:onchain}). In practical terms a fully post-quantum swap needs
\cfgThreeTotalMean{}\,$\mu$s of computation and tens of kilobytes of off-chain
messaging per exchange, overwhelmingly the proof, against the classical swap's
\cfgOneTotalMean{}\,$\mu$s: comfortable on a laptop and plausible on a modern phone,
but a large enough gap that a naive port of a mobile wallet's UX assumptions would
mislead. No mobile or low-power device was measured, so that point should not be
over-read (\cref{sec:res-validity}).

\section{Threats to validity}
\label{sec:res-validity}

Several factors qualify these results. All timings are machine-dependent, mitigated by
reporting standard deviations, fixing one machine and compiler, emphasising ratios over
absolutes, and by the cross-implementation check (\cref{sec:res-rust}). The LAS parameter
set's concrete security level is not formally established, security proofs being out of
scope, so every cross-scheme claim is qualified by the parameters of \cref{tab:params};
the modulus is $q\approx 2^{23}$ rather than the $2^{24}$ of \cite{esgin2020post}, which
leaves correctness unaffected ($q>2\gamma$) but changes the security margin. The
extracted witness is exact here, whereas the relaxed setting of \cite{esgin2020post}
allows witness noise that can grow across long payment-channel paths; exact extraction
sidesteps that growth but is a simplification.

Two further factors are specific to \cref{sec:res-stage2}. The UTXO ledger is an
in-process model, not a consensus implementation --- no peer-to-peer layer, mempool
policy, fee market, reorganisations or script interpreter --- so it yields real
transaction objects, real serialised sizes and the real leak channel on which atomicity
depends, but no fees or confirmation latency; on-chain byte counts are the honest proxy
and are reported as such. This is a property of the venue rather than a shortcut:
running configurations~2 and~3 against a real Bitcoin node is impossible without a
consensus change, which is exactly why \cite{esgin2020post} states its application over
a UTXO chain \emph{whose signature algorithm has been replaced}. Finally, the Stage-2
timings come from a single desktop machine (\stageTwoCpu{}), so the usability discussion
above is an extrapolation, not a measurement on constrained hardware.
