\chapter{Results and discussion}
\label{ch:results}

This chapter reports what was measured and why the results should be believed:
correctness, computation cost, communication cost, and the application results.

\section{Correctness and robustness}
\label{sec:res-correctness}

No performance claim means anything until the implementation is shown \emph{correct},
and an adaptor signature is not correct merely because it signs and verifies --- it must
satisfy a specific contract. \Cref{tab:contract} lists every clause; all
pass. Clauses~1--4 are the adaptor contract itself: a pre-signature is
verifiable yet unspendable (the tripwire), becomes an ordinary signature once the witness
is added, and completing it reveals that witness --- the mechanism that makes an atomic
swap atomic. Clauses~5--6 are robustness, clause~7 reproducibility.

\begin{table}[htbp]
  \centering
  \caption[The adaptor-signature correctness contract]{The adaptor-signature
  correctness contract at the target setting, with the test that checks each clause
  % Run metadata is not printed: Stage-1 evidence run of record = \benchRunId
  in the evidence run of record; all pass. Clauses~1--4
  and~5a are asserted on every one of the 1000 randomised iterations of the
  functional suite \texttt{test\_las}; 5b and~6 by \texttt{test\_serde}, which also
  performs 256 randomised serialization round trips; 7 by the known-answer test
  \texttt{test\_kat}, whose pinned cross-language value binds the packed public
  key, secret key, signature, pre-signature and adapted signature over four fixed
  vectors. PreVerify and Extract are asserted in all three tests, but produce no
  object that value covers.}
  \label{tab:contract}
  \small
  \begin{tabular}{@{}l L{0.74\linewidth} c@{}}
    \toprule
    \# & Property & Result \\
    \midrule
    1 & PreSign produces a pre-signature that PreVerify accepts & pass \\
    2 & the pre-signature \emph{fails} basic Verify (statement-binding tripwire) & pass \\
    3 & Adapt yields a signature that basic Verify accepts & pass \\
    4 & Extract recovers the witness \emph{exactly} ($y'=y$) & pass \\
    5a & a tampered message fails Verify & pass \\
    5b & each of the \tamperFlips{} low-bit flips of a packed-signature byte is rejected & pass \\
    6 & malformed bytes rejected on decode ($pk\!\ge\!Q$, ternary code-3), out-of-range objects on encode ($z$, non-ternary secret) & pass \\
    7 & the deterministic API is byte-identical on re-run & pass \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Computation cost}
\label{sec:res-compute}

The primary computation result is the cost of the \emph{adaptor layer}. The two paths
share algorithm, parameters and primitives, so any difference is purely the price of
adaptor functionality (\cref{sec:benchmethod}). \Cref{tab:overhead-l3} reports it at
\emph{both} boundaries and \cref{fig:timing} visualises the core tier, where every
paired adaptor operation at the target setting stays within single digits of its
counterpart, Extract is the cheapest of all,
and every operation completes in well under two milliseconds.

\begin{table}[htbp]
  \centering
  \caption[Adaptor overhead at the target setting, both timing tiers]{The primary
  computation result: per-operation adaptor overhead at the target setting
  \emph{Simplified Dilithium-III} (\cref{tab:params}), at \emph{both} measurement
  boundaries of \cref{sec:benchmethod}. Each LAS adaptor operation is paired with the
  basic operation it mirrors; ``Overhead'' is the extra cost as a percentage of that
  basic operation, within the same tier. Timings are $\mu$s per operation, measured on
  the C implementation, the implementation of record (mean $\pm$ sample standard
  deviation over \benchReps{} repetitions $\times$
  \benchItersSign{}/\benchItersVerify{} iterations; machine of record in
  \cref{sec:benchmethod}); the Rust port's independent measurements are in
  \cref{tab:rust}. KeyGen, Sign, and Verify are reused unchanged by LAS, and
  \textsc{Gen} executes exactly the same computation as \textsc{KeyGen}, so the
  measured \textsc{KeyGen} timing also gives the cost of relation generation. Extract
  has no basic analogue. The packed-tier overheads are larger because the byte API
  does codec work on the adaptor's own objects that the basic operation does not ---
  serialization rather than adaptor arithmetic, itemised per operation in the text;
  the core tier isolates the adaptor computation itself. Absolute core-tier timings at
  every parameter set are in \cref{tab:timing} (appendix).}
  \label{tab:overhead-l3}
  \small
  \input{generated/tab_overhead_target}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_timing}
  \caption[Adaptor overhead per operation: basic signature vs LAS]{The primary computation
  result, shown visually: each LAS adaptor operation (orange) paired beside the basic
  operation it mirrors (blue), at the target setting \emph{Simplified Dilithium-III} (its
  parameters are annotated above the plot), measured on the C implementation
  (the implementation of record, \cref{sec:benchmethod}). The percentage above each orange
  bar is the adaptor overhead over its blue partner. KeyGen, Sign, and Verify are reused
  unchanged by LAS, so KeyGen is shown once (shared) and Sign/Verify are the blue partners
  of PreSign/PreVerify/Adapt; Extract has no basic analogue and is shown LAS-only. Error
  bars are the sample standard deviation. Exact means, the repetition counts and the
  packed tier are in \cref{tab:overhead-l3}; the same comparison across all four parameter
  sets is the parameter-sensitivity check in \cref{fig:overhead}.}
  \label{fig:timing}
\end{figure}

At the core boundary the adaptor layer is almost free, and the reasons are structural.
PreSign ($+\ovPreSign\%$ over Sign) runs the same reject loop, adding only $Y$ to the
commitment and a tighter bound; PreVerify ($+\ovPreVerify\%$) recomputes the same
commitment shape with the extra $+Y$; Adapt ($+\ovAdapt\%$) runs a PreVerify then adds
the witness vector; Extract merely subtracts $s=z-\hat z$ and checks $A\,s=Y$. None changes
the asymptotic work. This is the headline of the standalone-signature evaluation:
\emph{at the core tier} adaptor functionality is single-digit at the target setting and
stays below $+\ovMaxAll\%$ across the parameter sweep. The supporting sensitivity plot is
deferred to Appendix~\ref{app:compute-support}, where \cref{fig:overhead} records the
same comparison across all four parameter sets. Adapt and Extract cost about a
verification or less each (\cref{tab:overhead-l3}).

At the packed boundary the premium grows to $+\packedOvPreSign\%$,
$+\packedOvPreVerify\%$ and $+\packedOvAdapt\%$ --- several times the core-tier
percentages. The increase is codec work on the adaptor's own objects rather than adaptor
arithmetic: PreSign and PreVerify additionally decode the statement, while Adapt also
decodes the witness and re-encodes the signature. A deployment working from bytes should
budget for the packed row --- reported lattice timings are notoriously sensitive to this
boundary \cite{riou2026apples}.

Signing and pre-signing take longer than verification because of rejection sampling,
intrinsic to the Fiat--Shamir-with-aborts family rather than a defect here.
\Cref{eq:rejacc} predicts 2.719 attempts per Sign and 2.775 per PreSign, the latter
restarting about $2\%$ more often because its bound is tighter by one; counted directly
rather than estimated from a timing ratio, the run of record observed \rejAttBase{} and
\rejAttLas{}. The supporting rejection table and CDF are deferred to
Appendix~\ref{app:compute-support}: \cref{tab:rejstats,fig:rejcdf} show that measured
rejection behaviour matches the geometric model and that every measured row passes the
five-standard-error validity gate of \cref{sec:benchmethod}. At 2500 timed calls the gate band
($\pm\!\sim\!8\%$) is wider than that $2\%$ separation, so the core-tier PreSign overhead
($+\ovPreSign\%$) should be read as of the same order as the prediction rather than as a
precise constant; only the $\approx\!10^5$-call Criterion harness resolves it.

\FloatBarrier
\section{Cross-implementation check: C implementation versus Rust port}
\label{sec:res-rust}

The scheme exists twice (\cref{sec:rustport}), and at the byte level the two
implementations agree exactly: the same packed
sizes (public key \pkBytesTarget{}~B, signature \sigBytesTarget{}~B, checked
programmatically on every Rust run) and the same pinned known-answer value.
% Value is not printed: pinned KAT digest = \katDigestHead\ldots\katDigestTail
Since that value binds the packed key
pair, signature, pre-signature and adapted signature, a divergence escapes it only by
leaving every one of those bytes unchanged.

\begin{table}[htbp]
  \centering
  \caption[Per-operation timing: C implementation vs Rust port]{Per-operation timing of the
  two implementations at the target setting, \emph{Simplified Dilithium-III}
  ($\mu$s per operation). The C and Rust columns come from the two protocol drivers,
  which deliberately mirror each other (same repetition scheme of \benchReps{}
  repetitions $\times$ \benchItersSign{}/\benchItersVerify{} iterations, fixed
  public-parameter seed, fixed message, same success-path gating), so they are directly
  comparable. The final column is the independent Criterion.rs statistical harness
  (\critSamples{} samples per operation, bootstrap 95\% confidence interval of the
  mean); its hot-loop protocol yields lower absolute times, and it is included to show
  that the \emph{relative} conclusions do not depend on the hand-rolled driver.}
  \label{tab:rust}
  \small
  \input{generated/tab_rust}
\end{table}

Absolute timings do not agree: every operation is slower in the Rust port, by up to
\rustCMaxDev\% (\rustCMaxDevOp), and no cause for that gap has been measured. What
reproduces is the comparison itself. At the core tier the Rust
port measures $+\rustOvPreSign\%$, $\rustOvPreVerify\%$ and $+\rustOvAdapt\%$ --- all
single-digit, like the C column beside them; the slightly \emph{negative} PreVerify figure is
the measurement floor rather than a real speed-up, the genuine overhead at this scale
being smaller than the gap between the two verify paths. The packed tier reproduces too
($+\rustPackedOvPreSign\%$, $+\rustPackedOvPreVerify\%$, $+\rustPackedOvAdapt\%$), all
positive and in the C figures' order. Both ports pass the same rejection gate
(\cref{fig:criterion}). The relative adaptor overheads, not the absolute timings, are
therefore properties of the algorithm rather than of one codebase, compiler or harness.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_criterion_presign}
  \caption[Criterion.rs sampling distribution for PreSign]{The statistical harness's
  own report for PreSign at the target setting, reproduced from Criterion.rs:
  \critSamples{} timed samples
  (dots), the kernel-density estimate of the sampling distribution (shaded), the mean
  (vertical blue line), and Criterion's automatic outlier classification (mild /
  severe, orange / red). Three presentational changes were made to Criterion's SVG: the
  type enlarged, the legend moved from the right-hand column into the row beneath the
  plot, and the margins re-flowed and cropped. Inside the plot frame the coordinate map
  is the identity, so no plotted value, curve, mean, axis scale or outlier
  classification is altered. This is the evidence behind the Criterion column of
  \cref{tab:rust}: a well-formed, single-mode distribution whose spread comes from
  rejection sampling and scheduling noise, with only a handful of classified
  outliers.}
  \label{fig:criterion}
\end{figure}

\FloatBarrier
\section{Communication cost and component breakdown}
\label{sec:res-comm}

The computation results show the adaptor layer is cheap at the core boundary; the size
results show where the real cost lies. \Cref{tab:complete-l3} decomposes every LAS object
into bytes at the target setting, explaining \emph{which} component drives the size.
Sizes are fixed by the encoding, so they are exact and carry no dispersion.

\begin{table}[htbp]
  \centering
  \caption[Serialized size of every LAS object]{Serialized size of every LAS object at
  the \emph{Simplified Dilithium-III} (target) setting, in packed wire bytes (not
  on-chain gas), with its notation --- that of Esgin et al.\ \cite{esgin2020post}, except that a signature carries the
  \emph{digest} $\tilde c$, not the polynomial $c$ (\cref{tab:notation}).
  ``In the basic signature?'' marks which
  objects LAS shares with the base scheme and which it adds.
  Three facts read off directly: the signature, pre-signature, and adapted signature are
  the same serialized size (adaptation changes the \emph{value} of the response, not its length); the
  response $z$ dominates every signature; and the only \emph{public-key-shaped} object LAS
  adds over a basic signature is the statement $Y$ --- the protocol also transmits a
  signature-sized pre-signature, which the table marks as added. The sizes
  hold for the C and the Rust implementation alike: the wire encoding is byte-identical
  in both by construction, asserted programmatically on every Rust run
  (\cref{sec:res-rust}). Sizes at
  every parameter set: \cref{tab:components} (appendix).}
  \label{tab:complete-l3}
  \small
  \input{generated/tab_complete_target}
\end{table}

Two points follow. First, the response $z$ is essentially the entire signature
--- \zPctTarget\% of it at every parameter set --- since the challenge travels only as its
short digest, so $z$ is the main target for reducing LAS signature size. Second,
the adaptor layer additionally transmits a public-key-sized statement $Y$ and a
signature-sized pre-signature --- the only objects a basic signature does not send; the
swap also carries $\pi$.

The cost of LAS is not adaptor computation but
communication --- specifically $z$ and $Y$. At the core tier, the adaptor operations add
at most $+\ovMaxAll\%$ of compute, while the objects on the wire are kilobytes. That matters more
in a blockchain setting than ``the signature is \sigBytesTarget{} bytes'': bytes reaching
a ledger are stored and replicated by every node, while the statement and pre-signature,
though off-chain, still cost both parties bandwidth. Each response coefficient is
bit-packed at a fixed width, with no entropy coding --- the clearest opportunity for
reducing that footprint (\cref{sec:future}).

\subsection{The price of post-quantum: classical adaptor baseline}
\label{sec:res-classical}

The second baseline is a classical secp256k1 ECDSA adaptor signature
\cite{secp256k1zkp}: the natural functional counterpart of LAS, driving the same
scriptless swaps but resting on discrete-log rather than lattice hardness. It measures
the practical cost of moving from a classical adaptor signature to a post-quantum one.
LAS is instantiated at \emph{Simplified Dilithium-II} and only there: secp256k1 offers
roughly 128-bit classical security, so those dimensions are the most like-for-like
engineering match, while -III or -V would pit the classical scheme against a deliberately
stronger configuration. The boundaries differ between the two libraries, so
\cref{tab:classical} matches them per operation and the comparison is read
conservatively --- the conclusions rest on order-of-magnitude gaps, not percent-level
differences.

\begin{table}[htbp]
  \centering
  \caption[Classical vs.\ post-quantum adaptor signature]{Classical vs.\ post-quantum
  adaptor signature: same functionality, different security foundation. Classical
  secp256k1 ($\approx$128-bit \emph{classical} security) against LAS at the
  \emph{Simplified Dilithium-II} parameters of \cref{tab:params}. Those dimensions are an
  engineering match to the $\approx$128-bit (NIST level~2) target, \emph{not} a formal
  security-equivalence claim; the stronger LAS settings are reserved for the
  parameter-sensitivity study (\cref{sec:res-compute}) and would overstate the
  post-quantum cost here. Timings $\mu$s/op (mean $\pm$ SD, machine of record); sizes in
  bytes. The two libraries draw their measurement boundaries differently, so the final
  column is \emph{tier-matched} --- each LAS row taken at whichever of its two tiers
  corresponds to the classical library's boundary for that operation. Why that matching is
  necessary, the boundaries themselves, the repetition counts, and the object-level
  differences behind the size rows are set out in \cref{app:methoddetail}. The two
  classical adaptor-layer ratios quoted in the text are each taken over \emph{their own}
  base operation --- PreSign over plain Sign ($\clOvPreSignX\times$), PreVerify over
  plain Verify ($\clOvPreVerifyX\times$) --- and are \emph{derived} from this library's
  single mixed native-API tier, not a paired, interleaved measurement of the kind
  \cref{sec:res-compute} reports for LAS. The corresponding LAS figures are the
  byte-tier ones, $+\packedOvPreSign\%$ for PreSign and $+\packedOvPreVerify\%$ for
  PreVerify, that tier being the more conservative of the two reported for LAS.}
  \label{tab:classical}
  \small
  \input{generated/tab_classical}
\end{table}

Two findings stand out. First, the post-quantum price is most pronounced in
size: LAS's signature and public key are roughly $\clRatioSig\times$ and
$\clRatioPk\times$ larger than the classical adaptor's, while every LAS operation stays
well under a millisecond. Even the largest per-operation factor (\clRatioTimeMaxOp,
$\clRatioTimeMax\times$) repays decomposition, because it compares two different
\emph{quantities of work} rather than two speeds for the same work: Algorithm~2 of
\cite{esgin2020post} obliges \textsc{Adapt} to run \textsc{PreVerify} before returning, so
\adaptPreVerifyPct\% of LAS's \lasAdaptPackedUs\,$\mu$s is a full lattice verification and
the rest is largely codec, whereas \texttt{secp256k1\_ecdsa\_adaptor\_decrypt} merely
inverts one scalar and multiplies, leaving verification to a separate exported call
\cite{secp256k1zkp}. Charging the classical side the verification LAS cannot skip puts the
two at \clAdaptMatchedUs\,$\mu$s against \lasAdaptPackedUs\,$\mu$s ---
$\clRatioAdaptMatched\times$, not $\clRatioTimeMax\times$: a property of the two
\emph{interfaces}, not of lattice arithmetic. Second, the two schemes pay their
adaptor overhead in opposite ways. The classical adaptor must attach a
discrete-logarithm-equality proof \cite{fournier2019adaptor}, charging its own PreSign
and PreVerify $\clOvPreSignX\times$ and $\clOvPreVerifyX\times$ their base operations,
whereas LAS folds the statement into a hash it already computes. Relative to each
scheme's own base operations, the lattice adaptor layer adds less computational
overhead.

\section{Testing the simplification: the adaptor with ML-DSA verification}
\label{sec:res-mldsa}

\Cref{sec:impl} follows the LAS paper's simplified Dilithium-style base, which omits
Dilithium's hint vector, Power2Round and high/low-bit decomposition. In this simplified
form, the exact identity $A\vecz-c\,t=w+Y$ is exposed. This section tests whether those
omitted optimisations can coexist with the adaptor layer: the four adaptor operations were
built around NIST ML-DSA \emph{as FIPS~204 specifies it} \cite{1274601}, all three
optimisations enabled. Three findings follow; the second pinpoints the boundary.

The naive port fails outright. Adding the statement to the challenge hash and
changing nothing else --- treating the hint machinery as a black box --- yields
pre-signatures that do not pre-verify at all (\mldsaNaiveP{}/\mldsaIters{}), because the
transmitted hint reconstructs $\mathrm{HighBits}(w)$ while the challenge committed to
$\mathrm{HighBits}(w+Y)$. Some modification is therefore genuinely unavoidable.

But the verifier is not what must change. Once the \emph{whole} commitment path
moves onto $w+Y$ --- the committed high bits, the low-bits rejection test \emph{and}
\textsc{MakeHint} --- and \textsc{PreSign} tightens its bound by the witness norm, the
construction satisfies the full adaptor contract (\mldsaContract{} items, including four
tamper rejections and byte-identical determinism), and, decisively, the unmodified
FIPS~204 verifier accepts the adapted signature (\mldsaRepairedP{}/\mldsaIters{}). So
% "necessarily new" was stronger than the evidence: what the naive port establishes is
% that THAT reuse fails (0/200), not that no reuse-based construction exists.  Same
% correction tab:challenges row 2 already records for the disabled optimisations.
\textsc{PreSign} and \textsc{PreVerify} require adaptor-specific algorithms, while
\textsc{Verify} does not: a post-quantum adaptor swap could settle against a standard
ML-DSA verifier. A matched no-statement baseline reproduces FIPS~204's own published repetition
rates, which is what makes these rows attributable to the adaptor rather than to a porting
error.

The size gain is real but bounded, and it relocates the bottleneck. Measured
against the scheme of record in one process, the adaptor layer stays single-digit on both
constructions ($+\mldsaOvPreSign\%$ against $+\mldsaOvPreSignSimp\%$ for \textsc{PreSign}
per attempt), and per-signature cost is close to equal for opposite reasons: ML-DSA
restarts about twice as often (\mldsaAttempts{} against \mldsaAttemptsSimp{}) but each
attempt is roughly half the price. What ML-DSA buys is \emph{bytes} --- the signature
falls to \mldsaSigBytes{}\,B from \mldsaSigBytesSimp{}\,B --- but the statement $Y$ does
not shrink at all (byte-identical at \mldsaStmtBytes\,B), because Power2Round compresses
a public key while $Y$ enters the verification identity \emph{before} any rounding. The
swap payload therefore improves only to \mldsaPayloadRatio$\times$, and $Y$ becomes larger
than the signature it accompanies. Compressing the signature moves the bottleneck
to the statement rather than removing it, redirecting the size question of
\cref{sec:res-comm} and the future work of \cref{sec:future}.

This is a functional demonstration, not a security argument: whether committing to
$\mathrm{HighBits}(w+Y)$ preserves ML-DSA's unforgeability is not analysed, and is
exactly the kind of claim \cref{sec:eval-limitations} declares out of scope.

\section{UTXO swap execution result}
\label{sec:res-app}

The protocol defined in \cref{fig:swapflow} was driven end-to-end on the model UTXO
ledger of \cref{sec:stage2method}. Two results matter. First, the protocol runs and every
step is asserted, including the counterfactuals that a pre-adaptation signature fails
basic verification and that $\pi$ is rejected against any other statement. Second, $\pi$
is what makes Bob's final step \emph{guaranteed} rather than merely expected: by the
Module-SIS uniqueness argument of \cite{esgin2020post} the extracted witness equals the
proven ternary one, so the adapted signature is certain to clear the verification bound.
Without $\pi$ a malicious counterparty could claim and leave an unadaptable
pre-signature behind. The proof measures \cmpProofTo\,B on the wire at a knowledge error
at most $2^{-\piKnowledgeError}$ and is exchanged off-chain only.

This section therefore reports execution evidence, not the protocol definition itself.
The UTXO cost study follows in \cref{sec:res-stage2,sec:res-txstruct}; the separate
gas-metered venue check is isolated in \cref{sec:res-evm}.

\section{The swap on a UTXO chain: three configurations compared}
\label{sec:res-stage2}

The execution result above establishes that the protocol runs; this section measures what
it \emph{costs}, over the UTXO ledger and under the design of \cref{sec:stage2method}:
three configurations (\cref{tab:configs}), of which only the $2\to3$ step is controlled.
Timings are the mean $\pm$ sample standard deviation over \stageTwoRuns{} complete swaps
at the \emph{packed} tier, so they are not comparable with \cref{sec:res-compute}. Both
LAS configurations passed the rejection gate (\cfgTwoRejMeasured{} attempts per
\textsc{PreSign} against the closed-form \cfgTwoRejExpected{}).


\paragraph{The proof, not the signature, dominates execution time.}
\Cref{tab:stage2-time} gives the per-phase timings behind this comparison. The
role-A proof consumes \cfgTwoProofPct{}\% of end-to-end time in configuration~2 and
\cfgThreeProofPct{}\% in configuration~3. Strip it out and the entire post-quantum
signature workload --- two key generations, a statement, two pre-signatures, a
pre-verification, two adaptations, an extraction and two settlements --- costs
\cfgTwoNonProofUs{}\,$\mu$s, about \stageTwoCryptoRatio{}$\times$ the classical swap's
\cfgOneNonProofUs{}\,$\mu$s. That increase stays millisecond-scale, while a proof of
hundreds of milliseconds decides whether the protocol feels instantaneous. Even at the packed tier the signature workload is not the bottleneck ---
the post-quantum penalty is not where intuition puts it.

\begin{table}[htbp]
  \centering
  \caption[Per-phase execution time of the swap]{Per-phase execution time of one
  complete swap, mean $\pm$ sample SD over \stageTwoRuns{} swaps
  % Run metadata is not printed: Stage-2 evidence run = \stageTwoRunId{}
  ($\mu$s; \stageTwoCpu{}). Dashes mark phases a
  configuration does not have. End-to-end is context, not the headline.}
  \label{tab:stage2-time}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Phase & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    KeyGen $\times2$ + Gen & \cfgOneKeyMatMean{} & \cfgTwoKeyMatMean{} & \cfgThreeKeyMatMean{} \\
    Prove ($\pi$)          & ---                 & \cfgTwoProveMean{}  & \cfgThreeProveMean{} \\
    PreSign ($u_1$)        & \cfgOnePreSignAMean{} & \cfgTwoPreSignAMean{} & \cfgThreePreSignAMean{} \\
    ProofVerify ($\pi$)    & ---                 & \cfgTwoProofVerifyMean{} & \cfgThreeProofVerifyMean{} \\
    PreVerify (abort gate) & \cfgOnePreVerifyMean{} & \cfgTwoPreVerifyMean{} & \cfgThreePreVerifyMean{} \\
    PreSign ($u_2$)        & \cfgOnePreSignBMean{} & \cfgTwoPreSignBMean{} & \cfgThreePreSignBMean{} \\
    Adapt ($u_1$)          & \cfgOneAdaptAMean{} & \cfgTwoAdaptAMean{} & \cfgThreeAdaptAMean{} \\
    Settle chain 2         & \cfgOneSettleBMean{} & \cfgTwoSettleBMean{} & \cfgThreeSettleBMean{} \\
    Extract ($u_2$)        & \cfgOneExtMean{} & \cfgTwoExtMean{} & \cfgThreeExtMean{} \\
    Adapt ($u_2$)          & \cfgOneAdaptBMean{} & \cfgTwoAdaptBMean{} & \cfgThreeAdaptBMean{} \\
    Settle chain 1         & \cfgOneSettleAMean{} & \cfgTwoSettleAMean{} & \cfgThreeSettleAMean{} \\
    \midrule
    end-to-end             & \cfgOneTotalMean{} & \cfgTwoTotalMean{} & \cfgThreeTotalMean{} \\
    \quad of which $\pi$   & ---                & \cfgTwoProofPct{}\% & \cfgThreeProofPct{}\% \\
    \quad excluding $\pi$  & \cfgOneNonProofUs{} & \cfgTwoNonProofUs{} & \cfgThreeNonProofUs{} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{The controlled comparison inverts the expected trade-off.} Replacing Groth16
with LaZer --- same signature, same $\mathbf{A}$, same inputs --- makes the swap
\emph{faster} by \cmpTimePctAbs{}\% while making it \emph{larger} by
\cmpBytesPctAbs{}\%: roughly three times as quick to produce a proof, yet emitting one about
\stageTwoProofSizeRatio{}$\times$ bigger. Groth16 is \emph{succinct} --- three group
elements whatever the circuit --- but producing that proof means a multi-scalar
multiplication over the whole constraint system, whereas LaZer's proof grows with the
statement yet proves a lattice relation with lattice machinery, avoiding the encoding of
a 23-bit modulus into a 254-bit pairing-friendly field. Verification runs the other way
(\cfgTwoProofVerifyMean{}\,$\mu$s against \cfgThreeProofVerifyMean{}), succinctness
paying off exactly where it is designed to.

\paragraph{Configuration~2's trade is worse than it looks.} Shor breaks Groth16's
pairing-group discrete logarithms, so its proof is precisely the component a
post-quantum adversary attacks: a
post-quantum signature does not protect a swap whose fairness rests on a classical proof.
Groth16 also needs a \emph{trusted, per-circuit} setup (\cfgTwoSetupTime{}) whose secret
must be destroyed for soundness, whereas LaZer's parameters are transparent.
Configuration~3 is thus not merely the post-quantum option: it is faster end-to-end,
needs no trusted party and removes a quantum vulnerability, paying in bytes and in slower
proof verification.

\begin{table}[htbp]
  \centering
  \caption[Communication cost of the swap]{Communication cost of one complete swap,
  in bytes, observed from the transmitted buffers over \stageTwoRuns{} swaps.
  Off-chain messages are counted, not only what reaches a ledger. \textbf{A range
  indicates a message whose size varied between runs, and only configuration~3's
  proof does so}: LaZer packs the proof's Gaussian response vectors with a
  variable-length code and then reports the length its encoder actually reached, so
  the packed size depends on the values sampled for that particular proof. Every
  other object here has a size fixed by its encoding, and the two ranged totals
  inherit that single varying term.}
  \label{tab:stage2-comm}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Message & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{Off-chain (exchanged directly between the parties)}} \\
    \quad statement $Y$    & \cfgOneMsgY{} & \cfgTwoMsgY{} & \cfgThreeMsgY{} \\
    \quad proof $\pi$      & ---           & \cfgTwoMsgPi{} & \cfgThreeMsgPi{} \\
    \quad $\hat{\sigma}_1$ & \cfgOneMsgPreSigA{} & \cfgTwoMsgPreSigA{} & \cfgThreeMsgPreSigA{} \\
    \quad $\mathit{tx}_1$  & \cfgOneMsgTxA{} & \cfgTwoMsgTxA{} & \cfgThreeMsgTxA{} \\
    \quad $\hat{\sigma}_2$ & \cfgOneMsgPreSigB{} & \cfgTwoMsgPreSigB{} & \cfgThreeMsgPreSigB{} \\
    \quad $\mathit{tx}_2$  & \cfgOneMsgTxB{} & \cfgTwoMsgTxB{} & \cfgThreeMsgTxB{} \\
    \addlinespace[2pt]
    \quad\textbf{off-chain total} & \textbf{\cfgOneBytesOffChain{}}
                                  & \textbf{\cfgTwoBytesOffChain{}}
                                  & \textbf{\cfgThreeBytesOffChain{}} \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{On-chain (published to the ledgers)}} \\
    \quad $\mathit{tx}_1 + \sigma_1$ (chain 1)
      & \cfgOneMsgChainA{} & \cfgTwoMsgChainA{} & \cfgThreeMsgChainA{} \\
    \quad $\mathit{tx}_2 + \sigma_2$ (chain 2)
      & \cfgOneMsgChainB{} & \cfgTwoMsgChainB{} & \cfgThreeMsgChainB{} \\
    \addlinespace[2pt]
    \quad\textbf{on-chain total} & \textbf{\cfgOneBytesOnChain{}}
                                 & \textbf{\cfgTwoBytesOnChain{}}
                                 & \textbf{\cfgThreeBytesOnChain{}} \\
    \midrule
    \textbf{total per swap} & \textbf{\cfgOneBytesTotal{}}
                            & \textbf{\cfgTwoBytesTotal{}}
                            & \textbf{\cfgThreeBytesTotal{}} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Communication is again the real price, with a usability consequence.}
\Cref{tab:stage2-comm} gives the corresponding byte counts. A
classical swap moves \cfgOneBytesTotal{}\,B; the fully post-quantum swap moves
\cfgThreeBytesTotal{}\,B, about \stageTwoBytesRatioThree{}$\times$ more, and puts
\stageTwoOnChainRatio{}$\times$ more on the ledgers --- the multiple that matters
economically, since a UTXO chain prices by size. A fully post-quantum swap needs
\cfgThreeTotalMean{}\,$\mu$s and tens of kilobytes of off-chain messaging per exchange,
overwhelmingly the proof, against \cfgOneTotalMean{}\,$\mu$s classically: comfortable on
a laptop, but a wide enough gap that a mobile wallet's assumptions would not port
naively. No constrained device was measured, so that point should not be over-read.

\section{Transaction structure and what a UTXO chain would charge}
\label{sec:res-txstruct}

The sizes above are the model ledger's own encoding (\cref{app:utxomodel}). What a
settled swap costs on a Bitcoin client is read from two \emph{mined} transactions: an
ordinary payment, and one leg of the swap of \cref{app:btcnode}. \Cref{fig:txstruct}
compares them structurally and \cref{tab:btctx} field by field. Both were mined on
regtest, the swap leg on the node carrying the experimental rule: the sizes are real, on
regtest rather than mainnet.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
      font=\normalsize,
      fld/.style={draw, text width=60mm, minimum height=9mm, inner xsep=2mm,
                  inner ysep=1.5mm, align=left, anchor=north},
      chg/.style={fld, fill=black!12, very thick},
      hdr/.style={font=\normalsize\bfseries, align=center, text width=64mm},
      note/.style={font=\normalsize, align=center, text width=64mm},
      off/.style={draw, dashed, rounded corners, align=left, inner sep=5pt,
                  font=\normalsize, text width=130mm}]

    %% Stacks start at a fixed depth so a wrapping header cannot shift the rows
    %% out of alignment between the two columns.
    %% Per-field byte counts are deliberately NOT repeated here: tab:btctx now sits
    %% beside this figure and gives every field exactly.  Only the two rows that
    %% DIFFER carry a size, and both are shaded -- an earlier version shaded the
    %% witness alone while the caption claimed "only the two shaded quantities
    %% differ", which the unshaded output-script row contradicted.
    %% ---------------- left: an ordinary payment ----------------
    \node[hdr, anchor=north] (h1) at (0,0) {(a) Standard payment};
    \node[fld] (a1) at (0,-8mm) {\texttt{version}};
    \node[fld] (a2) at (a1.south) {input: outpoint, \texttt{sequence}};
    \node[fld] (a3) at (a2.south) {output: value, \texttt{scriptPubKey}};
    \node[fld] (a4) at (a3.south) {\texttt{locktime}};
    \node[chg] (a5) at (a4.south) {witness: $\sigma$, $\mathit{pk}$\\
                                   \textbf{\btcMeasClStack\,B}};
    \node[note] (a6) at ([yshift=-3mm]a5.south)
      {total \btcMeasClTotal\,B \;$=$\; \btcMeasClVsize\,vB};

    %% ---------------- right: the swap settlement ----------------
    \node[hdr, anchor=north] (h2) at (70mm,0) {(b) Adaptor swap settlement};
    \node[fld] (b1) at (70mm,-8mm) {\texttt{version}};
    \node[fld] (b2) at (b1.south) {input: outpoint, \texttt{sequence}};
    \node[fld] (b3) at (b2.south) {output: value, \texttt{scriptPubKey}};
    \node[fld] (b4) at (b3.south) {\texttt{locktime}};
    \node[chg] (b5) at (b4.south) {witness: chunked $\sigma$ and $\mathit{pk}$,\\
                                   tapscript, control block\\
                                   \textbf{\btcMeasLasStack\,B}};
    \node[note] (b6) at ([yshift=-3mm]b5.south)
      {total \btcMeasLasTotal\,B \;$=$\; \btcMeasLasVsize\,vB};

    %% ---------------- what never appears on chain ----------------
    \node[off, anchor=north] at (35mm,-72mm)
      {\textbf{Never on chain:} statement $Y$ (\cfgThreeMsgY\,B), proof $\pi$, both
       pre-signatures $\hat{\sigma}_1,\hat{\sigma}_2$ --- exchanged off-chain only.
       \textbf{Never transmitted at all:} the witness $y$, which $u_2$ recovers as $y'$
       from the published $\sigma$ and its own $\hat{\sigma}_2$ via \textsc{Ext}.};
  \end{tikzpicture}
  \caption[Standard payment versus adaptor swap settlement]{Two \emph{mined}
  transactions: an ordinary payment, and one leg of the swap settled in
  \cref{app:btcnode}. \textbf{No field is added, removed or reordered}, and the base is
  byte-identical at \btcMeasBase\,B --- the swap condition needs no on-chain script of
  its own, which is what \emph{scriptless} means. Exactly one row differs, and the
  difference does not come from the adaptor layer: the witness carries lattice objects
  rather than an elliptic-curve signature and key, and because each stack element is
  capped at \btcChunkWidth\,B they arrive chunked, alongside the tapscript and its
  control block. Both transactions pay to the same output form, so the output does not
  grow; a settlement paying to taproot instead would add 12\,B of base, which is not what
  was mined here. Sizes are the client's own, and the field layout follows
  \cite{bip141,bip144,bip341}; \cref{tab:btctx} gives every field.}
  \label{fig:txstruct}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption[The settled transaction, field by field]{Two mined transactions field by
  field, every figure as the client decoded it: an ordinary payment against one leg of the
  swap of \cref{app:btcnode}. Total size, weight and virtual size are the client's own;
  the base is reconstructed from the fields and \emph{cross-checked} against that weight,
  since BIP-141 fixes weight as three times base plus total \cite{bip141}. ``Witness
  stack'' is the serialized items --- for the swap leg the chunked signature and key, the
  tapscript and the control block --- and ``witness size'' adds BIP-144's marker and flag
  \cite{bip144}. Virtual size is the quantity a UTXO chain prices, and the gap between it
  and total size is the witness discount. \textbf{Scope}: both were mined on regtest, the
  swap leg on the node of \cref{app:btcnode} carrying the experimental rule.}
  \label{tab:btctx}
  \small
  \input{generated/tab_btctx_measured}
\end{table}

\paragraph{The witness discount absorbs more than half of the post-quantum penalty.}
This rests on a transaction structure Bitcoin did not originally have. Before SegWit
\cite{bip141,bip144} a signature travelled in the input script, where every byte was billed
alike; the segregated witness bills witness bytes at one weight unit against four, and
Taproot \cite{bip341} supplies the script-path spend the swap input uses. Derived from the two mined transactions, the post-quantum settlement is
\btcMeasRawRatio$\times$ the classical one in raw bytes; in virtual size --- what fees are
charged on --- only \btcMeasVsizeRatio$\times$, a post-quantum signature being
\emph{entirely} witness data. That decision, taken long before anyone considered lattice
signatures on Bitcoin, removes \btcMeasDiscountSaving\,\% of the size penalty; on the
original structure the same settlement would pay the undiscounted rate throughout. It is a concrete argument for
% Keep the venue argument on transaction weight; gas is handled in sec:res-evm.
UTXO chains as the venue, with no counterpart on the EVM, where calldata carries no
comparable discount; \cref{sec:res-evm} measures what a gas-metered venue charges.

\paragraph{It fits the weight limits, not the element-size or verification rules.}
The mined settlement weighs \btcMeasLasWeight{}\,WU against the
\btcMaxStdWeight{}\,WU standardness limit --- \btcMeasPctStd\,\% of it. The limit is a
weight limit, not a gas limit, and one settlement transaction sits well inside it.

Two obstacles survive, neither caused by the adaptor layer. No deployed consensus rule
verifies a lattice signature: Script offers \texttt{OP\_CHECKSIG} over ECDSA or BIP-340
Schnorr and nothing else, so configurations 2 and 3 could not settle on Bitcoin as it
stands; the application setting in \cite{esgin2020post} assumes a UTXO chain ``where the signature
algorithm is replaced with a lattice-based signature scheme'', and this study takes no
position on which consensus route would deliver it. Separately, the objects collide with
size limits: the signature is \btcSigOverElement$\times$ over the 520\,B
consensus stack-item limit of \cite{bip342} and \btcSigOverStdElement$\times$ over the
80\,B standardness limit \cite{bitcoincorepolicy}.
% Without this sentence the section reads as a contradiction: the signature is 12.9x over a
% CONSENSUS limit, yet the next paragraph has a stock node carrying it.  Reading the pinned
% v31.1 ExecuteWitnessScript settled that the 520 B limit does bind the INITIAL witness
% stack, so chunking is required rather than prudent -- and the patched opcode reassembles
% a fixed number of length-checked chunks, which is why no limit has to be raised.
Both travel as length-checked 520\,B chunks the new rule reassembles, satisfying the
consensus limit rather than raising it. Both apply equally to an ordinary post-quantum
payment: they are properties of lattice signature sizes.

\paragraph{Both obstacles were put to a real client.} A stock Bitcoin Core node carries the
lattice objects --- refused by default relay policy, yet consensus-valid and mined --- but
verifies nothing: an ordinary Schnorr signature authorises it. Adding one consensus
rule, an \texttt{OP\_SUCCESS} opcode \cite{bip342} over the byte interface of
\cref{sec:serialize}, gives a node settling a two-leg swap with no elliptic-curve
signature, \btcSwapControls{} mutations tried being refused by it and accepted by an
unmodified node --- the control attributing the refusals to the new rule. The blocker is a consensus rule, not
engineering. Its cost was measured: one verification runs
\btcLasVerifyUs\,$\mu$s against the curve baseline, a BIP340 Schnorr
check's \btcSchnorrVerifyUs\,$\mu$s --- \btcLasRatioSchnorr$\times$ --- and a signature that
fails late costs about as much as one that verifies. The valid-path latency corresponds to a
derived serial rate of \btcLasSerialRateK{}k LAS verifications/s on this machine, not
whole-node or network throughput. A patched node is not Bitcoin, so that
conclusion stands; the exercise is functional, with no security argument (\cref{app:btcnode}).

\FloatBarrier
\section{EVM verification as a separate venue check}
\label{sec:res-evm}

As a separate check on deployability, native on-chain LAS verification was
\emph{implemented} and measured on a real Solidity stack: one claim transaction running
full verification and releasing one leg's escrow costs $\approx$\gasLasVerifiedM{} million
gas (\cref{tab:onchain}; \cref{fig:onchain}), $\approx$\gasRatioVerified{}$\times$ the
classical claim and over the EIP-7825 cap. The application is built on the UTXO
setting assumed in \cite{esgin2020post} (\cref{sec:stage2method}); this section measures what
a gas-metered venue charges for the same verification.

That figure measures one \emph{implementation}, not the scheme. A restructured verifier
--- same parameters, bounds and challenge preimage, differing in how the computation is
expressed --- was mined by a real client, running full LAS verification and releasing the
escrow at \gasOptReceipt{} gas: \gasOptCapPct\% of the EIP-7825 cap, which bounds the
transaction gas limit and so covers calldata as well as execution. Its equivalence is
conditional on a registration invariant, and both are set out in \cref{tab:onchain}.
On-chain verification therefore fits in one transaction at \emph{Simplified
Dilithium-III} for the 32-byte signed message and signature instance measured, with a
thin margin. Verification alone at \emph{Simplified Dilithium-II}, charged as one
transaction, is \gasDTwoCapPct\% of the cap; at \emph{Simplified Dilithium-V} one
transaction is exceeded, derived rather than measured (\cref{app:methoddetail}). The
contrast with the application's own venue remains: the UTXO constraint is a transaction
\emph{weight} limit one settlement sits well inside (\cref{sec:res-txstruct}).

\begin{table}[!htbp]
  \centering
  \caption[On-chain settlement gas: classical vs.\ post-quantum]{On-chain gas for the
  swap's claim step, measured on a local EVM (\texttt{forge\ --gas-report}); EVM gas is
  deterministic for fixed bytecode, revision, inputs and state. \textbf{These are
  \emph{execution} figures} --- they exclude the 21{,}000 intrinsic charge and the
  per-byte calldata a real transaction also pays under EIP-7623, which is why the
  under-the-cap claim in the text rests on a client receipt rather than on this table.
  \texttt{claimLAS} performs \emph{no} lattice verification and is a strict lower bound;
  ECDSA verifies in the native \texttt{ecrecover} precompile, whereas both LAS rows run
  in EVM bytecode. The two \texttt{claimLASVerified*} rows decide the same predicate as
  \texttt{base\_verify} (\cref{sec:construction}), the second restructuring that
  computation rather than changing it --- but \textbf{only under a registration invariant
  that neither row checks}, and where it fails a \emph{different} predicate is decided;
  the equivalence testing, the invariant and the cost of breaking it are set out in
  \cref{app:methoddetail}. The client run used anvil at a pinned
  \texttt{osaka} revision with EIP-7825 enforced, over \gasOptCalldata{}\,B of calldata.
  \textbf{Scope}: \emph{Simplified Dilithium-III}, a 32-byte signed message, that
  revision, and one signature instance --- two stages of the verifier branch on
  coefficient values, so variation across instances is unquantified. The
  \gasOptHeadroom{}\,gas of headroom is \emph{measured}; that it
  corresponds to a few hundred further bytes of signed message is \emph{derived} from it
  and the measured per-permutation hash cost.}
  \label{tab:onchain}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{@{}L{0.33\linewidth}L{0.30\linewidth}rr@{}}
    \toprule
    Claim path & On-chain verification & Gas & $\times$ classical \\
    \midrule
    Classical ECDSA adaptor (\texttt{claimClassical})
      & full, \texttt{ecrecover} precompile   & \gasClassical{}     & 1$\times$ \\
    LAS floor (\texttt{claimLAS})
      & none (calldata + \texttt{keccak256})   & \gasLasFloor{}    & \gasRatioFloor{}$\times$ \\
    LAS full verify (\texttt{claimLASVerified})
      & full \texttt{base\_verify}, in Solidity & \gasLasVerified{} & \gasRatioVerified{}$\times$ \\
    LAS full verify, restructured (\texttt{claimLASVerifiedOpt})
      & full \texttt{base\_verify}, in Solidity & \gasLasOpt{} & \gasRatioOpt{}$\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

The same venue distinction is structural as well as numerical:
\cref{fig:evmtx} shows that the EVM keeps the authorising transaction signature
classical and carries the lattice verifier inputs as contract data, unlike the UTXO
settlement structure of \cref{fig:txstruct}.

% Keep these as ordinary floats so they can share nearby body text.
\begingroup
\setlength{\intextsep}{6pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{0pt}

\begin{figure}[tbp]
  \centering
  % Preserve the drawn text size.
  \includegraphics[width=\linewidth]{figures/fig_onchain}
  \caption[On-chain settlement gas for the four claim paths]{The claim paths of
  \cref{tab:onchain} on a logarithmic axis, which is what makes the spread legible. The
  dashed line is the EIP-7825 cap on the transaction gas limit ($16\,777\,216 = 2^{24}$):
  the baseline verifier's bar crosses it, while the restructured
  \texttt{claimLASVerifiedOpt} --- same scheme, same predicate under the registration
  invariant of \cref{tab:onchain}, different expression --- falls below. All four bars
  come from one \texttt{--gas-report} table and so are mutually comparable; the
  under-the-cap claim itself rests on the separate real-client run described in
  \cref{tab:onchain}.}
  \label{fig:onchain}
\end{figure}

% EVM counterpart to the UTXO transaction-structure figure.
\begin{figure}[tbp]
  \centering
  \begin{tikzpicture}[
      font=\normalsize,
      fld/.style={draw, rounded corners=3pt, fill=black!3, align=center,
                  text width=104mm, minimum height=10mm, inner xsep=1.5mm,
                  inner ysep=1mm, line width=0.55pt},
      fix/.style={fld, fill=blue!7, draw=blue!50!black},
      chg/.style={fld, fill=orange!12, draw=orange!60!black, line width=0.75pt},
      off/.style={fld, dashed, fill=green!6, draw=green!45!black}]

    \node[fld] (e1) at (60mm,0mm)
      {normal transaction fields\\nonce, fees, contract address, value};
    \node[fix] (e2) at (60mm,-14mm)
      {consensus signature\\still secp256k1 ECDSA\\65\,B};
    \node[chg] (e3) at (60mm,-31mm)
      {\texttt{data}\\LAS signature and verifier inputs\\\gasOptCalldata\,B};
    \node[off] (e4) at (60mm,-49mm)
      {off-chain only: $Y$, $\pi$, $\hat{\sigma}_1$, $\hat{\sigma}_2$\\
       never transmitted at all: the witness $y$};
  \end{tikzpicture}
  \caption[The claim transaction on the EVM]{The same settlement on Ethereum: the claim
  transaction of the client run in \cref{tab:onchain}, read against the UTXO counterpart
  in \cref{fig:txstruct}. Neither venue gains a field from the adaptor layer, but the
  lattice signature lands in a different structural position. On a Bitcoin-like UTXO
  chain it must occupy the signature slot consensus checks, so the chain needs a new
  consensus rule before it can verify LAS. On the EVM the authorising transaction
  signature remains secp256k1; the lattice signature rides in \texttt{data} as an
  argument to a contract. Contracts therefore make Ethereum the more flexible venue and
  Bitcoin the more restricted one here. The \gasOptCalldata\,B is measured from the
  client receipt and is the whole calldata payload, not the signature alone.}
  \label{fig:evmtx}
\end{figure}
\endgroup

% Let the final section absorb any remaining EVM floats.
\section{Threats to validity}
\label{sec:res-validity}

Several factors qualify these results. All timings are machine-dependent, mitigated by
reporting standard deviations, fixing one machine and compiler, emphasising ratios over
absolutes, and by the cross-implementation check (\cref{sec:res-rust}). The parameter
set's concrete security level is not formally established, security proofs being out of
scope, so every cross-scheme claim is qualified by \cref{tab:params}; the modulus is
FIPS~204's $q\approx 2^{23}$ rather than the $2^{24}$ of \cite{esgin2020post}, which
leaves correctness unaffected ($q>2\gamma$) but changes the security margin. The extracted
witness is exact here, whereas the relaxed setting of \cite{esgin2020post} allows witness
noise that can grow across long payment paths.

Two further factors are specific to \cref{sec:res-stage2}. The UTXO ledger is a model
rather than a node (\cref{app:utxomodel}): real transaction objects, real serialised sizes
and the real leak channel atomicity depends on, but no fees or confirmation latency, so
on-chain byte counts are the honest proxy. That limit belongs to the venue rather than the
study: configurations~2 and~3 need a consensus change, built and run in
\cref{app:btcnode}. Finally, the Stage-2 timings come from a single desktop machine
(\stageTwoCpu{}), so the usability discussion is an extrapolation, not a measurement on
constrained hardware.
