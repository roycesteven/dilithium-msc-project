\chapter{Results and discussion}
\label{ch:results}

This chapter reports what was measured and why the results should be believed:
correctness, computation cost, communication cost, and the application results.

\section{Correctness and robustness}
\label{sec:res-correctness}

No performance claim means anything until the implementation is shown \emph{correct},
and an adaptor signature is not correct merely because it signs and verifies --- it must
satisfy a specific contract. \Cref{tab:contract} lists every clause; all pass, with zero
compiler warnings. Clauses~1--4 are the adaptor contract itself: a pre-signature is
verifiable yet unspendable (the tripwire), becomes an ordinary signature once the witness
is added, and completing it reveals that witness --- the mechanism that makes an atomic
swap atomic. Clauses~5--6 are robustness, clause~7 reproducibility.

\begin{table}[htbp]
  \centering
  \caption[The adaptor-signature correctness contract]{The adaptor-signature correctness contract, each clause checked and passed
  by \texttt{test\_contract} (corroborated at scale by the 1000-iteration functional
  suite on modes 2/3/5, the 256-instance serialization suite, and the pinned
  known-answer test).}
  \label{tab:contract}
  \small
  \begin{tabular}{@{}l L{0.74\linewidth} c@{}}
    \toprule
    \# & Property & Result \\
    \midrule
    1 & PreSign produces a pre-signature that PreVerify accepts & pass \\
    2 & the pre-signature \emph{fails} basic Verify (statement-binding tripwire) & pass \\
    3 & Adapt yields a signature that basic Verify accepts & pass \\
    4 & Extract recovers the witness \emph{exactly} ($y'=y$) & pass \\
    5a & a tampered message fails Verify & pass \\
    5b & every one of the \tamperFlips{} single-byte flips of the packed signature is rejected & pass \\
    6 & malformed packed bytes are rejected ($pk\!\ge\!Q$, ternary code-3, $z$ out of band) & pass \\
    7 & the deterministic API is byte-identical on re-run & pass \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Computation cost}
\label{sec:res-compute}

The primary computation result is the cost of the \emph{adaptor layer}. The two paths
share algorithm, parameters and primitives, so any difference is purely the price of
adaptor functionality (\cref{sec:benchmethod}). \Cref{tab:overhead-l3} reports it at
\emph{both} boundaries and \cref{fig:timing} visualises the core tier: every adaptor
operation stays within single digits of its counterpart, Extract is the cheapest of all,
and every operation completes in well under two milliseconds, signing and pre-signing
dominating because of rejection sampling.

\begin{table}[htbp]
  \centering
  \caption[Adaptor overhead at the target setting, both timing tiers]{The primary
  computation result: per-operation adaptor overhead at the target setting
  \emph{Simplified Dilithium-III} ($n{=}6$, $\ell{=}5$, $\kappa{=}49$,
  $\gamma{=}137\,984$, $d{=}256$), at \emph{both} measurement boundaries of
  \cref{sec:benchmethod}. Each LAS adaptor operation is paired with the basic
  operation it mirrors; ``Overhead'' is the extra cost as a percentage of that
  basic operation, within the same tier. All timings are measured on the C
  implementation, the implementation of record (\cref{sec:benchmethod}); the
  Rust port's independent measurements are in \cref{tab:rust}. Timings are $\mu$s per operation (mean
  $\pm$ sample standard deviation over \benchReps{} repetitions $\times$
  \benchItersSign{}/\benchItersVerify{} iterations; machine of record in
  \cref{sec:benchmethod}). KeyGen, Sign, and Verify are reused unchanged by LAS;
  Extract has no basic analogue. The packed-tier overheads are larger because
  each adaptor operation additionally decodes the public-key-sized statement
  $Y$ (\stmtBytesTarget~B) --- serialization, not adaptor arithmetic. Absolute
  core-tier timings at every parameter set are in \cref{tab:timing} (appendix).}
  \label{tab:overhead-l3}
  \small
  \input{generated/tab_overhead_target}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_timing}
  \caption[Adaptor overhead per operation: basic signature vs LAS]{The primary computation
  result, shown visually: each LAS adaptor operation (orange) paired beside the basic
  operation it mirrors (blue), at the target setting \emph{Simplified Dilithium-III} (its
  parameters are annotated above the plot), measured on the C implementation
  (the implementation of record, \cref{sec:benchmethod}). The percentage above each orange bar is the
  adaptor overhead over its blue partner; exact means and standard deviations, and the
  packed tier, are in \cref{tab:overhead-l3}. KeyGen, Sign, and Verify are reused unchanged by LAS, so KeyGen is
  shown once (shared) and Sign/Verify are the blue partners of PreSign/PreVerify/Adapt;
  Extract has no basic analogue and is shown LAS-only. The absolute $\mu$s scale also shows
  the shape: every operation is sub-two-milliseconds and the rejection-sampling operations
  (Sign, PreSign) dominate, while the verify-class operations and Extract are far cheaper.
  Error bars are the sample standard deviation over \benchReps{} repetitions of
  \benchItersSign{} (sign-class) / \benchItersVerify{} (verify-class) iterations on the
  machine of record (\cref{sec:benchmethod}); the same comparison across all four parameter
  sets is the scaling check in \cref{fig:overhead} (appendix).}
  \label{fig:timing}
\end{figure}

At the core boundary the adaptor layer is almost free, and the reasons are structural.
PreSign ($+\ovPreSign\%$ over Sign) runs the same reject loop, adding only $Y$ to the
commitment and a tighter bound; PreVerify ($+\ovPreVerify\%$) recomputes the same
commitment shape with the extra $+Y$; Adapt ($+\ovAdapt\%$) runs a PreVerify then adds
the witness vector; Extract merely subtracts $z-\hat z$ and checks $A\,y=Y$. None changes
the asymptotic work. This is the headline of the standalone-signature evaluation:
\emph{at the core tier} adaptor functionality costs single-digit percentages at every
parameter set, and the two operations unique to an adaptor signature cost no more than a
verification each (\cref{fig:overhead}, appendix).

At the packed boundary the premium grows to $+\packedOvPreSign\%$,
$+\packedOvPreVerify\%$ and $+\packedOvAdapt\%$ --- an order of magnitude higher. The gap
is serialization, not adaptor arithmetic: each operation additionally decodes the
public-key-sized statement $Y$ (\stmtBytesTarget~B). The object dominating
\emph{communication} cost thus reappears as the dominant \emph{computation} cost once
bytes are included, so a deployment verifying from bytes should budget for the packed row
--- and reported lattice timings are notoriously sensitive to this boundary
\cite{riou2026apples}.

\begin{table}[htbp]
  \centering
  \caption[Rejection-sampling statistics: model vs measurement]{Rejection-sampling
  statistics at the target setting \emph{Simplified Dilithium-III}: the closed-form
  geometric model of \cref{eq:rejacc} against every measured sample. ``Mean'' is
  attempts per call and ``Accept.''\ is the per-attempt acceptance rate --- the two
  quantities every source records, and the ones the model predicts. The per-call
  \emph{dispersion} (the geometric standard deviation $\sigma=E\sqrt{1-1/E}$, the
  medians and 95th percentiles, and the sampled maxima) is annotated on the companion
  acceptance curve of \cref{fig:rejcdf} rather than repeated here. The C driver is the
  implementation of record; the Rust protocol driver and the Criterion harness are
  the independent samples of \cref{sec:rustport}. Every measured row passes the
  five-standard-error validity gate of \cref{sec:benchmethod}.}
  \label{tab:rejstats}
  \small
  \input{generated/tab_rejstats}
\end{table}

\begin{figure}[tbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_rejection_cdf}
  \caption[Cumulative probability of acceptance at the target setting]{The
  probability that a sign-class call has been accepted \emph{within} $k$
  rejection-sampling attempts, at the target setting \emph{Simplified
  Dilithium-III}. The curves are the closed-form geometric model of
  \cref{eq:rejacc} for the basic Sign bound $\gamma-\kappa$ (blue) and the
  tighter LAS PreSign bound $\gamma-\kappa-1$ (orange): acceptance is
  $36.8\%$ and $36.0\%$ respectively on the first attempt, passes $90\%$ by
  five attempts, and then flattens --- the two curves are nearly coincident,
  which is exactly why the PreSign-versus-Sign timing gap is so small. The
  annotated statistics are \emph{measured} on the C implementation (the
  2000-call distribution sample tabulated exactly in \cref{tab:rejstats}). The
  flattening is the practical point: allowing many more attempts buys almost
  nothing, because the overwhelming majority of calls are accepted within the
  first few.}
  \label{fig:rejcdf}
\end{figure}

Signing and pre-signing are dearer than verification because of rejection sampling,
intrinsic to the Fiat--Shamir-with-aborts family rather than a defect here.
\Cref{eq:rejacc} predicts 2.719 attempts per Sign and 2.775 per PreSign, the latter
restarting about $2\%$ more often because its bound is tighter by one; counted directly
rather than estimated from a timing ratio, the run of record observed \rejAttBase{} and
\rejAttLas{} (\cref{tab:rejstats}). At 2500 timed calls the gate band
($\pm\!\sim\!8\%$) is wider than that $2\%$ separation, so the core-tier PreSign overhead
($+\ovPreSign\%$) should be read as of the same order as the prediction rather than as a
precise constant; only the $\approx\!10^5$-call Criterion harness resolves it.

\section{Cross-implementation check: C implementation versus Rust port}
\label{sec:res-rust}

The scheme exists twice (\cref{sec:rustport}), and the two implementations agree at every
level at which they can be compared. At the byte level agreement is exact: the same packed
sizes (public key \pkBytesTarget{}~B, signature \sigBytesTarget{}~B, checked
programmatically on every Rust run) and the same pinned known-answer digest
(\texttt{\katDigestHead\ldots\katDigestTail}). Since that digest covers key generation,
signing, the four adaptor operations and the serialization, a divergence anywhere in
either implementation's sampling, algebra or encoding would flip it.

\begin{table}[htbp]
  \centering
  \caption[Per-operation timing: C implementation vs Rust port]{Per-operation timing of the
  two implementations at the target setting, \emph{Simplified Dilithium-III}
  ($\mu$s per operation). The C and Rust columns come from the two protocol drivers,
  which deliberately mirror each other (same repetition scheme of \benchReps{}
  repetitions $\times$ \benchItersSign{}/\benchItersVerify{} iterations, fixed
  public-parameter seed, fixed message, same success-path gating), so they are directly
  comparable. The final column is the independent Criterion.rs statistical harness
  (\critSamples{} samples per operation, bootstrap 95\% confidence interval of the
  mean); its hot-loop protocol yields lower absolute times, and it is included to show
  that the \emph{relative} conclusions do not depend on the hand-rolled driver.}
  \label{tab:rust}
  \small
  \input{generated/tab_rust}
\end{table}

Timing agreement is looser, the builds going through different compilers, but close:
every operation agrees within about \rustCMaxDev\% (largest gap \rustCMaxDevOp). More
importantly the central conclusion reproduces in both languages. At the core tier the Rust
port measures $+\rustOvPreSign\%$, $+\rustOvPreVerify\%$ and $\rustOvAdapt\%$ --- all
single-digit, like the C column beside them; the slightly \emph{negative} Adapt figure is
the measurement floor rather than a real speed-up, the genuine overhead at this scale
being smaller than the gap between the two verify paths. The packed tier reproduces too
($+\rustPackedOvPreSign\%$, $+\rustPackedOvPreVerify\%$, $+\rustPackedOvAdapt\%$), all
positive and in the C figures' order. Both ports pass the same rejection gate
(\cref{fig:criterion}). The headline results are therefore properties of the algorithm,
not of one codebase, compiler or harness.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_criterion_presign}
  \caption[Criterion.rs sampling distribution for PreSign]{The statistical harness's
  own report for PreSign at the target setting, reproduced unmodified from
  Criterion.rs (its SVG output converted to PDF): \critSamples{} timed samples
  (dots), the kernel-density estimate of the sampling distribution (shaded), the mean
  (vertical blue line), and Criterion's automatic outlier classification (mild /
  severe, orange / red). This is the evidence behind the Criterion column of
  \cref{tab:rust}: a well-formed, single-mode distribution whose spread comes from
  rejection sampling and scheduling noise, with only a handful of classified
  outliers.}
  \label{fig:criterion}
\end{figure}

\section{Communication cost and component breakdown}
\label{sec:res-comm}

The computation results show the adaptor layer is cheap at the core boundary; the size
results show where the real cost lies. \Cref{tab:complete-l3} decomposes every LAS object
into bytes at the target setting, explaining \emph{which} component drives the size.
Sizes are fixed by the encoding, so they are exact and carry no dispersion.

\begin{table}[htbp]
  \centering
  \caption[Serialized size of every LAS object]{Serialized size of every LAS object at
  the \emph{Simplified Dilithium-III} (target) setting, in packed wire bytes (not
  on-chain gas), with its paper notation. ``In the basic signature?'' marks which
  objects LAS shares with the base scheme and which it adds.
  Three facts read off directly: the signature, pre-signature, and adapted signature are
  byte-identical (adaptation changes the \emph{value} of the response, not its size); the
  response $z$ dominates every signature; and the only extra \emph{public} object LAS
  adds over a basic signature is the statement $Y$, the size of a public key. The sizes
  hold for the C and the Rust implementation alike: the wire encoding is byte-identical
  in both by construction, asserted programmatically on every Rust run
  (\cref{sec:res-rust}). Sizes at
  every parameter set: \cref{tab:components} (appendix).}
  \label{tab:complete-l3}
  \small
  \input{generated/tab_complete_target}
\end{table}

Two points follow. First, \textbf{the response $z$ is essentially the entire signature}
--- \zPctTarget\% of it at every parameter set --- since the challenge travels only as its
short digest, so any optimisation of LAS signatures is an optimisation of $z$. Second,
\textbf{an adaptor swap additionally transmits a public-key-sized statement $Y$ and a
signature-sized pre-signature}: the only objects an adaptor scheme sends that a basic
signature does not.

\noindent\textbf{The sharp conclusion: the cost of LAS is not adaptor computation but
communication --- specifically $z$ and $Y$.} The adaptor operations add at most
$+\ovMaxAll\%$ of compute, while the objects on the wire are kilobytes. That matters more
in a blockchain setting than ``the signature is \sigBytesTarget{} bytes'': bytes reaching
a ledger are stored and replicated by every node, while the statement and pre-signature,
though off-chain, still cost both parties bandwidth. The response is stored at full width,
neither range-reduced nor entropy-coded --- the clearest opportunity for reducing that
footprint (\cref{sec:future}).

\subsection{The price of post-quantum: classical adaptor baseline}
\label{sec:res-classical}

The second baseline is a classical secp256k1 ECDSA adaptor signature
\cite{secp256k1zkp}: the natural functional counterpart of LAS, driving the same
scriptless swaps but resting on discrete-log rather than lattice hardness. It measures
the practical cost of moving from a classical adaptor signature to a post-quantum one.
LAS is instantiated at \emph{Simplified Dilithium-II} and only there: secp256k1 offers
roughly 128-bit classical security, so those dimensions are the most like-for-like
engineering match, while -III or -V would pit the classical scheme against a deliberately
stronger configuration. Following
the tier rule, LAS appears at both its boundaries while the classical scheme sits at its
\emph{hybrid native-API boundary}, so the comparison is read conservatively --- but the
conclusions survive either reading, resting on order-of-magnitude gaps rather than
percent-level differences (\cref{tab:classical}).

\begin{table}[htbp]
  \centering
  \caption[Classical vs.\ post-quantum adaptor signature]{Classical vs.\ post-quantum adaptor signature: same functionality, different
  security foundation. Classical secp256k1 ($\approx$128-bit \emph{classical} security)
  versus LAS at the \emph{Simplified Dilithium-II} parameters ($n=4$, $\ell=4$, $M=8$,
  $\kappa=39$, $\gamma=79\,872$, $d=256$, $q=8\,380\,417$). The Dilithium-II dimensions are
  an engineering match to the $\approx$128-bit (NIST level~2) target, \emph{not} a formal
  security-equivalence claim (\cref{tab:params}); the stronger LAS settings are reserved for
  the parameter-sensitivity study (\cref{sec:res-compute}) and would overstate the
  post-quantum cost here. Timings $\mu$s/op (classical: mean $\pm$ SD over \clReps{} runs
  $\times$ \clIters{} iterations; LAS: \benchReps{} repetitions $\times$
  \benchItersSign{}/\benchItersVerify{} iterations; same machine); sizes in bytes.
  Measurement boundaries (\cref{sec:benchmethod}): the classical column is the library's
  \emph{hybrid} native-API boundary --- PreSign/PreVerify/Adapt/Extract pack or parse the
  162-byte pre-signature \emph{inside} the timed calls (packed-like), while
  KeyGen/Sign/Verify exchange in-memory structs with their wire codecs outside
  (core-like); LAS is
  shown at both its core and packed tiers, measured on the C implementation. KeyGen
  also produces the statement/witness, which take the public-key/secret-key size. The
  classical pre-signature is a syntactically distinct object (an ECDSA signature plus a
  discrete-logarithm-equality proof), whereas the LAS pre-signature shares the signature
  format. The final column is \emph{tier-matched}: LAS is taken at whichever of its two
  tiers corresponds to the classical library's boundary for that operation --- the core
  tier for KeyGen/Sign/Verify, whose wire codecs sit outside the timed call on both
  sides, and the packed tier for the four adaptor operations, whose codecs sit inside it
  on both sides. Comparing every row against a single LAS tier would count the codec on
  one side only for four of the seven operations.}
  \label{tab:classical}
  \small
  \input{generated/tab_classical}
\end{table}

Two findings stand out. First, \textbf{the post-quantum price is paid almost entirely in
size}: LAS's signature and public key are roughly $\clRatioSig\times$ and
$\clRatioPk\times$ larger than the classical adaptor's, while every LAS operation stays
well under a millisecond. Even the largest per-operation factor (\clRatioTimeMaxOp,
$\clRatioTimeMax\times$) repays decomposition, because it compares two different
\emph{quantities of work} rather than two speeds for the same work: Algorithm~2 of
\cite{esgin2020post} obliges \textsc{Adapt} to run \textsc{PreVerify} before returning, so
\adaptPreVerifyPct\% of LAS's \lasAdaptPackedUs\,$\mu$s is a full lattice verification and
the rest is largely codec, whereas \texttt{secp256k1\_ecdsa\_adaptor\_decrypt} merely
inverts one scalar and multiplies, leaving verification to a separate exported call
\cite{secp256k1zkp}. Charging the classical side the verification LAS cannot skip puts the
two at \clAdaptMatchedUs\,$\mu$s against \lasAdaptPackedUs\,$\mu$s ---
$\clRatioAdaptMatched\times$, not $\clRatioTimeMax\times$: a property of the two
\emph{interfaces}, not of lattice arithmetic. Second, \textbf{the two schemes pay their
adaptor overhead in opposite ways}. The classical adaptor must attach a
discrete-logarithm-equality proof \cite{fournier2019adaptor}, making its PreSign and
PreVerify several times its own base operations, whereas LAS folds the statement into a
hash it already computes. The lattice adaptor layer is the cheaper one to add.

\section{Testing the simplification: the adaptor on unmodified ML-DSA}
\label{sec:res-mldsa}

\Cref{sec:impl} disabled Dilithium's hint vector, Power2Round and high/low-bit
decomposition, reasoning that the adaptor needs the exact identity $A\vecz-c\,t=w+Y$.
That was an \emph{assertion}, so it was tested: the four adaptor operations were built a
third time on NIST ML-DSA \emph{as FIPS~204 specifies it} \cite{1274601}, all three
optimisations enabled. Three findings follow; the second overturns the assertion.

\textbf{The naive port fails outright.} Adding the statement to the challenge hash and
changing nothing else --- treating the hint machinery as a black box --- yields
pre-signatures that do not pre-verify at all (\mldsaNaiveP{}/\mldsaIters{}), because the
transmitted hint reconstructs $\mathrm{HighBits}(w)$ while the challenge committed to
$\mathrm{HighBits}(w+Y)$. Some modification is therefore genuinely unavoidable.

\textbf{But the verifier is not what must change.} Once the \emph{whole} commitment path
moves onto $w+Y$ --- the committed high bits, the low-bits rejection test \emph{and}
\textsc{MakeHint} --- and \textsc{PreSign} tightens its bound by the witness norm, the
construction satisfies the full adaptor contract (\mldsaContract{} items, including four
tamper rejections and byte-identical determinism), and, decisively, \textbf{the unmodified
FIPS~204 verifier accepts the adapted signature} (\mldsaRepairedP{}/\mldsaIters{}). So
\textsc{PreSign} and \textsc{PreVerify} are necessarily new algorithms, while
\textsc{Verify} is not: a post-quantum adaptor swap could settle against a standard ML-DSA
verifier. A matched no-statement baseline reproduces FIPS~204's own published repetition
rates, which is what makes these rows attributable to the adaptor rather than to a porting
error.

\textbf{The size gain is real but bounded, and it relocates the bottleneck.} Measured
against the scheme of record in one process, the adaptor layer stays single-digit on both
constructions ($+\mldsaOvPreSign\%$ against $+\mldsaOvPreSignSimp\%$ for \textsc{PreSign}
per attempt), and per-signature cost is close to equal for opposite reasons: ML-DSA
restarts about twice as often (\mldsaAttempts{} against \mldsaAttemptsSimp{}) but each
attempt is roughly half the price. What ML-DSA buys is \emph{bytes} --- the signature
falls to \mldsaSigBytes{}\,B from \mldsaSigBytesSimp{}\,B --- but the statement $Y$ does
not shrink at all (byte-identical at \mldsaStmtBytes\,B), because Power2Round compresses
a public key while $Y$ enters the verification identity \emph{before} any rounding. The
swap payload therefore improves only to \mldsaPayloadRatio$\times$, and $Y$ becomes larger
than the signature it accompanies. \textbf{Compressing the signature moves the bottleneck
to the statement rather than removing it}, redirecting the size question of
\cref{sec:res-comm} and the future work of \cref{sec:future}.

This is a functional demonstration, not a security argument: whether committing to
$\mathrm{HighBits}(w+Y)$ preserves ML-DSA's unforgeability is not analysed, and is
exactly the kind of claim \cref{sec:eval-limitations} declares out of scope.

\section{Application demonstration}
\label{sec:res-app}

The implementation drives an end-to-end post-quantum atomic swap following the swap
protocol \cite{esgin2020post} \emph{verbatim}, including its proof of knowledge $\pi$
(\cref{app:swapdemo}). Two results matter. First, the protocol runs and every step is
asserted, including the counterfactuals that a pre-adaptation signature fails basic
verification and that $\pi$ is rejected against any other statement. Second, $\pi$ is what
makes the second party's final step \emph{guaranteed} rather than merely expected: by the
Module-SIS uniqueness argument of \cite{esgin2020post} the extracted witness equals the
proven ternary one, so the adapted signature is certain to clear the verification bound
--- without $\pi$ a malicious counterparty could claim and leave an unadaptable
pre-signature behind. The proof measures approximately 31 kilobytes at a knowledge error
below $2^{-127}$ and is exchanged off-chain only.

As a supporting check on deployability, native on-chain LAS verification was
\emph{implemented} and measured on a real Solidity stack. One full verified settlement
costs approximately \gasLasVerifiedM{} million gas (\cref{tab:onchain};
\cref{fig:onchain}), roughly \gasRatioVerified{}$\times$ the classical ECDSA-adaptor
claim, which verifies in a native precompile whereas LAS runs entirely in EVM bytecode
--- exceeding the EIP-7825 per-transaction cap of $16\,777\,216$ gas. An \emph{optimistic}
(Naysayer) alternative makes honest settlement cheap ($\approx$\gasNaysayClaimM{} million
gas) but leaves a worst-case fraud proof at $\approx$\gasNaysayDigestM{} million, also
over the cap, and a fraud proof that cannot be mined is not one (\cref{app:naysayer}).
These measurements are why the application was taken to a UTXO chain.

\begin{table}[htbp]
  \centering
  \caption[On-chain settlement gas: classical vs.\ post-quantum]{On-chain settlement gas
  for the atomic swap's claim step, measured on a local EVM (Foundry
  \texttt{forge\ --gas-report}). Gas is deterministic for the fixed bytecode, EVM
  revision, inputs, and contract state, and is independent of the host CPU. The ratios
  compare complete settlement calls, including calldata, memory, state transition, event
  emission, transfer, and verification. Both the classical path and the full LAS path perform
  \emph{complete} signature verification --- the difference is that ECDSA verifies in the
  native \texttt{ecrecover} precompile whereas LAS runs its entire verifier in EVM
  bytecode. \texttt{claimLAS} is a floor that performs \emph{no} lattice verification
  (calldata for the \sigBytesTarget-byte signature plus one \texttt{keccak256}), a strict
  lower bound. \texttt{claimLASVerified} runs the complete native \texttt{base\_verify}
  (\cref{sec:construction}) --- decode, the norm bound, $w'=A\vecz-c\,t$, and the SHAKE256
  challenge check --- assembled from the vendored ZKNox ETHDILITHIUM primitives and
  validated end-to-end against the C reference. At $\approx$\gasLasVerifiedM{}\,M gas it exceeds the
  EIP-7825 per-transaction cap ($16\,777\,216 = 2^{24}$) by $\approx$3.4$\times$, so it
  cannot execute as a single mainnet transaction; its raw demand is below the 60-million
  default block gas limit, but the per-transaction cap is the binding ceiling.}
  \label{tab:onchain}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{@{}L{0.33\linewidth}L{0.30\linewidth}rr@{}}
    \toprule
    Claim path & On-chain verification & Gas & $\times$ classical \\
    \midrule
    Classical ECDSA adaptor (\texttt{claimClassical})
      & full, \texttt{ecrecover} precompile   & \gasClassical{}     & 1$\times$ \\
    LAS floor (\texttt{claimLAS})
      & none (calldata + \texttt{keccak256})   & \gasLasFloor{}    & \gasRatioFloor{}$\times$ \\
    LAS full verify (\texttt{claimLASVerified})
      & full \texttt{base\_verify}, in Solidity & \gasLasVerified{} & \gasRatioVerified{}$\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_onchain}
  \caption[On-chain settlement gas for the three claim paths]{On-chain settlement gas for
  the three atomic-swap claim paths (\cref{tab:onchain}), measured on a local EVM
  (\texttt{forge --gas-report}; deterministic EVM gas), on a logarithmic axis. The
  classical \texttt{claimClassical} (\gasClassical{} gas) verifies in the native \texttt{ecrecover}
  precompile; \texttt{claimLAS} (\gasLasFloor{}) is a settlement floor doing \emph{no} lattice
  verification; and \texttt{claimLASVerified} (\gasLasVerified{}, $\approx$\gasRatioVerified{}$\times$ the
  classical claim) runs the complete native LAS \texttt{base\_verify} in EVM bytecode. The
  dashed line is the EIP-7825 per-transaction gas cap ($16\,777\,216 = 2^{24}$); the full
  verifier's bar crosses it, which is exactly why it cannot execute as a single mainnet
  transaction and why a deployable path needs a precompile, a succinct proof, or an
  optimistic (Naysayer) scheme.}
  \label{fig:onchain}
\end{figure}

\section{The swap on a UTXO chain: three configurations compared}
\label{sec:res-stage2}

The demonstration above establishes that the protocol runs; this section measures what
it \emph{costs}, over the UTXO ledger and under the design of \cref{sec:stage2method}:
three configurations (\cref{tab:configs}), of which only the $2\to3$ step is controlled.
Timings are the mean $\pm$ sample standard deviation over \stageTwoRuns{} complete swaps
at the \emph{packed} tier, so they are not comparable with \cref{sec:res-compute}. Both
LAS configurations passed the rejection gate (\cfgTwoRejMeasured{} attempts per
\textsc{PreSign} against the closed-form \cfgTwoRejExpected{}).

\begin{table}[htbp]
  \centering
  \caption[Per-phase execution time of the swap]{Per-phase execution time of one
  complete swap, mean $\pm$ sample SD over \stageTwoRuns{} swaps
  ($\mu$s; run \stageTwoRunId{}, \stageTwoCpu{}). Dashes mark phases a
  configuration does not have. End-to-end is context, not the headline.}
  \label{tab:stage2-time}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Phase & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    KeyGen $\times2$ + Gen & \cfgOneKeyMatMean{} & \cfgTwoKeyMatMean{} & \cfgThreeKeyMatMean{} \\
    Prove ($\pi$)          & ---                 & \cfgTwoProveMean{}  & \cfgThreeProveMean{} \\
    PreSign ($u_1$)        & \cfgOnePreSignAMean{} & \cfgTwoPreSignAMean{} & \cfgThreePreSignAMean{} \\
    ProofVerify ($\pi$)    & ---                 & \cfgTwoProofVerifyMean{} & \cfgThreeProofVerifyMean{} \\
    PreVerify (abort gate) & \cfgOnePreVerifyMean{} & \cfgTwoPreVerifyMean{} & \cfgThreePreVerifyMean{} \\
    PreSign ($u_2$)        & \cfgOnePreSignBMean{} & \cfgTwoPreSignBMean{} & \cfgThreePreSignBMean{} \\
    Adapt ($u_1$)          & \cfgOneAdaptAMean{} & \cfgTwoAdaptAMean{} & \cfgThreeAdaptAMean{} \\
    Settle chain 2         & \cfgOneSettleBMean{} & \cfgTwoSettleBMean{} & \cfgThreeSettleBMean{} \\
    Extract ($u_2$)        & \cfgOneExtMean{} & \cfgTwoExtMean{} & \cfgThreeExtMean{} \\
    Adapt ($u_2$)          & \cfgOneAdaptBMean{} & \cfgTwoAdaptBMean{} & \cfgThreeAdaptBMean{} \\
    Settle chain 1         & \cfgOneSettleAMean{} & \cfgTwoSettleAMean{} & \cfgThreeSettleAMean{} \\
    \midrule
    end-to-end             & \cfgOneTotalMean{} & \cfgTwoTotalMean{} & \cfgThreeTotalMean{} \\
    \quad of which $\pi$   & ---                & \cfgTwoProofPct{}\% & \cfgThreeProofPct{}\% \\
    \quad excluding $\pi$  & \cfgOneNonProofUs{} & \cfgTwoNonProofUs{} & \cfgThreeNonProofUs{} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[htbp]
  \centering
  \caption[Communication cost of the swap]{Communication cost of one complete swap,
  in bytes, observed from the transmitted buffers over \stageTwoRuns{} swaps. A
  range indicates a message whose size varied between runs. Off-chain messages are
  counted, not only what reaches a ledger.}
  \label{tab:stage2-comm}
  \small
  \begin{tabular}{@{}l r r r@{}}
    \toprule
    Message & Classical & LAS + Groth16 & LAS + LaZer \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{Off-chain (exchanged directly between the parties)}} \\
    \quad statement $Y$    & \cfgOneMsgY{} & \cfgTwoMsgY{} & \cfgThreeMsgY{} \\
    \quad proof $\pi$      & ---           & \cfgTwoMsgPi{} & \cfgThreeMsgPi{} \\
    \quad $\hat{\sigma}_1$ & \cfgOneMsgPreSigA{} & \cfgTwoMsgPreSigA{} & \cfgThreeMsgPreSigA{} \\
    \quad $\mathit{tx}_1$  & \cfgOneMsgTxA{} & \cfgTwoMsgTxA{} & \cfgThreeMsgTxA{} \\
    \quad $\hat{\sigma}_2$ & \cfgOneMsgPreSigB{} & \cfgTwoMsgPreSigB{} & \cfgThreeMsgPreSigB{} \\
    \quad $\mathit{tx}_2$  & \cfgOneMsgTxB{} & \cfgTwoMsgTxB{} & \cfgThreeMsgTxB{} \\
    \addlinespace[2pt]
    \quad\textbf{off-chain total} & \textbf{\cfgOneBytesOffChain{}}
                                  & \textbf{\cfgTwoBytesOffChain{}}
                                  & \textbf{\cfgThreeBytesOffChain{}} \\
    \midrule
    \multicolumn{4}{@{}l}{\textit{On-chain (published to the ledgers)}} \\
    \quad $\mathit{tx}_1 + \sigma_1$ (chain 1)
      & \cfgOneMsgChainA{} & \cfgTwoMsgChainA{} & \cfgThreeMsgChainA{} \\
    \quad $\mathit{tx}_2 + \sigma_2$ (chain 2)
      & \cfgOneMsgChainB{} & \cfgTwoMsgChainB{} & \cfgThreeMsgChainB{} \\
    \addlinespace[2pt]
    \quad\textbf{on-chain total} & \textbf{\cfgOneBytesOnChain{}}
                                 & \textbf{\cfgTwoBytesOnChain{}}
                                 & \textbf{\cfgThreeBytesOnChain{}} \\
    \midrule
    \textbf{total per swap} & \textbf{\cfgOneBytesTotal{}}
                            & \textbf{\cfgTwoBytesTotal{}}
                            & \textbf{\cfgThreeBytesTotal{}} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{The proof, not the signature, is the cost of a post-quantum swap.}
The role-A proof consumes \cfgTwoProofPct{}\% of end-to-end time in configuration~2 and
\cfgThreeProofPct{}\% in configuration~3. Strip it out and the entire post-quantum
signature workload --- two key generations, a statement, two pre-signatures, a
pre-verification, two adaptations, an extraction and two settlements --- costs
\cfgTwoNonProofUs{}\,$\mu$s, about \stageTwoCryptoRatio{}$\times$ the classical swap's
\cfgOneNonProofUs{}\,$\mu$s. A factor of five on a millisecond-scale workload is
unremarkable; a proof of hundreds of milliseconds decides whether the protocol feels
instantaneous. Even at the packed tier the signature workload is not the bottleneck ---
the post-quantum penalty is not where intuition puts it.

\paragraph{The controlled comparison inverts the expected trade-off.} Replacing Groth16
with LaZer --- same signature, same $\mathbf{A}$, same inputs --- makes the swap
\emph{faster} by \cmpTimePctAbs{}\% while making it \emph{larger} by
\cmpBytesPctAbs{}\%: roughly twice as quick to produce a proof, yet emitting one about
\stageTwoProofSizeRatio{}$\times$ bigger. Groth16 is \emph{succinct} --- three group
elements whatever the circuit --- but producing that proof means a multi-scalar
multiplication over the whole constraint system, whereas LaZer's proof grows with the
statement yet proves a lattice relation with lattice machinery, avoiding the encoding of
a 23-bit modulus into a 254-bit pairing-friendly field. Verification runs the other way
(\cfgTwoProofVerifyMean{}\,$\mu$s against \cfgThreeProofVerifyMean{}), succinctness
paying off exactly where it is designed to.

\paragraph{Configuration~2's trade is worse than it looks.} Pairing security is broken
by Shor, so its proof is precisely the component a post-quantum adversary attacks: a
post-quantum signature does not protect a swap whose fairness rests on a classical proof.
Groth16 also needs a \emph{trusted, per-circuit} setup (\cfgTwoSetupTime{}) whose secret
must be destroyed for soundness, whereas LaZer's parameters are transparent.
Configuration~3 is thus not merely the post-quantum option: it is faster end-to-end,
needs no trusted party and removes a quantum vulnerability, paying only in bytes.

\paragraph{Communication is again the real price, with a usability consequence.} A
classical swap moves \cfgOneBytesTotal{}\,B; the fully post-quantum swap moves
\cfgThreeBytesTotal{}\,B, about \stageTwoBytesRatioThree{}$\times$ more, and puts
\stageTwoOnChainRatio{}$\times$ more on the ledgers --- the multiple that matters
economically, since a UTXO chain prices by size. A fully post-quantum swap needs
\cfgThreeTotalMean{}\,$\mu$s and tens of kilobytes of off-chain messaging per exchange,
overwhelmingly the proof, against \cfgOneTotalMean{}\,$\mu$s classically: comfortable on
a laptop, but a wide enough gap that a mobile wallet's assumptions would not port
naively. No constrained device was measured, so that point should not be over-read.

\section{Transaction structure and what a UTXO chain would charge}
\label{sec:res-txstruct}

The sizes above are the model ledger's own encoding (\cref{app:utxomodel}). Laid out
instead in Bitcoin's wire format, what does a settled swap cost, and does it fit?
\Cref{tab:btctx} answers by projecting the \emph{measured} object sizes onto the
serialisation of \cite{bip141,bip144,bip341}; the arithmetic is generated, and accepted
only if it first reproduces two published reference figures.

\begin{table}[htbp]
  \centering
  \caption[The settled transaction, field by field]{One settled swap transaction in
  Bitcoin's wire format, field by field. The layout is identical in both columns
  (\cref{fig:txstruct}); only the sizes differ. Object sizes are measured (run
  \stageTwoRunId{}); the field arithmetic is derived from \cite{bip141,bip144,bip341}
  and self-checked against the published 110\,vB P2WPKH reference spend. Virtual size is
  the quantity a UTXO chain prices, and the gap between it and total size is the
  witness discount. The model ledger's own encoding (\cref{tab:stage2-comm}) totals
  \cfgThreeMsgChainA\,B for the same objects, agreeing with the \btcThreeTotal\,B here
  to within 0.2\,\%: the two differ only in where the public key sits --- inline in the
  model's output, in the witness under Bitcoin's layout --- and in Bitcoin's marker,
  flag and compactSize prefixes.}
  \label{tab:btctx}
  \small
  \input{generated/tab_btctx}
\end{table}

\paragraph{The witness discount absorbs more than half of the post-quantum penalty.}
In raw bytes the post-quantum settlement is \btcRawRatio$\times$ the classical one; in
virtual size --- what fees are charged on --- only \btcVsizeRatio$\times$. SegWit bills
witness bytes at one weight unit and all others at four, and a post-quantum signature is
\emph{entirely} witness data. That decision, taken long before anyone considered lattice
signatures on Bitcoin, removes \btcDiscountSaving\,\% of the size penalty. It is a
concrete argument for UTXO chains as the venue, with no counterpart on the EVM, where
calldata carries no comparable discount and native verification cost
\gasLasVerifiedM{}\,M gas (\cref{tab:onchain}).

\paragraph{It fits the size limits, but not the verification rules.}
A settlement weighs \btcThreeWeight{}\,WU against the \btcMaxStdWeight{}\,WU standardness
limit --- \btcThreePctStd\,\% of it --- and the \btcMaxBlockWeight{}\,WU block limit
admits an upper bound of \btcThreePerBlock{} settlements per block against
\btcOnePerBlock{} classical ones, ignoring competing transactions. A UTXO chain does
impose a limit, then, but a weight limit rather than a gas limit, and one swap sits well
inside it --- the contrast with the EVM, where native verification exceeded the gas limit
outright and forced the change of venue.

Two obstacles survive, neither caused by the adaptor layer. No deployed consensus rule
verifies a lattice signature: Script offers \texttt{OP\_CHECKSIG} over ECDSA or BIP-340
Schnorr and nothing else, so configurations 2 and 3 could not settle on Bitcoin as it
stands; \cite{esgin2020post} assumes only the venue, a UTXO chain ``where the signature
algorithm is replaced with a lattice-based signature scheme'', and this study takes no
position on which consensus route would deliver it. Separately, the objects collide with
limits that exist regardless: the signature is \btcSigOverElement$\times$ over the 520\,B
consensus stack-item limit and \btcSigOverStdElement$\times$ over the 80\,B standardness
limit \cite{bitcoincorepolicy}. Both obstacles apply equally to an ordinary post-quantum
payment: they are properties of lattice signature sizes, not of adaptor signatures.

\section{Threats to validity}
\label{sec:res-validity}

Several factors qualify these results. All timings are machine-dependent, mitigated by
reporting standard deviations, fixing one machine and compiler, emphasising ratios over
absolutes, and by the cross-implementation check (\cref{sec:res-rust}). The parameter
set's concrete security level is not formally established, security proofs being out of
scope, so every cross-scheme claim is qualified by \cref{tab:params}; the modulus is
FIPS~204's $q\approx 2^{23}$ rather than the $2^{24}$ of \cite{esgin2020post}, which
leaves correctness unaffected ($q>2\gamma$) but changes the security margin. The extracted
witness is exact here, whereas the relaxed setting of \cite{esgin2020post} allows witness
noise that can grow across long payment paths.

Two further factors are specific to \cref{sec:res-stage2}. The UTXO ledger is a model
rather than a node (\cref{app:utxomodel}): real transaction objects, real serialised sizes
and the real leak channel atomicity depends on, but no fees or confirmation latency, so
on-chain byte counts are the honest proxy. That limit belongs to the venue rather than the
study, since configurations~2 and~3 cannot settle on a real node without a consensus
change. Finally, the Stage-2 timings come from a single desktop machine
(\stageTwoCpu{}), so the usability discussion is an extrapolation, not a measurement on
constrained hardware.
