\chapter{Introduction}
\label{ch:intro}

\section{Context and motivation}
\label{sec:motivation}

Public blockchains commonly authorise transactions with the Elliptic Curve Digital
Signature Algorithm (ECDSA) or Schnorr. Their security relies on the hardness of the
elliptic-curve discrete logarithm problem, which Shor's algorithm solves in polynomial
time on a sufficiently large quantum computer \cite{shor1999polynomial}, allowing an
adversary to forge signatures and spend other users' funds. A published 2026 resource
estimate addresses that curve directly (\cref{fig:whynow}). A
chain cannot treat that as distant: its record is permanent, so a break reaches keys
already exposed, and changing how it verifies signatures requires a coordinated
consensus-rule change, not merely a local software update. ``Post-quantum'' (PQ)
cryptography replaces this assumption with
problems believed hard even for quantum computers, such as those over lattices.

The U.S. National Institute of Standards and Technology (NIST) has standardised
\emph{basic} PQ signatures, notably ML-DSA (the Module-Lattice-Based Digital Signature
Algorithm), derived from CRYSTALS-Dilithium
\cite{1274601,ducas2018crystals}. A basic signature authenticates a message and nothing
more. Blockchains, however, depend on \emph{exotic} signatures that add functionality
on top of a basic scheme: multi-signatures, aggregate signatures, threshold
signatures, and \emph{adaptor signatures}. A recent survey documents that their exotic
counterparts remain unevenly served by practical implementations
in blockchain settings \cite{buser2023survey}. Turning one such paper design into a
tested, measured artefact is the problem this project addresses.

\begin{figure}[htbp]
  \centering
  % Type is \normalsize throughout: nothing inside a figure may be smaller than the
  % body text.  Every width below was re-derived for 12pt from the longest line it has
  % to hold; if something no longer fits, widen the box or break the line -- never
  % shrink the font.
  \begin{tikzpicture}[
      font=\normalsize,
      ttl/.style={font=\normalsize\bfseries, align=center, text width=140mm},
      flow/.style={draw, rounded corners=2pt, align=center, text width=38mm,
                   minimum height=22mm, inner xsep=1.6mm, inner ysep=1.2mm,
                   line width=0.6pt},
      note/.style={draw, rounded corners=2pt, align=center, font=\normalsize,
                   text width=120mm, inner xsep=1.5mm, inner ysep=1.2mm,
                   line width=0.5pt},
      head/.style={font=\normalsize\bfseries, align=center, text width=47mm},
      row/.style={font=\normalsize\bfseries, align=right, text width=26mm},
      cell/.style={draw, rounded corners=2pt, align=center, text width=44mm,
                   minimum height=22mm, inner xsep=1.5mm, inner ysep=1.2mm,
                   line width=0.6pt},
      >={Stealth[]}]

    %% The drawing follows the Meeting-12 clarity rule: the proof numbers are evidence,
    %% but the reader's first mental image should be "current chain signatures -> quantum
    %% key recovery -> replacement pressure", followed by the simple basic/exotic map.

    %% ---- (a) current chain signatures under quantum pressure ----
    %% RSA is deliberately absent.  Wang's objection was that the motivation should start
    %% from Bitcoin/Ethereum signatures, not a cryptosystem neither chain uses.
    \node[ttl] at (0mm,0mm) {(a) Why current chain signatures need replacement};

    \node[flow, fill=black!5, draw=black!55] (today) at (-48mm,-20mm)
      {\textbf{Current chains}\\
       Bitcoin:\\
       ECDSA, Schnorr\\
       Ethereum: ECDSA\\
       \emph{over secp256k1}};
    \node[flow, fill=purple!8, draw=purple!60!black] (quantum) at (0mm,-20mm)
      {\textbf{Quantum attack}\\
       Shor recovers\\
       signing keys\\
       % The year is the ESTIMATE'S VINTAGE (babbush2026securing), never a date by
       % which the attack arrives -- "resource" is what keeps the two readings apart.
       \emph{2026 resource estimate}};
    \node[flow, fill=red!7, draw=red!60!black] (risk) at (48mm,-20mm)
      {\textbf{Replacement}\\
       \textbf{pressure}\\
       forged spends\\
       exposed keys\\
       consensus change};

    \draw[->, line width=0.75pt, red!60!black] (today.east) -- (quantum.west);
    \draw[->, line width=0.75pt, red!60!black] (quantum.east) -- (risk.west);
    \node[note, fill=yellow!12, draw=yellow!55!black] at (0mm,-45mm)
      {Evidence markers: $<500{,}000$ physical qubits and minutes for secp256k1 key
       recovery \cite{babbush2026securing}; NIST disallows vulnerable signatures at
       this strength after 2035 \cite{nistir8547}.};

    %% ---- (b) LAS in the basic/exotic and classical/PQ map ----
    \node[ttl] at (0mm,-66mm)
      {(b) Where LAS sits: basic vs.\ exotic $\times$ classical vs.\ post-quantum};

    \node[head] at (-14mm,-80mm) {\textbf{Classical}\\breaks under Shor};
    \node[head] at (40mm,-80mm) {\textbf{Post-quantum}\\quantum-resistant};

    \node[row] at (-52mm,-100mm) {Basic\\{\normalfont just sign}};
    % "sign + a condition" describes the ADAPTOR only: multi-signatures change who must
    % authorise and aggregate signatures compress, so that label mis-defined the class.
    \node[row] at (-52mm,-126mm) {Exotic\\{\normalfont extra functionality}};

    \node[cell, fill=black!5, draw=black!55] at (-14mm,-100mm)
      {\textbf{ECDSA / Schnorr}\\current chain authentication};
    \node[cell, fill=green!8, draw=green!50!black] at (40mm,-100mm)
      {\textbf{Dilithium / ML-DSA}\\NIST standard \cite{1274601}\\deployment started
       \cite{valenta2026pqauth}};
    \node[cell, fill=black!5, draw=black!55] at (-14mm,-126mm)
      {\textbf{ECDSA adaptor}\\scriptless swaps\\and channels};
    \node[cell, fill=red!7, draw=red!60!black, line width=0.9pt] at (40mm,-126mm)
      {\textbf{LAS --- this project}\\post-quantum\\adaptor signature};
  \end{tikzpicture}
  \caption[Why now, and where the gap is]{Why the problem is timely, and where this
  project sits within it. \textbf{(a)} The quantum argument is made from the signatures
  Bitcoin and Ethereum actually use, not from RSA: a published 2026 estimate gives the
  cost of recovering secp256k1 keys, while the 2035 marker is the independent NIST
  transition deadline for quantum-vulnerable signatures at this strength. \textbf{(b)}
  Basic post-quantum signatures are standardised, but blockchain applications need
  exotic signatures such as adaptors. That post-quantum exotic cell is the gap this
  dissertation addresses.}
  \label{fig:whynow}
\end{figure}
\FloatBarrier

This dissertation focuses on the \emph{adaptor signature}, which ties publishing
a valid signature to simultaneously revealing a secret witness
\cite{poelstra2017,aumayr2021generalized}. That property makes adaptor signatures useful
for scriptless blockchain protocols. In an atomic swap, two parties exchange assets across
chains without a trusted intermediary (\cref{fig:swapidea}). They also apply to payment-channel
networks, linking conditional off-chain updates through the same
witness mechanism. The condition is embedded in the signature, so settlement verifies as
% "for that condition" is NOT optional padding: without it the sentence reads as a claim
% that settlement uses no script at all, which is false -- every Bitcoin output carries a
% scriptPubKey.  Scriptless means no swap-SPECIFIC script.
an ordinary signature and needs no on-chain script for that condition
\cite{esgin2020post,buser2023survey}. The exotic family is large, so the choice needs a
reason. Multi-signatures and threshold signatures change who must cooperate to authorise
a spend. Aggregate signatures compress many signatures into one. An adaptor signature
serves a different purpose: it links two \emph{separate} settlements. Once an adapted
signature is published, a party holding the matching pre-signature can extract the secret
and use it to complete the other settlement. That is the property an atomic swap needs,
and the other three primitives do not provide it by themselves. Common classical adaptor
constructions rely on discrete-logarithm assumptions, so they do not survive Shor's
algorithm. Practical post-quantum alternatives remain limited. This distinct function,
the applications that depend on it, and the implementation gap are why the adaptor
signature was selected.

% MOVED HERE FROM ch:results 2026-08-27, after Meeting 12.  Wang reviewed the REPORT with
% the PDF on screen and named the adaptor motivation as the one substantive shortfall --
% "why is it important for blockchains?", "we need some concrete motivation, the concrete
% examples", "you should at least give some introduction regarding applications of the
% adaptor signature".  This figure is exactly that concrete example, so it belongs beside
% the claim it makes concrete rather than three chapters later.
%
% This does NOT disturb Meeting 10's ruling: fig:swapflow itself remains ACCEPTED, while
% this simpler diagram is the friendly application picture "before it".  The protocol map
% now belongs in ch:methodology, because it defines the run book; ch:results reports what
% happened when that run book was executed.
%
% Word cost is nil: a figure body and its caption are both excluded from the count, and the
% \cref added above is paid for by the one dropped from ch:results.
%
% PARTY NAMING IS SETTLED (2026-08-16) -- do not "fix" it either way.  The subscripts stay:
% this figure is the ONLY place Alice/Bob is tied to the u_1/u_2 the technical chapters use,
% and dropping them reintroduces exactly the inconsistency that decision removed.  What did
% not survive the move is the caption's old "used from here on", which was true when this
% figure sat in ch:results and is false here; the tie is restated neutrally instead.
%
% REDRAWN 2026-08-27 and the redraw MUST survive any future edit.  An earlier version drew
% one arrow from Alice to Bob and another back, with the ledgers reduced to words ON those
% arrows -- so the picture invited the reading that a coin travels from one chain to the
% other, which is false, and left the caption denying what the drawing showed.  The ledgers
% are the drawn objects now: each payment is a transaction sitting INSIDE its own lane.  Do
% not reintroduce a party-to-party coin arrow.  Absolute coordinates only -- no calc.
\begin{figure}[htbp]
  \centering
  % Type is \normalsize throughout: nothing inside a figure may be smaller than the
  % body text.  The whole picture was re-laid out for 12pt -- lanes 138mm, the chain
  % title inside each lane's top strip and the "settles here" label in its bottom
  % strip, which is what stops the two colliding once the type is this size.  Widen a
  % box or move a row; never shrink the font back.
  %
  % The Ethereum mark's own colour, scoped to this figure so it cannot leak into the
  % report's semantic palette, which it must never join.  Bitcoin needs no counterpart here:
  % its #F7931A field is a path INSIDE figures/bitcoin-symbol.svg, not painted by TikZ, so a
  % \definecolor{btcmark} was left dead by that change and is gone.
  \definecolor{ethmark}{HTML}{627EEA}
  \begin{tikzpicture}[
      font=\normalsize,
      party/.style={draw, rounded corners=4pt, align=center, minimum width=50mm,
                    minimum height=20mm, line width=0.65pt},
      lane/.style={draw, rounded corners=4pt, minimum width=138mm,
                   minimum height=26mm, line width=0.65pt},
      tx/.style={draw, rounded corners=3pt, align=center, font=\normalsize,
                 minimum width=36mm, minimum height=13mm, line width=0.5pt},
      coin/.style={circle, draw, align=center, minimum size=13mm, inner sep=0pt,
                   font=\normalsize\bfseries},
      lbl/.style={font=\normalsize, align=center},
      >={Stealth[]}]

    \node[party, fill=orange!10, draw=orange!65!black] (alice) at (38mm,0mm)
      {\textbf{Alice $(u_1)$}\\[-0.4mm]
       has 1 BTC\\[-0.4mm]
       wants ETH};
    \node[party, fill=blue!8, draw=blue!60!black] (bob) at (102mm,0mm)
      {\textbf{Bob $(u_2)$}\\[-0.4mm]
       has ETH\\[-0.4mm]
       wants BTC};
    % THE REAL CURRENCY MARKS, not the letters "BTC"/"ETH" (Royce, 2026-09-01).  The
    % Bitcoin mark is the Bitcoin Design/Bitboy SVG symbol included as a figure asset,
    % so the report no longer depends on a font-drawn approximation.  Ethereum stays as
    % inline facets because that mark is already exact at this scale.  #F7931A and
    % #627EEA are the ONLY literal brand colours here; every other colour in this figure
    % is the report's own semantic palette and must never be recoloured to a brand.
    %
    % Bitcoin: include the actual SVG-derived mark, not a local font's capital B plus
    % hand-drawn strokes.  The matching PDF is generated from figures/bitcoin-symbol.svg.
    % ⚠ A MARK IS ARTWORK, NOT A LABEL: the "nothing smaller than the body's 12pt" rule is a
    % FLOOR, so it says nothing about a logo's size.  Do not rescale either mark to satisfy
    % a type sweep, and do not replace this asset with a font's capital B -- the earlier
    % font-drawn version needed hand-placed strokes whose full-height form filled the
    % letter's counters white, which only a 600dpi render showed.
    \node[inner sep=0pt] at (6mm,0mm)
      {\includegraphics[width=13mm]{figures/bitcoin-symbol}};
    % Ethereum: the six facets of the diamond, ported coordinate-for-coordinate from the
    % deck's SVG of the mark and pre-multiplied to mm (one SVG unit = 0.40mm), white at the
    % mark's own three fill opacities over the blue disc.  The facets tile the diamond and
    % do not overlap, so the opacities read as the mark's shading rather than as blending.
    \node[coin, fill=ethmark, draw=ethmark!75!black] at (134mm,0mm) {};
    \begin{scope}[shift={(134mm,0mm)}]
      \fill[white, opacity=0.602]
        (0mm,4.80mm)  -- (0mm,1.25mm)   -- (3.00mm,-0.09mm)  -- cycle;
      \fill[white]
        (0mm,4.80mm)  -- (-3.00mm,-0.09mm) -- (0mm,1.25mm)   -- cycle;
      \fill[white, opacity=0.2]
        (0mm,-1.83mm) -- (3.00mm,-0.09mm)  -- (0mm,1.25mm)   -- cycle;
      \fill[white, opacity=0.602]
        (-3.00mm,-0.09mm) -- (0mm,-1.83mm) -- (0mm,1.25mm)   -- cycle;
      \fill[white, opacity=0.602]
        (0mm,-2.39mm) -- (0mm,-4.80mm)  -- (3.00mm,-0.65mm)  -- cycle;
      \fill[white]
        (0mm,-4.80mm) -- (0mm,-2.39mm)  -- (-3.00mm,-0.65mm) -- cycle;
    \end{scope}

    \node[lane, fill=orange!7, draw=orange!60!black] at (70mm,-33mm) {};
    \node[font=\normalsize\bfseries, anchor=west, orange!50!black] at (3mm,-24mm)
      {Chain A: Bitcoin-side UTXO ledger};
    \node[tx, fill=white, draw=orange!65!black] at (38mm,-34mm)
      {Alice output\\1 BTC};
    \node[tx, fill=orange!12, draw=orange!65!black] at (102mm,-34mm)
      {Bob address\\on Chain A};
    \draw[->, line width=1pt, orange!70!black] (56mm,-34mm) -- (84mm,-34mm);
    \node[lbl, orange!45!black] at (70mm,-43mm) {$\mathrm{tx}_1$ settles here};

    % Only the cryptographic dependency links the two ledgers; no coin crosses.
    % Publishing one completed signature lets the counterparty extract y using the
    % matching pre-signature -- y itself is never transmitted, so this box must NOT
    % say a settlement "reveals" it.  Keep this as the visual centre of the figure.
    % The two dashed connectors run BESIDE the box (x=44, x=96 clear its 50..90 span);
    % at the old coordinates they were drawn underneath it and never showed at all.
    \draw[<->, dashed, line width=0.9pt, green!40!black] (44mm,-46mm) -- (44mm,-70mm);
    \draw[<->, dashed, line width=0.9pt, green!40!black] (96mm,-46mm) -- (96mm,-70mm);
    \node[draw, rounded corners=3pt, fill=green!9, draw=green!45!black,
          align=center, font=\normalsize, minimum width=40mm,
          minimum height=14mm] at (70mm,-58mm)
      {\textbf{one settlement}\\enables $y$ extraction};

    \node[lane, fill=blue!7, draw=blue!60!black] at (70mm,-83mm) {};
    \node[font=\normalsize\bfseries, anchor=west, blue!50!black] at (3mm,-74mm)
      {Chain B: Ethereum account ledger};
    % NOT "Bob account -> Alice account".  Naming Ethereum makes this a venue-specific
    % claim, and on the EVM this project settles through an escrow contract:
    % AdaptorSwapBound.claimBound verifies the adapted signature supplied as calldata and
    % then transfers to the beneficiary.  fig:evmtx states the same thing -- the
    % authorising transaction signature stays secp256k1 and the lattice signature rides in
    % `data' as an argument to a contract -- so an ordinary account-to-account payment here
    % would contradict this report's own results chapter.
    \node[tx, fill=blue!12, draw=blue!60!black] at (38mm,-84mm)
      {Alice account\\on Ethereum};
    \node[tx, fill=white, draw=blue!60!black] at (102mm,-84mm)
      {Bob's ETH in\\a swap escrow};
    \draw[->, line width=1pt, blue!65!black] (84mm,-84mm) -- (56mm,-84mm);
    \node[lbl, blue!45!black] at (70mm,-93mm) {$\mathrm{tx}_2$ settles here};

    \node[draw, rounded corners=3pt, fill=green!8, draw=green!45!black,
          align=center, font=\normalsize, minimum width=138mm,
          minimum height=14mm] at (70mm,-106mm)
      % Two lines, not one: at 12pt this sentence pair is ~167mm on a single line and
      % pushed the picture 24pt past \textwidth.
      {\textbf{Both settle, or neither settles.}\\
       No custodian; no coin crosses between ledgers.};
  \end{tikzpicture}
  \caption[An atomic swap, in outline]{A motivation-level picture of an atomic
  swap, one concrete blockchain application of adaptor signatures. Alice and Bob trade
  assets recorded on two different ledgers: an unspent-transaction-output (UTXO) chain and
  an account-based one. Each payment settles on its own chain:
  $\mathrm{tx}_1$ moves Alice's bitcoin to an address Bob owns, while $\mathrm{tx}_2$
  releases Bob's escrowed ether to an account Alice owns. The two venues place a lattice
  signature differently, which is why the Ethereum leg settles through a contract rather
  than as a plain payment (\cref{fig:evmtx}). No coin
  crosses chains; the cross-chain object is the secret witness, which the counterparty extracts
  from the published signature using its matching pre-signature. Adaptor signatures provide this cryptographic link
  without requiring the swap condition to appear as a separate on-chain script. The two
  parties are named as in Esgin et al.\ \cite{esgin2020post}, whose $u_1$ and $u_2$ the
  later chapters use; \cref{fig:swapflow} sets the exchange out message by message.}
  \label{fig:swapidea}
\end{figure}

Cross-chain exchange first relied on custodial intermediaries; hash-time-locked contracts
removed the custodian by locking both transfers under one hash preimage, so claiming one
leg reveals the preimage unlocking the other. Such contracts need explicit
on-chain script support, place a common hash on both chains --- linking the legs --- and
enlarge the transactions. Moving the lock into the signature answers all three
\cite{poelstra2017,aumayr2021generalized}: settlements look like ordinary single-signer
payments, atomicity is retained, and the witness reaches only the counterparty holding
the matching pre-signature. The link removed is the explicit protocol one; timing and
amounts can still correlate the legs.

\section{Subject area and related work}
\label{sec:relatedwork}

%% The "Exotic signatures for post-quantum blockchains" entry that stood here stated the
%% buser2023survey point a THIRD time (it is already made in sec:motivation and again in
%% the adaptor paragraph, both citing the same survey).  Removed 2026-08-27 to pay for
%% the expanded adaptor motivation Meeting 12 asked for; the citation is not lost.
\paragraph{Lattice signatures and Dilithium.}
CRYSTALS-Dilithium is a lattice signature in the ``Fiat--Shamir with aborts'' family
\cite{ducas2018crystals,lyubashevsky2012lattice}, its security reducing to the Module
Learning With Errors and Module Short Integer Solution problems. It samples a masked
commitment, derives a challenge by hashing, computes a response, and \emph{rejects and
retries} when the response would leak the secret --- the rejection-sampling step
characterising the family. Its reference implementation provides the polynomial
arithmetic, number-theoretic transform (NTT) and SHAKE-based sampling this project
reuses.

\paragraph{Adaptor signatures.}
Introduced informally as ``scriptless scripts'' \cite{poelstra2017} and later formalised
\cite{aumayr2021generalized}, adaptor signatures add \textsc{PreSign}, \textsc{PreVerify},
\textsc{Adapt} and \textsc{Extract} to a base signature, relative to a hard relation
$(Y,y)$.
% The mechanism is NOT restated here: sec:motivation gives it in application terms and
% sec:construction defines all four functions with their equations.  This paragraph's
% job is related-work positioning.

\paragraph{Post-quantum adaptor signatures.}
Esgin et al.\ \cite{esgin2020post} proposed LAS, a lattice-based adaptor signature on a
Dilithium-style base; it is the design implemented here. Their work
is primarily theoretical --- definitions, a construction and security proofs --- leaving
the practical, benchmarked, blockchain-facing implementation open.
% IAS goes AFTER the "primarily theoretical" sentence, never before it: that judgement is
% about Esgin et al. alone, and IAS itself reports an implementation and an evaluation.
A second post-quantum route is isogeny-based: Tairi et al.\ \cite{tairi2021post} build IAS
on CSI-FiSh, prove it secure in the quantum random oracle model, and report an
implementation with measured costs. Kysil et al.\ \cite{kysil2025poqeth} put basic PQ
signature verification on Ethereum, ruling out Dilithium for its key sizes.

\paragraph{Classical baseline.}
The natural answer to ``what does post-quantum cost'' is a comparison with a
\emph{classical adaptor} signature, not merely plain ECDSA. Mature ECDSA-adaptor
implementations exist \cite{secp256k1zkp}; this project benchmarks against one
(\cref{ch:methodology}).

\section{Aim and objectives}
\label{sec:aims}

\paragraph{Aim.}
To implement and evaluate a post-quantum exotic signature scheme --- a lattice-based
adaptor signature reusing the CRYSTALS-Dilithium primitives --- and to demonstrate it
in a post-quantum blockchain atomic-swap application. The work is positioned as
\emph{systems implementation and evaluation}: it does not propose a new cryptographic
protocol.

\paragraph{Objectives.}
\begin{enumerate}
  \item Review post-quantum and exotic-signature literature sufficiently to justify
        selecting the adaptor signature LAS and the Dilithium primitives.
  \item Implement LAS additively on the Dilithium reference, reusing the lattice core
        unchanged and adding the adaptor operations and a byte-level wire encoding.
  \item Validate correctness and robustness through functional, serialization,
        tamper, and known-answer tests, including cross-validation by a second,
        independent implementation checked against the same known-answer value.
  \item Benchmark LAS fairly --- at explicitly stated parameters, with repeated runs and
        reported variance --- against a simplified Dilithium baseline at identical
        parameters and a classical ECDSA-adaptor baseline, separating computation from
        communication cost across the five standard signature metrics.
  \item Demonstrate an end-to-end post-quantum atomic swap using the implementation
        --- following the protocol of \cite{esgin2020post}, including its
        zero-knowledge proof of knowledge $\pi$ --- and discuss its feasibility.
\end{enumerate}

\section{Contributions}
\label{sec:contributions}

This dissertation contributes a tested, independently cross-validated implementation of a
post-quantum adaptor signature, and measurements of its cost. Four findings carry it.
% SCOPE IS PART OF THE CLAIM.  Two scopings here are mandatory, not stylistic.  (1) The
% TIER: "close to free" holds at the core tier; at the packed tier the premium is an order
% of magnitude higher (sec:res-compute), because each adaptor operation additionally decodes
% the statement Y.  (2) The BASE: Wang's Meeting-11 ruling is that "nearly free" must name
% what it is measured against.  "A basic signature" does not discharge that -- the base is
% named as the abstract names it, the simplified Dilithium-style base at identical
% parameters, never ECDSA, against which LAS is emphatically not cheap.  An unscoped version
% of this sentence asserted more than the measurement supports on both counts.
At the core tier, adaptor functionality measures close to free in computation over the
simplified Dilithium-style base at identical parameters, but expensive in communication:
bytes, not time (\cref{sec:res-compute,sec:res-comm}). The base in \cite{esgin2020post}
simplifies Dilithium by omitting its hint and rounding machinery. On the full
ML-DSA path, \textsc{PreSign} and
\textsc{PreVerify} require adaptor-specific algorithms, yet
an unmodified FIPS~204 verifier accepts the adapted signature (\cref{sec:res-mldsa}). At
application scale the bottleneck is not the signature but the zero-knowledge proof, leaving
the fully post-quantum swap the faster of the two such configurations and the only one Shor
does not break (\cref{sec:res-stage2}). Native on-chain verification, first
% "THE SAME PREDICATE" IS TRUE ONLY UNDER THE REGISTRATION INVARIANT.  tab:onchain records
% that neither verifier row checks it and that where it fails a DIFFERENT predicate is
% decided, so stating the equivalence flatly here contradicted this report's own table.  The
% signature instance belongs in the scope for the reason sec:res-evm gives: two verifier
% stages branch on coefficient values, so one client receipt is not a bound over inputs.
measured far above the transaction gas cap, fits under it once the same predicate is
re-expressed --- under an unchecked registration invariant, at the one parameter set,
message length and signature instance measured --- while a settled swap sits well inside
% NOT "a UTXO chain's weight limit".  Weight units are a Bitcoin/SegWit construct, not a
% property every UTXO chain has, and the measured comparison in sec:res-txstruct is against
% MAX_STANDARD_TX_WEIGHT -- Core's POLICY ceiling for a standard transaction, not a
% consensus rule.  Both halves of the scope have to travel with the claim.
Bitcoin's standard-transaction weight limit
(\cref{sec:res-evm,sec:res-txstruct}). All
five objectives were met (\cref{tab:objectives}), subject to two limits throughout: no
concrete security is formally analysed, and nothing ran on a live network.

\section{Dissertation structure}
\label{sec:structure}

\Cref{ch:methodology} describes the methods; \cref{ch:results} reports the measurements
and discusses their validity; \cref{ch:evaluation} judges the work against the
objectives; \cref{ch:conclusion} concludes, reflects critically, and proposes future
work.
